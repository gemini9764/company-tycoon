{
  "name": "company-tycoon",
  "version": "0.3.0",
  "description": "경영 시뮬레이션 프로토타입 — 구멍가게에서 시작해 M&A로 그룹을 키운다",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node tools/serve.mjs",
    "build": "node tools/build.mjs",
    "check": "node tools/modcheck.mjs && node tools/build.mjs >/dev/null && node tools/deadfield.mjs",
    "smoke": "npm run check && npm run build && node tools/smoke.mjs",
    "sim": "npm run build && node tools/sim.mjs",
    "test": "npm run smoke",
    "shot": "npm run build && node tools/shot.mjs"
  },
  "devDependencies": {
    "@napi-rs/canvas": "^1.0.3",
    "acorn": "^8.18.0",
    "jsdom": "^27.0.0"
  }
}
