# 렌더 규칙 — 읽지 않고 고치면 다시 흐려진다

화면이 흐릿하던 원인 세 가지를 잡으면서 생긴 제약이다. 캔버스나 CSS를 만질 때
이 문서의 규칙을 먼저 확인할 것.

> 스택 자체를 바꾸는 논의(three.js 이식)는 **`GRAPHICS.md`** 에 있다.
> 아래 폰트·정수 배율 규칙은 3D 로 가더라도 그대로 살아남는다 — 픽셀을 지키는
> 규칙이지 캔버스 2D 를 지키는 규칙이 아니다.

---

## 1. 폰트는 설계 크기에서만 또렷하다

Galmuri는 비트맵 폰트다. [배포 문서](https://github.com/quiple/galmuri) 기준
설계 크기는 다음과 같고, **그 값과 정수배에서만** 글자가 깨지지 않는다.

| 패밀리 | 설계 크기 | 쓸 수 있는 값 |
|---|---|---|
| Galmuri7 | 8px | 8, 16, 24 |
| Galmuri9 | 10px | 10, 20, 30 |
| Galmuri11 | 12px | 12, 24, 36 |
| Galmuri14 | 15px | 15, 30 |

이 프로젝트는 **10 / 12 / 15 세 단만** 쓴다. CSS 변수로 묶어 뒀다.

```css
--f-sm: Galmuri9   /* 10px — 보조 정보 */
--f:    Galmuri11  /* 12px — 기본 */
--f-lg: Galmuri14  /* 15px — HUD 수치, 모달 제목 */
```

**9px, 11px, 13px, 14px 를 쓰면 안 된다.** 이전 코드가 7~14px을 섞어 쓴 게
DOM 쪽 흐림의 직접 원인이었다.

## 2. 폰트 CDN 경로

`cdn.jsdelivr.net/gh/quiple/galmuri/dist/Galmuri11.css` 는 **404다.**
이 경로를 쓰던 동안 Galmuri는 한 번도 로드된 적이 없고, 전부 시스템 고딕으로
폴백돼 안티에일리어싱이 걸렸다. 정식 경로는 npm 배포본 하나다.

```html
<link href="https://cdn.jsdelivr.net/npm/galmuri/dist/galmuri.css" rel="stylesheet">
```

> `dist/company-tycoon.html` 은 이 링크를 외부 참조로 남긴다. 오프라인으로 열면
> 폰트가 없어 시스템 고딕으로 떨어진다. 완전 오프라인 배포가 필요해지면 woff2를
> base64로 인라인해야 하고, 그만큼 파일이 커진다.

## 3. 캔버스는 두 층으로 그린다

`render/canvas.js` 의 `draw()` 가 층을 가른다.

```
① 월드 층   setTransform(PX,0,0,PX,OX,OY)  — 도트를 찍는다
② 텍스트 층 setTransform(1,0,0,1,0,0)      — 글자를 찍는다 (drawText)
```

**PX는 반드시 정수다.** `fitCanvas()` 가 `floor(min(폭/월드폭, 높이/월드높이))`
로 잡는다. 분수 배율이면 어떤 월드 픽셀은 2칸, 어떤 픽셀은 3칸을 차지해서
도트가 울렁거린다. 남는 여백은 레터박스로 두고 `#0B0F1B` 로 채운다.

- 캔버스 백킹스토어 = CSS 픽셀 × DPR. **CSS로 캔버스를 늘리지 않는다.**
- 글자는 월드 층에 그리지 않는다. 확대되면서 픽셀 폰트가 깨진다.
  좌표는 월드 단위로 넘기고 `drawText(wx, wy, str, {size, color, align})` 이
  화면 좌표로 옮겨 준다. 크기는 10 / 12 / 15 중 하나.
- 라벨 배경 상자처럼 두 층을 맞춰야 하면 `textW(str, size)` 로 글자 폭을
  월드 단위로 환산해서 쓴다.
- **글자를 담는 상자는 월드와 같이 스케일하지 않는다.** 글자는 월드가 커져도
  안 커지므로, 상자를 같이 키우면 상자만 헐거워진다. 폰트 크기에서 뽑을 것 —
  `drawLabel` 의 `h = size + 6`, `flushPlates` 의 `bh = 16`.

배율은 **카메라**가 쥔다 (`canvas.js` 의 `applyCamera`).

- **도시** — 월드 1536×824. `fitPX × zoom` 이고 `zoom` 은 **1·2·3 정수만**.
  1x 는 전체 보기(1080p 캔버스에 세로가 딱 들어간다), 그 위는 드래그로 훑는다.
- **사옥** — 월드 624×404, 화면 맞춤 고정. 카메라가 없다. 방 하나가 통째로
  들어오는 배치가 읽기 쉽다. `PX=2` 에 808px 이 필요하니 여기를 40px 만 키우면
  배율이 1로 떨어져 화면이 절반이 된다.

> **캔버스 CSS 크기를 JS 에서 px 로 박지 말 것.** 스타일시트가 `100%` 로 쥐고
> 있는데 JS 가 px 를 덮으면, 레이아웃이 잡히기 전에 잰 값이 그대로 굳는다.
> 실제로 백킹이 래퍼보다 150px 커진 채 남았고, 모드를 한 번 바꿔야 제자리로
> 돌아왔다. `fitCanvas` 는 **백킹스토어만** 맞추고, 크기 변화는
> `ResizeObserver` 로 잡는다.

### 카메라

| | |
|---|---|
| 이동 | 드래그. 4 CSS 픽셀을 넘게 움직이면 뒤따르는 `click` 을 버린다 |
| 확대 | 휠 · 독의 `+`/`−` · 키보드 `+`/`−`. **정수 배율 1~3x** |
| 기준점 | 확대는 포인터 밑 월드 좌표를 고정한다 |
| 한계 | 지면 레이어가 구워진 범위(`CITY_PAD`)까지만 나간다 |
| 초기화 | `S` 가 바뀌면(새 게임·불러오기) 1x 가운데로 |

드래그 판정을 빼면 맵을 끌 때마다 회사 창이 열린다. 클릭과 드래그는 반드시 가른다.

## 4. 쿼터뷰 규칙 (`render/iso.js`)

2:1 아이소메트릭. 타일 한 장은 **48×24** 마름모다 (2단계 이전 32×16).
도시는 `blockPitch`(4) 주기로 2칸이 블록, 2칸이 도로다. pitch 가 4의 배수라
`gx → W-1-gx` 로 뒤집어도 도로 판정이 그대로 성립한다 — **회전이 안 깨진다.**

**`ctx.fill()` 로 마름모를 그리면 안 된다.** 사선이 안티에일리어싱되어 도트가
아니게 된다. 모든 도형은 `fillRect` 로 계단을 쌓아 만든다. 이미 만들어 둔 것:

| 함수 | 쓰임 |
|---|---|
| `rhomb` | 마름모 한 장 (바닥 타일, 지붕) |
| `rhombEdge` | 마름모 테두리 (구획선, 강조) |
| `prism` | 상자 — 윗면 + 좌하·우하 두 면 |
| `faces` | 옆면의 높이 구간만 칠하기 (간판띠, 처마) |
| `isoWin` | 옆면에 붙는 창문. 2px 세로줄을 어긋나게 쌓아 벽에 붙인다 |

지켜야 할 것:

- **바닥은 정사각(w == d)만 쓴다.** 직사각 바닥은 마름모가 아니라 평행사변형이
  되어 `prism` 이 못 그린다. 길쭉한 물체(차량)는 상자 두 채를 진행 축으로
  겹쳐 만든다.
- `rx` 는 항상 `ry` 의 2배. 아니면 2:1 기울기가 깨진다. **`rx` 는 반드시 짝수다** —
  홀수면 마름모 폭이 한 줄씩 어긋나 사선이 지저분해진다. 삼항으로 `rx` 를 줄 때
  (`big ? 16 : 12`) **`ry` 도 같이 삼항으로 줘야 한다.** 한쪽만 고치면 비율이 깨진다.
- **깊이 정렬은 바닥 y 로 한다.** 투영식이 `y = (gx+gy) * 8` 이라 y 순서가
  곧 `gx+gy` 순서다. 그릴 것을 `{y, f}` 로 모아 `sort((a,b) => a.y - b.y)`
  한 뒤 실행한다.
- **상호판·라벨은 정렬 뒤에 따로 얹는다.** 건물과 같이 그리면 앞 건물에 가린다.
  (`city.js` 의 `plates` / `flushPlates`)
- 지면 타일은 프레임마다 수백 장이라 `makeLayer` 로 한 번 굽고 `drawImage`
  한다. 캐시는 `builtFor === S` 로 무효화한다 — 새 게임이나 불러오기로 `S` 가
  바뀌면 다시 굽는다.
- **레터박스는 지면 레이어를 월드보다 크게 구워 덮는다.** 월드 변환은 캔버스를
  자르지 않으므로 `drawImage(layer, -PAD_X, -PAD_Y)` 로 붙이면 여백까지 그림이
  들어간다. 월드 크기(배율 계산의 분모)는 그대로라 **좁은 창에서 배율이 안 떨어진다** —
  월드 자체를 넓히면 그 순간 배율이 한 단계 내려간다. (`CITY_PAD_X/Y`, `drawOutskirts`)

### 캐릭터

`drawPerson(x, y, look, dir, step)` — 카이로소프트 톤(머리가 몸통만큼 크고
어두운 아웃라인). `dir` 은 `s`/`n`/`e`/`w`, `step` 은 걷기 위상이다.

**`step` 은 반드시 이동 거리로 뽑는다.** 프레임 수나 좌표(`frame + p.x`)로
뽑으면 소수점 위치가 매 프레임 흔들려 발이 떨리는 것처럼 보인다.
누적 이동량을 따로 들고 `Math.floor(walk / 5)` 처럼 쓸 것.

### 카메라 회전

`view`(0~3)는 **타일 좌표만 돌린다.** 투영식은 손대지 않는다.
`city.js` 의 모든 타일→월드 변환은 `cityX` / `cityY` 를 지나야 한다.
여기를 우회해 `isoX` / `isoY` 를 직접 부르면 **그 요소만 회전을 안 따라가고,
보이는 위치와 클릭 위치가 어긋난다.**

방향을 타는 것은 셋뿐이다 — 도로 차선(홀수 뷰에서 rx·ry 스왑), 보행자 방향
(`rotFace`), 차체 진행축. 건물 몸통은 `prism` 이 좌우 대칭이라 그대로다.
지면 레이어는 `bakedView` 로 캐시를 무효화한다.

#### 전환 애니메이션

예전에는 화면을 어둡게 덮고 그 사이에 방향을 바꿨다. **"2D 라 중간 각도를 못
그린다"는 전제가 틀렸다** — 투영식은 그대로 두고 **타일 좌표만 실수로 돌리면**
중간 각도가 나온다 (`rotGf`). 건물은 그 궤적을 따라 호를 그리며 옮겨 가고,
스프라이트 자체는 세워 둔다.

지면 레이어만 프레임마다 다시 구울 수 없다. 그래서 구워 둔 비트맵 두 장
(이전 방향 · 새 방향)에 **같은 회전의 화면 좌표 변환**을 걸어 교차 페이드한다.
그 변환이 `isoRotMat` 이고, `M = P·R(a)·P⁻¹` 를 풀면 `[[cos, -k·sin], [sin/k, cos]]`,
`k = HW/HH = 2` 다. 타일 공간 회전과 **정확히 같은 변환**이라 건물과 지면이
어긋나지 않는다. 교외에 구워 둔 집·나무만 전환 중 잠깐 기울어진다.

지켜야 할 것:

- **지면을 굽는 동안 `viewA()` 는 정수를 돌려줘야 한다.** 중간 각도로 구우면
  그 프레임의 그림이 통째로 캐시에 박힌다 (`baking` 플래그).
- 도는 중에는 호버·클릭을 받지 않는다 (`rotating()`).

검수: `npm run shot` → `shots/view0..3.png` 와 **`shots/rot-mid.png`**(전환 50%).
시작·끝은 view0..3 로 보이지만 정작 봐야 하는 건 도는 도중이다.

### 클릭 판정

**사각형 근사로 판정하지 않는다.** 마름모 기둥의 실루엣은 그 x 에서의 세로 반경이
`(1 - |dx|/rx) * ry` 다. 사각형으로 잡으면 건물 모서리 바깥의 빈 하늘까지
앞 건물이 먹어서 뒤 건물을 못 집는다. (`city.js` 의 `isoHit`)

**상호판은 히트박스이기도 하다.** `flushPlates` 가 건물을 전부 그린 뒤 얹으므로
절대 가려지지 않는다. 이걸 클릭 타깃으로 쓰면 뒤에 완전히 숨은 회사도 집힌다.
`plateBoxes` 를 매 프레임 다시 채우고, 판정은 **역순**으로 — 나중에 그린 게 위다.

상호판을 옮기거나 그리는 순서를 바꾸면 이 판정도 같이 깨진다. 셋이 한 몸이다.

1. 그리는 상자 — `X.fillRect(x, y, w, bh)`
2. 판정 상자 — `plateBoxes.push({ x, y, w, h: bh })` — **같은 `bh` 를 봐야 한다**
3. 스모크 — `tools/smoke.mjs` 가 `g.y + 29` 로 상호판을 찍어 본다. 위치를 옮기면
   이 상수도 같이 옮긴다

### 손님 경로

매장은 타일 격자다. `SHOP_BLOCK` 에 집기 칸을 넣어 두고 `findPath` 가 너비
우선으로 길을 찾는다. 집기를 추가하면 **`SHOP_BLOCK` 에도 넣어야** 손님이
뚫고 지나가지 않는다. 목적지는 통행 가능한 칸이어야 한다 — 진열대 자체가
아니라 그 앞 칸(`gy + 1`)을 목표로 준다.

## 5. 번들러가 스코프를 합친다 — 여기서 한 번 크게 물렸다

번들러가 모듈을 한 스코프로 이어 붙인다. 겹치면 빌드가 멈춘다.
(예: `drawPlant` 가 city와 store 양쪽에 생겨서 store 쪽을 `drawPotPlant` 로 개명)

> **더 위험한 건 반대 방향이다.** 스코프가 합쳐지므로 **import 를 빠뜨려도
> 다른 모듈의 최상위 이름이 그냥 보인다.** 번들에서는 멀쩡히 돌고, `src/` 를
> 그대로 로드할 때만 `ReferenceError` 로 죽는다.
>
> 실제로 `canvas.js` 가 `HH` 를 import 하지 않고 쓴 적이 있다. smoke·shot·sim 이
> 전부 `dist` 를 띄우므로 **검증은 전부 통과했고, GitHub Pages 배포본만 흰
> 화면이 나왔다.** README 의 '바로 플레이'가 `src/` 를 직접 서빙하기 때문이다.
>
> 그래서 `npm run check`(`tools/modcheck.mjs`)를 두었다. 진짜 ESM 그래프를
> 노드에서 평가하고 두 모드·회전 4방향·40일 진행·UI 경로를 한 번씩 통과시킨다.
> **`npm run smoke` 가 이걸 먼저 돌린다.** 렌더를 고쳤으면 반드시 같이 볼 것.

**import 는 한 줄로 쓴다.** 번들러가 줄 단위로 파싱하므로 여러 줄에 걸친 import 는
걷어내지 못하고 그대로 남아 `Cannot use import statement outside a module` 로 터진다.

그리고 **모듈 최상위에서 다른 모듈의 `const` 를 읽으면 안 된다.**
번들은 의존 순서대로 붙으므로 `store.js` 가 `canvas.js` 보다 먼저 평가된다.
`store.js` 최상위에서 `STORE_H` 를 읽으면 TDZ로 터진다. 파생 상수는
`canvas.js` 쪽에 두고 import 해서 **함수 안에서만** 쓴다. (`FOOT_Y` 가 그 예)

## 6. 검수

```bash
npm run smoke   # 64 케이스. 캔버스는 스텁이라 "안 터진다"까지만 본다
npm run shot    # shots/{title,city,store,icons,view0..3,facil0,facil3}.png
                # + shots/style-{tower,lab,plant,neon,shop}.png (건물 5종 3x)
                # — 실제 픽셀로 그려서 눈으로 본다
```

`tools/shot.mjs` 는 `@napi-rs/canvas` 로 진짜 2D 컨텍스트를 물려 두 모드를
PNG로 떨군다. 노드에는 Galmuri가 없어 시스템 CJK 폰트를 같은 이름으로 등록하므로
**글자 모양은 실제와 다르다.** 배치와 겹침 확인용이다.

---

## 7. 색 — 파스텔 팔레트

맵은 채도를 낮추고 명도를 올린 파스텔로 간다. 색을 새로 넣을 때 지킬 것.

- **건물 몸통은 밝은 쪽으로 섞는다.** `mix(sec.color, '#F0EEE4', 0.30)`.
  어두운 색과 섞으면 아무리 원본이 파스텔이어도 탁해진다.
- **밝은 바닥 위에서는 그림자가 회색 얼룩으로 뜬다.** `globalAlpha` 는
  0.13~0.17 대를 쓴다. 0.24 를 그대로 두면 건물마다 때가 낀 것처럼 보인다.
- **창문이 묻힌다.** 몸통이 밝아진 만큼 소등 창(`#7F8DAB`)은 더 어둡게,
  점등 창(`#FFE79E`)은 더 노랗게 잡아야 층이 읽힌다.
- **상호판은 어두운 판 그대로 둔다.** 밝은 맵 위에서 대비가 커져 오히려 잘 읽힌다.
  파스텔로 같이 눕히면 글자가 사라진다.
- 아웃라인(`OUT`)은 파스텔 바닥에서도 실루엣이 남는 진하기여야 한다 (`#40384F`).

## 8. 건물 — 마감 프리미티브

다섯 종(`tower`/`lab`/`plant`/`neon`/`shop`)이 `pane`·`windowGrid`·`floorBands`·
`cornerPost`·`parapet`·`roofDeck`·`awning` 을 공유하고, 그 위에 특징만 얹는다.
새 건물을 만들 때도 이 뼈대를 먼저 깐다.

- **창 열 자리를 상수로 박지 말 것.** `isoWin` 은 `d > rx - 2` 인 열을 조용히
  버린다. `10 + i*12` 처럼 고정 좌표를 쓰면 `rx=30` 짜리 건물에서 바깥 열이
  통째로 사라진다. `windowGrid` 가 rx 로 열 수를 계산하니 그걸 쓸 것.
- **`off` 는 짝수여야 한다.** `isoWin` 이 2px 단위로 훑기 때문이다.
- **1층(`drawSign`)은 15px 를 넘기지 말 것.** 상가는 몸통이 33px 뿐이라
  여기가 두꺼우면 창을 넣을 벽이 안 남는다.
- **지붕을 비워 두지 말 것.** 쿼터뷰는 지붕이 화면의 3분의 1을 먹는다.
  `parapet` + `roofDeck` 없이는 아무리 벽을 잘 그려도 잘라 놓은 상자로 보인다.
- 검수는 `shots/style-*.png` 다섯 장. 업종을 통째로 바꿔 3x 로 찍으므로
  섞여 있는 도시 그림에서는 안 보이던 마감이 드러난다.

## 9. 사옥 집기

도시와 같은 방법을 쓴다 — 모서리를 깎고(`rrect`), 면을 층으로 나누고
(걸레받이·허리 몰딩·상판 테두리), 유리에 틀과 대각 반사를 넣는다(`glassPanel`).
**색은 도시와 달리 그대로 두었다.** 실내는 이미 톤이 잡혀 있었다.

- **집기가 차지하는 타일과 크기는 건드리지 말 것.** `blockedAt` 과 손님 경로가
  거기에 묶여 있어 크기를 바꾸면 통행이 막힌다. 마감만 얹는다.
- 스모크가 **시설 최대 상태에서 모든 진열대에 경로가 닿는지** 검사한다.

## 10. 캐릭터

`drawPerson(x, y, look, dir, step)` — 22×44. 발밑이 (x, y) 다.

- **모서리 한 픽셀을 깎는다** (`rrect`). 각진 `fillRect` 를 그대로 쌓으면
  사람이 아니라 상자로 보인다. 카이로소프트 톤의 둥근 실루엣이 전부 여기서 나온다.
- 팔은 몸통보다 **먼저** 그려 뒤로 보낸다. 나중에 그리면 가슴 위에 붙는다.
- 머리 모양 4종을 `look.style` 이 고른다. 수십 명이 동시에 서 있으므로
  실루엣이 갈려야 서로 달라 보인다.
- **`step` 은 반드시 이동 거리로 뽑는다** (§4 캐릭터 참고). 프레임이나 좌표로
  뽑으면 발이 떨린다.
- `look` 은 **연출 스트림**(`vpick`/`vrint`)으로 뽑는다. 게임 난수를 쓰면
  사람이 몇 명 태어났는지가 인수 판정을 흔든다 (`core/rng.js`).

