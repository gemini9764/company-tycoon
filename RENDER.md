# 렌더 규칙 — 읽지 않고 고치면 다시 흐려진다

화면이 흐릿하던 원인 세 가지를 잡으면서 생긴 제약이다. 캔버스나 CSS를 만질 때
이 문서의 규칙을 먼저 확인할 것.

---

## 1. 폰트는 설계 크기에서만 또렷하다

Galmuri는 비트맵 폰트다. [배포 문서](https://github.com/quiple/galmuri) 기준
설계 크기는 다음과 같고, **그 값과 정수배에서만** 글자가 깨지지 않는다.

| 패밀리 | 설계 크기 | 쓸 수 있는 값 |
|---|---|---|
| Galmuri7 | 8px | 8, 16, 24 |
| Galmuri9 | 10px | 10, 20, 30 |
| Galmuri11 | 12px | 12, 24, 36 |
| Galmuri14 | 15px | 15, 30 |

이 프로젝트는 **10 / 12 / 15 세 단만** 쓴다. CSS 변수로 묶어 뒀다.

```css
--f-sm: Galmuri9   /* 10px — 보조 정보 */
--f:    Galmuri11  /* 12px — 기본 */
--f-lg: Galmuri14  /* 15px — HUD 수치, 모달 제목 */
```

**9px, 11px, 13px, 14px 를 쓰면 안 된다.** 이전 코드가 7~14px을 섞어 쓴 게
DOM 쪽 흐림의 직접 원인이었다.

## 2. 폰트 CDN 경로

`cdn.jsdelivr.net/gh/quiple/galmuri/dist/Galmuri11.css` 는 **404다.**
이 경로를 쓰던 동안 Galmuri는 한 번도 로드된 적이 없고, 전부 시스템 고딕으로
폴백돼 안티에일리어싱이 걸렸다. 정식 경로는 npm 배포본 하나다.

```html
<link href="https://cdn.jsdelivr.net/npm/galmuri/dist/galmuri.css" rel="stylesheet">
```

> `dist/company-tycoon.html` 은 이 링크를 외부 참조로 남긴다. 오프라인으로 열면
> 폰트가 없어 시스템 고딕으로 떨어진다. 완전 오프라인 배포가 필요해지면 woff2를
> base64로 인라인해야 하고, 그만큼 파일이 커진다.

## 3. 캔버스는 두 층으로 그린다

`render/canvas.js` 의 `draw()` 가 층을 가른다.

```
① 월드 층   setTransform(PX,0,0,PX,OX,OY)  — 도트를 찍는다
② 텍스트 층 setTransform(1,0,0,1,0,0)      — 글자를 찍는다 (drawText)
```

**PX는 반드시 정수다.** `fitCanvas()` 가 `floor(min(폭/월드폭, 높이/월드높이))`
로 잡는다. 분수 배율이면 어떤 월드 픽셀은 2칸, 어떤 픽셀은 3칸을 차지해서
도트가 울렁거린다. 남는 여백은 레터박스로 두고 `#0B0F1B` 로 채운다.

- 캔버스 백킹스토어 = CSS 픽셀 × DPR. **CSS로 캔버스를 늘리지 않는다.**
- 글자는 월드 층에 그리지 않는다. 확대되면서 픽셀 폰트가 깨진다.
  좌표는 월드 단위로 넘기고 `drawText(wx, wy, str, {size, color, align})` 이
  화면 좌표로 옮겨 준다. 크기는 10 / 12 / 15 중 하나.
- 라벨 배경 상자처럼 두 층을 맞춰야 하면 `textW(str, size)` 로 글자 폭을
  월드 단위로 환산해서 쓴다.

1920×1080에서 실제 배율은 도시 2배, 사옥 3배다.

> **세로가 배율의 병목이다.** 캔버스 높이 ≈ 865px 이라 도시 월드 높이가 432를 넘으면
> 배율이 1로 떨어져 맵이 확 작아진다. 현재 412로 잡아 두었다. 상단 독이나 뉴스 바를
> 키워 무대가 40px 이상 줄면 이 한계에 걸린다. 맵을 키우고 싶으면 월드 높이가 아니라
> **타일 수**를 늘려야 한다 — 같은 배율에서 더 큰 도시가 담긴다.

## 4. 쿼터뷰 규칙 (`render/iso.js`)

2:1 아이소메트릭. 타일 한 장은 32×16 마름모다.

**`ctx.fill()` 로 마름모를 그리면 안 된다.** 사선이 안티에일리어싱되어 도트가
아니게 된다. 모든 도형은 `fillRect` 로 계단을 쌓아 만든다. 이미 만들어 둔 것:

| 함수 | 쓰임 |
|---|---|
| `rhomb` | 마름모 한 장 (바닥 타일, 지붕) |
| `rhombEdge` | 마름모 테두리 (구획선, 강조) |
| `prism` | 상자 — 윗면 + 좌하·우하 두 면 |
| `faces` | 옆면의 높이 구간만 칠하기 (간판띠, 처마) |
| `isoWin` | 옆면에 붙는 창문. 2px 세로줄을 어긋나게 쌓아 벽에 붙인다 |

지켜야 할 것:

- **바닥은 정사각(w == d)만 쓴다.** 직사각 바닥은 마름모가 아니라 평행사변형이
  되어 `prism` 이 못 그린다. 길쭉한 물체(차량)는 상자 두 채를 진행 축으로
  겹쳐 만든다.
- `rx` 는 항상 `ry` 의 2배. 아니면 2:1 기울기가 깨진다. `rx` 는 짝수로 둘 것.
- **깊이 정렬은 바닥 y 로 한다.** 투영식이 `y = (gx+gy) * 8` 이라 y 순서가
  곧 `gx+gy` 순서다. 그릴 것을 `{y, f}` 로 모아 `sort((a,b) => a.y - b.y)`
  한 뒤 실행한다.
- **상호판·라벨은 정렬 뒤에 따로 얹는다.** 건물과 같이 그리면 앞 건물에 가린다.
  (`city.js` 의 `plates` / `flushPlates`)
- 지면 타일은 프레임마다 수백 장이라 `makeLayer` 로 한 번 굽고 `drawImage`
  한다. 캐시는 `builtFor === S` 로 무효화한다 — 새 게임이나 불러오기로 `S` 가
  바뀌면 다시 굽는다.
- **레터박스는 지면 레이어를 월드보다 크게 구워 덮는다.** 월드 변환은 캔버스를
  자르지 않으므로 `drawImage(layer, -PAD_X, -PAD_Y)` 로 붙이면 여백까지 그림이
  들어간다. 월드 크기(배율 계산의 분모)는 그대로라 **좁은 창에서 배율이 안 떨어진다** —
  월드 자체를 넓히면 그 순간 배율이 한 단계 내려간다. (`CITY_PAD_X/Y`, `drawOutskirts`)

### 캐릭터

`drawPerson(x, y, look, dir, step)` — 카이로소프트 톤(머리가 몸통만큼 크고
어두운 아웃라인). `dir` 은 `s`/`n`/`e`/`w`, `step` 은 걷기 위상이다.

**`step` 은 반드시 이동 거리로 뽑는다.** 프레임 수나 좌표(`frame + p.x`)로
뽑으면 소수점 위치가 매 프레임 흔들려 발이 떨리는 것처럼 보인다.
누적 이동량을 따로 들고 `Math.floor(walk / 5)` 처럼 쓸 것.

### 손님 경로

매장은 타일 격자다. `SHOP_BLOCK` 에 집기 칸을 넣어 두고 `findPath` 가 너비
우선으로 길을 찾는다. 집기를 추가하면 **`SHOP_BLOCK` 에도 넣어야** 손님이
뚫고 지나가지 않는다. 목적지는 통행 가능한 칸이어야 한다 — 진열대 자체가
아니라 그 앞 칸(`gy + 1`)을 목표로 준다.

## 5. 최상위 이름은 전부 고유해야 한다

번들러가 모듈을 한 스코프로 이어 붙인다. 겹치면 빌드가 멈춘다.
(예: `drawPlant` 가 city와 store 양쪽에 생겨서 store 쪽을 `drawPotPlant` 로 개명)

그리고 **모듈 최상위에서 다른 모듈의 `const` 를 읽으면 안 된다.**
번들은 의존 순서대로 붙으므로 `store.js` 가 `canvas.js` 보다 먼저 평가된다.
`store.js` 최상위에서 `STORE_H` 를 읽으면 TDZ로 터진다. 파생 상수는
`canvas.js` 쪽에 두고 import 해서 **함수 안에서만** 쓴다. (`FOOT_Y` 가 그 예)

## 6. 검수

```bash
npm run smoke   # 25 케이스. 캔버스는 스텁이라 "안 터진다"까지만 본다
npm run shot    # shots/{city,store,icons}.png — 실제 픽셀로 그려서 눈으로 본다
```

`tools/shot.mjs` 는 `@napi-rs/canvas` 로 진짜 2D 컨텍스트를 물려 두 모드를
PNG로 떨군다. 노드에는 Galmuri가 없어 시스템 CJK 폰트를 같은 이름으로 등록하므로
**글자 모양은 실제와 다르다.** 배치와 겹침 확인용이다.
