# M&A 모드 확장 — 세션 인수인계

> 이 문서만 읽고 새 세션에서 바로 이어서 작업할 수 있도록 정리했다.
> 프로젝트 전반은 `HANDOFF.md`, 원래 계획은 `MNA_PLAN.md`, 원 기획서는 `NHN_사전과제.md`.
> **`MNA_PLAN.md` 는 여러 군데가 계측으로 뒤집혔다.** 그 문서를 그대로 따르지 말고
> 이 문서의 §2 를 먼저 읽을 것.

---

## 0. 한 줄 요약

> **[최신] 감사 패스가 끝났다.** 계획(단계 1~5 · 감축 A~D · §11~13)은 전부 들어가
> 있었지만, **문서가 코드보다 뒤처져 기준선 표가 재현되지 않았고** 그 사이에
> 규칙 결함 네 개가 자라 있었다. 전부 고치고 6시드로 다시 잡았다. → **§14**

**§14 를 먼저 읽을 것.** 아래 §0~§13 은 그때까지의 경위이고, **§3 · §10-6 · §12-6 의
기준선 표는 전부 폐기됐다.** 현재 값은 §16-4 하나뿐이다.

현재 상태: **smoke 197/197**, `npm run check` 통과, **`SAVE_V 13`**.

```
5a61f3b  단계 1 — 계열사 특성
60397f8  단계 2 — 협상 중 능동 개입 · sim 능동 운영 전략 · 등급 구간 계측 수정
a3eb09d  단계 3 — 밑작업 통합 + 미리 사두기
148de84  docs: 인수인계 문서
e21c620  단계 3-b — 비상장 지분 매입 (D안)
9997264  단계 4 — 협상 테이블
         단계 5 — 매물 경쟁 · 협상 기간 단축 · 성장 곡선 조정
11bc87f  감축 A — 노출 수치 축소 · 용어 정리 (밸런스 무영향)
a060156  감축 B — 개입 4종 → 2종 · 정보 공개를 밑작업으로 이관
cfcbeef  감축 C — 협상 테이블을 단판으로 · 한 수의 무게를 키웠다
         라이벌 그룹에 이름과 성격
         감사 패스 — 죽은 선택지·죽은 기능·무료 디폴트·등급 구간 (§14)
         첫 M&A 를 40일차로 — 스타터 매물 (§15)
(HEAD)   조용한 버그 세 건 — 경험치·닫기 경로·죽은 선택지 (§16)
```

> **방향이 바뀌었다.** 287일 구간을 *줄이는* 것은 포기했다 (네 번 실패). 경영 쪽
> 콘텐츠(하우징 등)는 별도로 기획 중이므로 이쪽에서 건드리지 않는다.
> 목표는 **그 구간 안에서 플레이어가 개입할 여지를 늘리는 것**이다.

---

## 1. 이번 세션에 들어간 것

### 1-1. 단계 2 — 협상 중 능동 개입 (`60397f8`)

협상 1건당 **3회**. 회사 창의 기존 `진행 중 M&A` 블록에 붙였다 (새 창 없음, 잔여는 `◆◆◇`).

| 개입 | 효과 | 대가 |
|---|---|---|
| 접대비 집행 | 성공도 +12 | 자금 · **수사 +3** |
| 시한 제시 | 진행도 +30 | 성공도 −8 |
| 실사 요청 | 숨은 특성 공개 | 자금 · 진행도 −10 |
| 협상 중단 | 즉시 종료 · 협상단 복귀 | 위약금 = 예상 인수가 × 3% |

- 판정은 전부 `systems/mna.js:negoAct()`. UI 는 껍데기이고 sim 봇도 같은 함수를 부른다
- 하위 호환은 `negoLeft(n) = n.acts ?? BAL.negoActs` 하나로 처리했다. 구버전 세이브의
  **진행 중 협상**에는 `acts` 가 없다

### 1-2. 단계 3 — 밑작업 통합 + 미리 사두기 (`a3eb09d`)

회사 팝업 선택지를 두 단으로 갈랐다 (`modal.js` 에 선택지 머리말 `head` 지원 추가).

```
[ 밑작업 — 협상 전에 깔아 두는 것 ]
  이 회사 주식 조금씩 사둔다  ★★★☆☆
  사둔 주식을 되판다 · 찌라시 사용 · 살굿 의뢰
────────────────────────────────
[ 준비가 끝나면 ]  협상단 파견
```

- 매일 `시총 × 0.012` 자동 매집 → ★ 5칸 (**1칸 = 지분 3%**)
- ★ 1칸당 협상 **성공도 시작값 +6 · 인수가 −3%p**
- **★★★ 초과 시 소문** — 주가 +25% · 인수가 +8%p · 난이도 1단계 상승
- 인수 시 지분 흡수 / 결렬 시 지분 유지 → `되판다`
- 자금 부족 시 자동 해제 + 토스트 + 알림 (조용히 멈추지 않는다)
- `mna.js:startNego` 가 `stakeBonus()` 를 읽는다 — **주식과 M&A 가 이어지는 유일한 지점**

**계수를 왜 이렇게 잡았나:** ★ 1칸에 드는 돈(지분 3%)과 그 칸이 주는 인수가 할인(3%p)이
**같다.** 가격으로는 본전이고 얻는 것은 성공도뿐이며, 대가는 현금이 2주 묶이는 것이다.
할인을 비용보다 크게 두면 "항상 켜는 게 정답"이 되어 판단이 사라진다.

### 1-3. sim 봇 — `active` 전략과 스위치 2개

- **`active`** — 매각/투자/재편을 쓰는 전략. 전부 `systems/subs.js` 함수를 그대로 호출한다
- **`--acts`** — 협상 중 개입을 쓰는 봇
- **`--stake`** — 미리 사두기를 쓰는 봇

`--desk` 와 같은 관례다. 기본 봇에 켜면 기준선이 통째로 바뀌므로 스위치로 분리했다.
협상 성사/결렬 집계와 개입 사용 내역도 sim 출력에 붙였다.

---

## 2. 계측으로 뒤집힌 전제 — **여기가 이 문서의 핵심**

### 2-1. 등급 구간 표가 한 칸씩 밀려 있었다

`sim.mjs` 가 각 행의 구간을 *직전 행의 도달일* 로 계산하고 있었다.
그래서 지금까지 문서에 적힌 값들이 **엉뚱한 등급을 가리키고 있었다.**

| 문서에 적혀 있던 것 | 실제 |
|---|---|
| "중견기업 구간 279~287일 — 최장" | **중소기업 구간** |
| "글로벌그룹 구간 17일 — 등급 낭비" | **대기업 구간 25일** |

수정 후 (합계 830일 = 무차입 기준선과 일치, 별도 probe 로 교차 검증):

```
구멍가게    59일     중소기업   287일  ← 최장 (34%)
동네슈퍼   115일     중견기업   118일
스타트업    58일     대기업      25일  ← 등급 하나가 낭비된다
                    글로벌그룹  168일  (등극 → 시총 1위)
```

> **교훈:** 계측 도구를 믿기 전에 **합계가 아는 값과 맞는지** 한 번 대조할 것.

### 2-2. 287일 구간은 '기다리기'가 아니다

| | 완주 | 중소기업 구간 |
|---|---|---|
| 기준선 | 830일 | **287일** |
| 처리량 2배 (협상단 2팀의 천장) | 700일 (−130) | 263일 (−24) |
| 결렬 0% (미리 사두기의 천장) | 730일 (−100) | 271일 (−16) |
| 둘 다 | 657일 (−173) | 276일 (−11) |
| 등급 목표에 계열사 자산 포함 | 860일 | 266일 (−21, 대신 중견 118→159) |

**전체 완주는 크게 줄어드는데 287일 구간은 어느 것으로도 안 움직인다.**

구간 안을 들여다보면 이렇다.

```
협상 착수 16~20회 · 협상 중 237~298일 / 놀고 있음 2~45일 (1~14%)
인수 규모: 2.0억 → 4.0억 → 14억 → 57억 → 109억   (사다리를 오른다)
```

등급 진입 시점의 장부 (시드 1001):

```
중소기업 진입:  순자산 3.3억   시총 241억    계열사 3개
중견기업 진입:  순자산 377억   시총 1.02조   계열사 19개
```

**287일의 정체는 시총을 241억 → 1조로 42배 불리는 복리 성장 시간이다.**
협상을 두 배로 빨리 돌리면 계열사가 28 → 33개로 늘 뿐, 그만큼 현금을 더 쓰므로
등급 관문에 닿는 시점은 그대로다. 관문을 어디에 놓아도(순자산 → 자산 포함) 마찬가지다.

→ **이 구간은 M&A 루프로 건드릴 수 없다.** §5 참조.

### 2-3. 매각은 자금원으로 성립하지 않는다 (MNA_PLAN §12-1 의 답)

`active` 봇의 갈아타기 문턱을 바꿔가며 계측했다.

| 갈아타기 문턱 | 완주 | 매각 횟수 |
|---|---|---|
| 1.15배 | 916일 (**−10% 손해**) | 19 / 6 / 7 |
| 2.0배 | 823일 (본전) | 6 / 1 / 0 |

산식으로도 맞는다 — `rot` 매물은 **인수 0.81C + 투자 0.25C = 1.06C** 를 넣고
매각으로 **0.952C** 를 회수해 −10% 다. 게다가 계열사 회수기간이 26일이라
**어떤 매각가에서도 보유가 낫다.** 매각가를 올리면 이번엔 사고팔기 회전이 이득이 되어
남용된다.

→ **계수 튜닝으로 풀 문제가 아니다.** 매각의 존재 이유를 자금 조달이 아니라
**관리 인력 압박 해소 · 사업부 정리**로 재정의할 것.
(도움말 문구는 이미 그렇게 고쳐 두었다 — `ui/help.js`)

### 2-4. 미리 사두기는 후반 콘텐츠였다 → **비상장 경로로 해소 (D안)**

원래 계측은 이랬다.

```
--stake 무차입:  857일 (기준선 830) · 결렬률 36% (무변화) · 평균 ★0.7
```

봇 정책 문제가 아니었다. **상장 기준이 시총 1,000억 이상인데 중소기업 구간에서 사는
매물은 2억~109억이라 전부 비상장**이라 토글 자체가 안 떴다.

`LIST_TIER` 를 낮추는 안(구 §5-A)은 **계측으로 폐기했다.** 4 → 3(100억)으로 내려도
그 구간 매물 15개 중 **0~1개**만 상장된다. 실효를 내려면 2(10억)까지 내려야 하고
그러면 NPC 상장사가 18 → 34개로 늘어 요구사항 변경 폭이 훨씬 커진다.

대신 **비상장 지분 매입(장외)** 경로를 뚫었다. 상장 기준은 그대로다.

| | 상장사 | 비상장사 |
|---|---|---|
| 매집 방식 | 주식 시장 (`holds`) | **장외** — 투입 누계 `stock.priv` |
| 지분율 | 주가 × 수량 / 시총 | 투입 누계 / 시총 |
| 소문 효과 | 주가 +25% · 인수가 +8%p · 난이도 +1 | 인수가 +8%p · 난이도 +1 |
| 되팔기 | 시장가 | 투입 × **0.85** (유동성이 없다) |

계측 (`--stake` 무차입 3시드):

```
완주      857 → 819일  (기준선 830 보다 빠르다)
결렬률    36% → 8~23%  (평균 17%)
평균 ★    0.7 → 1.6~2.6
계열사    25~31 → 33~35
```

**단, 중소기업 구간은 287 → 299일로 오히려 늘었다.** 그 구간에 현금이 지분으로
묶이기 때문이다. §2-2 의 결론(이 구간은 M&A 루프로 못 건드린다)이 네 번째로 확인됐다.
D안이 산 것은 **구간 단축이 아니라 그 구간에 할 일이 생겼다는 것**이다.

---

## 3. 현재 밸런스 기준선

시드 `1001 / 2002 / 3003` 고정. 계측 기준은 **시총 1위 도달일**.

**단계 5 이후 기준선.** 협상 기간·`subYield` 를 손댔으므로 이전 숫자와 비교 불가.

6시드(1001~6006) 로 잰 값이 더 믿을 만하다 — 3시드는 단일 시드 편차 ±100일에 흔들린다.

| 전략 | 3시드 | **6시드** | 비고 |
|---|---|---|---|
| 무차입 | 712 | **739** | 결렬률 43% |
| 공격 `aggressive` | 605 | **609** | 격차 17.6% · **대출을 안 쓴다**(§3-d) |

등급 구간 (무차입):

```
구멍가게 59 · 동네슈퍼 112 · 스타트업 57 · 중소기업 237 · 중견기업 118 · 대기업 17 · 글로벌 112
```

<details><summary>단계 4 시점의 옛 기준선 (참고)</summary>

| 무차입 830 · 레버리지 726 · 극단차입 632 · 무속 550 · `active` 823 |
|---|

</details>

스위치를 켠 경우.

| 전략 | 평균 | 결렬률 | 수사 |
|---|---|---|---|
| 무차입 `--acts` | 859 | 32% | 57~78 |
| 무차입 `--stake` | **819** | **17%** | 0 |
| 무차입 `--table` | **790** | **22%** | 0 |
| 극단 차입 `--acts` | 587 | 30% | **69~101 · 2/3 수사 엔딩** |

**§10-3(극단 차입이 안 죽는다)이 해소됐다.** 접대비의 수사 +3 이 "빠른 루트일수록
수사가 붙는" 구조를 만들었다. 무차입은 같은 조건에서 57~78 로 생존해 경계가 섰다.

> `negoWineProbe` 를 처음 5 로 넣었더니 **무차입 봇마저 수사 95~100 으로 죽었다.**
> 3 으로 내려 지금 값이 됐다. 이 상수를 건드리면 두 전략을 같이 볼 것.

---

## 3-b. 단계 4 — 협상 테이블

287일 구간은 시간의 86~99% 가 '협상 중'인데 그 15일이 완전 자동이었다.
협상 그 자체를 판단거리로 만드는 것이 그 구간에 개입을 넣는 유일한 직접 수단이다.

**기획서의 규모 분기를 버렸다.** 원안("난이도 중 이상 또는 등급 상한 60% 이상")을
이 구간에 대입하면 상한 60% 선이 1,200억인데 매물은 최대 109억이라 규모 조건은
0건, 난이도 조건으로 4~5건만 열린다 — 57일에 한 번이다.

대신 **파견 시 플레이어가 고른다.**

```
[ 협상단 파견 — 직접 협상한다 ]   클로징에 테이블 · 2~5라운드
[ 협상단 파견 — 맡긴다 ]          아무 일도 일어나지 않는다
```

- 라운드 수 `2 + diff` (하 2 · 최상 5). 이 구간 매물은 전부 하/중이라 2~3라운드
- 상대 요구 4종 × 우리 접근 3종. 각 접근이 요구 2종에 강하다 (가위바위보 수준)
- 접근은 **직원의 성격에서 나온다** — 선택지는 협상단원 3명이 전부다
- 클로징(진행도 100%)에 열린다. 모달이 이미 게임을 멈추므로 일시정지 처리가 공짜고
  새 창·캔버스가 필요 없다 — 라운드마다 모달을 다시 띄우는 것으로 끝냈다

### 위임 정책을 두 번 고쳤다 — **여기가 함정이다**

| 시도 | 결과 |
|---|---|
| 위임 = 최선 수 | 직접 협상의 상한이 위임과 같아져 **직접 할 이유가 0** |
| 위임 = 무작위 수 | 기대값은 0 인데 **분산**이 생겨 기준선 830 → 869일, 결렬률 23~55% 로 튐 |
| **위임 = 무영향 (채택)** | 난수도 쓰지 않는다. 기준선 837/878/776 — 도입 전과 한 자리도 안 다르다 |

테이블은 기존 밸런스 **위에 새로 얹은 판정**이다. 맡기면 정확히 0 이어야 기준선이
보존되고 직접 두는 쪽만 ± 를 진다. `tableHitSuccess` / `tableMissSuccess` 를
대칭(+9 / −9)으로 잡은 것도 같은 이유다 — **이 대칭을 깨면 기준선이 통째로 밀린다.**

### 계측

| | 완주 | 결렬률 | 중소기업 구간 |
|---|---|---|---|
| 위임 (기본) | 830 | 36% | 287일 |
| **직접 `--table`** | **790** | **22%** | 293일 |

구간은 여전히 안 줄어든다(+6일). **산 것은 구간 단축이 아니라 판단 밀도다** —
그 287일 동안 협상 16~20건 × 2~3라운드 = **40회 안팎의 추가 판단**이 생겼다.
개입 3회(단계 2)와 매집 토글(단계 3)에 얹히는 층이다.

---

## 3-c. 단계 5 — 매물 경쟁 · 기간 조정

### 협상 기간 15일 → 10일

`negoProgressPerDay` 6.8 → 10.0. 같은 기간에 협상이 16~20회에서 22~28회로 늘어
**판단 밀도가 직접 오른다.**

> **성공도 기본 증가량을 같이 올려야 한다.** 성공도는 일당 누적이라 기간만 줄이면
> 최종 성공도가 32% 낮아진다. 기간 비율(×1.46)만 반영한 1.62 로는 결렬률이
> 36 → 42% 로 올라 1.75 까지 얹었다. **이 두 상수는 항상 같이 본다.**

### 매물 경쟁

협상 착수 시 확률 `rivalChance 0.18` 로 마감(7~11일)이 걸린다. 협상 소요가 약 10일이라
**개입을 한 번도 안 쓰면 절반쯤 놓친다.**

- 마감을 넘기면 협상이 끝나고 매물이 넘어간다
- **매물은 시장에서 사라지지 않는다** — 빠지면 후반에 살 것이 마른다.
  대신 시총 +30%, 난이도 +1 로 다시 노릴 수 있다
- 회사 창에 남은 일수를 띄운다

**`시한 제시` 개입이 여기서 처음으로 쓸모를 얻었다.** 그전에는 급할 이유가 없어
성공도 88 이상일 때만 눌리던 카드였는데, 지금은 봇이 협상당 9~15회 쓴다.
결렬률 47% → 38% 로 돌아온다 — **개입을 안 쓰면 벌 받는 구조**다.

처음에 `rivalChance` 를 0.22 로 뒀더니 기본 봇 결렬률이 48% 까지 올라 0.18 로 내렸다.

### 성장 곡선 — 287일 구간이 드디어 움직였다

`subYield` 0.068 → 0.085 (회수기간 26 → 21일).

§2-2 에서 287일의 정체는 **시총 241억 → 1조로 42배 복리 성장하는 시간**이라고
계측했다. M&A 루프(처리량·결렬률·등급 목표)로는 네 번 다 실패했고, 성장 계수를
직접 건드리자 처음으로 줄었다.

```
중소기업 구간   287 → 237일 (−50)
전체 완주       830 → 712일 (−14%)
```

> **레버리지 격차가 벌어졌다** (6시드 재측정 시 무차입 739 vs 레버리지 609 = 17.6%).
> 처음엔 "빌려서 산 계열사가 더 벌기 때문"이라고 적었는데 **틀렸다** — §3-d 참조.
> 레버리지 봇은 대출을 안 쓴다.

---

## 3-d. 레버리지 균형 — **진단이 틀렸다**

`HANDOFF.md` §10-3 은 이렇게 적혀 있었다.

> 레버리지가 리스크 없이 빠르다. 압류가 거의 안 터진다. **인수금융 금리나 상환
> 기간을 조이면 경계가 선명해진다.**

조여 봤다. **아무것도 안 변한다.**

| 실험 | 레버리지 6시드 평균 |
|---|---|
| 기준 (`acqRatePremium 3.2` · `acqLoanRatio 0.80` · 15개월) | 609일 |
| 가산금리 3.2 → 5.2 | **609일 (완주일까지 동일)** |
| 한도 0.80 → 0.65 · 상환 15 → 12개월 | **609일 (동일)** |
| **인수금융을 사실상 차단** (`acqLoanRatio 0.05`) | 617일 (6시드 중 4개 동일) |

### 원인

인수가 확정 모달에서 `인수금융 대출` 선택지는 **`dis: short === 0`** 이다.
자기자금이 충분하면 비활성이다 — 게임 규칙상 인수금융은 **부족액을 메우는 용도**다.

그런데 `leveraged` 봇(예산 = 현금 × 1.5)은 협상 10일 동안 현금이 쌓여
성사 시점엔 부족액이 거의 안 생긴다. **그래서 대출을 안 쓴다.**
봇에게 인수금융을 강제로 고르게 해 봐도 선택지가 비활성이라 눌리지 않는다.

### 그러면 격차 17.6% 는 무엇인가

무차입 739 vs 레버리지 609 (6시드). 이 격차는 **대출이 아니라 `예산 = 현금 × 1.5`
라는 공격적 타겟 선정**에서 나온다. 이름만 '레버리지'이고 실제로는 무차입이다.

즉 지금 열려 있는 진짜 질문은 **"큰 매물을 노리는 게 왜 항상 유리한가"** 다.
대가는 난이도(`diff`)와 프리미엄뿐인데 그 대가가 이득보다 작다.

> **대출 리스크를 재려면 `reckless`(예산 × 3)를 볼 것.** 거기서는 부족액이 생겨
> 실제로 빌리고, 파산 1/3 · 수사 엔딩 1/3 로 경계가 서 있다.

### 확정 — 부채 지표를 넣고 세어 봤다

`sim.mjs` 에 부채 지표(대출 실행 횟수·총 차입·최대 잔액·이자·연체)를 추가했다.
**이게 없어서 여섯 번 헛짚었다.**

| 전략 (예산 배수) | 인수금융 실행 | 총 차입 | 연체 |
|---|---|---|---|
| 무차입 (×0.62) | 0회 | 0 | 0 |
| ~~중간 레버리지~~ (×1.5) | **0~3회** (6시드 중 4개가 0회) | 대부분 0 · 최대 1.68조 | 0 |
| 극단 차입 (×3) | 4~12회 | 39.8억 ~ 25.4조 | 2~4회 · 파산 1/3 |

800일 동안 대출 1회, 총 1,639만원. **`leveraged` 는 레버리지 전략이 아니다.**
→ `aggressive` 로 개명했다. 이 봇이 재는 것은 대출이 아니라 **예산 배수(공격성)** 다.

### 지금 게임에는 레버리지 플레이가 없다

인수금융은 **부족액 보전용**이다. 자기자금이 충분하면 선택지 자체가 비활성이라
"현금을 남겨 두고 빌린다"는 플레이가 원천적으로 불가능하다.
실제로 빌리는 것은 예산 ×3(reckless) 뿐이고, 그건 이미 파산 1/3 로 대가를 치른다.

즉 `HANDOFF.md` §10-3 의 과제("레버리지 경계를 세운다")는 **경계를 세울 대상이
없다.** 이 과제를 살리려면 게임 규칙을 바꿔야 한다 (§5-D).

### 손댄 것

- `sim.mjs` 에 부채 지표 추가 · `leveraged` → `aggressive` 개명
- `loanRate` 안에 박혀 있던 인수금융 가산금리 `3.2` 를 `BAL.acqRatePremium` 으로 꺼냈다
- **밸런스 값은 하나도 안 바꿨다.** 계측이 근거를 못 만들었으므로 바꾸지 않는다

---

## 4. 기획서(MNA_PLAN)와 다르게 간 것

되돌리려면 여기를 보면 된다.

1. **'협상단 교체' 개입을 넣지 않았다** — 성공도 증가율은 이미 `teamPower(s)` 가
   `teamOf(s)` 를 매일 읽으므로 직원 창에서 편성을 바꾸면 즉시 반영된다.
   교체 버튼은 진행도만 깎는 **순손실 버튼**이 된다
2. **실사는 숨은 태그가 없어도 항상 누를 수 있다** — 비활성화하면 버튼 상태 자체가
   "문제가 없다"는 정보를 공짜로 흘린다
3. **`negoWineProbe` 5 → 3** — §3 각주 참조
4. **미리 사두기의 ★ 단가를 지분 3% 로 잡았다** (기획서에 수치 없음) — 할인과
   비용을 같게 만들어 "항상 켜는 게 정답"이 되지 않게 하려는 것
5. **`stakeLeakAt = 4`** — 기획서의 "★★★ 초과 시 소문" 을 그대로 옮긴 값
6. **테이블 분기를 규모가 아니라 플레이어 선택으로** — §3-b. 원안대로면 287일
   구간에 4~5번만 열려 후반 콘텐츠가 된다
7. **위임에 페널티(성공도 −12)를 두지 않았다** — 페널티가 있으면 '항상 직접'이
   정답이 되어 선택이 사라지고 반복 피로만 남는다
8. **테이블을 캔버스가 아니라 모달 연쇄로 만들었다** — `ui/negoTable.js` 는
   판정을 하지 않고 `systems/negoTable.js` 를 부르기만 한다

---

## 5. 지금 열려 있는 결정 — **다음 세션이 여기서 시작한다**

287일 구간을 계속 팔 것인가, 다른 데로 갈 것인가.

~~A. 상장 기준을 낮춘다~~ → **계측으로 폐기.** §2-4 참조. 대신 D안(비상장 지분 매입)이
들어갔고 `HANDOFF.md` §2 요구사항 10번(상장 기준 = 중견기업 이상)은 그대로 지켰다.

### D. 인수금융을 '선택'으로 바꾼다 — **§3-d 에서 새로 열린 것**

지금 인수금융은 부족액 보전용이라 레버리지 플레이가 존재하지 않는다.
인수가 확정 모달의 `인수금융 대출` 을 **자기자금이 충분해도 고를 수 있게** 하면
"현금을 남기고 빌릴까"가 처음으로 선택이 된다.

- 손대는 곳: `mna.js:judgeNego` 의 `dis: short === 0` 과 `ui/bankPanel.js:openAcqLoan`
- 그러면 `aggressive` 와 별개로 진짜 `leveraged` 봇을 세울 수 있고,
  §10-3(레버리지 경계)이 비로소 계측 가능한 과제가 된다
- 리스크: 현금을 남기는 것이 항상 유리하면 "무조건 대출"이 정답이 된다.
  금리·상환 기간이 그 대가인데, 지금 계수로 충분한지는 재 봐야 안다

### B. 287일을 M&A 밖에서 채운다

그 구간의 실제 활동은 매장 운영과 그룹 관리다. 사옥 쪽 콘텐츠(시설·재고·결재·직원)를
두껍게 하는 것이 정공법이다 — 어차피 플레이어가 그 287일 동안 실제로 보고 있는 화면이다.

### C. 287일을 놔두고 MNA_PLAN 을 계속한다

~~단계 4~~ 는 분기 조건을 플레이어 선택으로 바꿔 **287일 구간에 닿게 만들었다** (§3-b).
남은 것은 5(인수 방식 4종) → 6(라이벌).
**단계 5 는 큰 인수에만 열리므로 후반 콘텐츠다** — 287일에는 안 닿는다.
라이벌(6)은 매물 경쟁으로 시간 압박을 만들어 이 구간에 닿을 수 있다.

> **네 번 연속으로 "M&A 에 무엇을 얹어도 287일은 안 움직인다" 가 나왔다.**
> (처리량 2배 −24일 / 결렬 0% −16일 / 등급 목표 변경 −21일 / 비상장 매집 **+12일**)
> 이 구간을 실제로 줄이려면 **B 로 축을 옮기는 것**이 맞다.
> C 로 가더라도 287일에는 안 닿는다는 것을 알고 들어갈 것.

---

## 6. 다음 단계별 비용 메모 (착수 전 참고)

| 후보 | 세이브 | 손대는 범위 | 계측 예상 |
|---|---|---|---|
| ~~단계 4 — 협상 테이블~~ | — | **완료.** §3-b | 완주 −5% · 결렬률 36 → 22% |
| **단계 5 — 인수 방식 4종** | 유지 (`nego.mode`) | `mna.js` 분기 · `companyPopup.js` | 적대적 인수의 수사 +7 은 이미 접대비가 같은 일을 한다 — **중복 확인 필요** |
| **협상단 2팀** | **12 → 13** | `s.nego` 를 배열로. `mna.js` 전체 · `panelLeft` · `city.js` 라벨 · sim · smoke | 천장 −130일. 관리 인력 대가를 빼면 실효는 그보다 작다 |
| **단계 6 — 라이벌** | **12 → 13** | 신규 `systems/rival.js` · `market[].owner` · sim 봇 재작성 | 기준선 표를 통째로 다시 잡아야 한다 |

**단계 5 의 '적대적 인수 수사 +7' 은 다시 볼 것.** 접대비가 이미 행위당 수사를 올리고
있어서 그대로 얹으면 극단 차입 루트가 즉사할 가능성이 있다 (§3 각주와 같은 함정).

---

## 7. 검증 방법과 함정

```bash
npm run check   # ES 모듈 무결성. smoke 가 이걸 먼저 돌린다
npm run smoke   # 138 케이스
node tools/sim.mjs --noref --strats=aggressive        # 예전 이름은 leveraged
npm run sim     # 빌드부터 한다

node tools/sim.mjs --runs=0                          # 등급 구간만
node tools/sim.mjs --noref --strats=normal --acts    # 협상 중 개입을 쓰는 봇
node tools/sim.mjs --noref --strats=normal --stake   # 미리 사두기를 쓰는 봇
node tools/sim.mjs --noref --strats=normal --table   # 클로징 테이블을 직접 두는 봇
node tools/sim.mjs --noref --strats=active           # 매각/투자/재편을 쓰는 봇
```

- **`npm run sim` 은 빌드부터 하고 `node tools/sim.mjs` 는 안 한다.** 소스만 고치고
  `node` 로 돌리면 이전 빌드를 재는 것이다
- **sim 을 백그라운드로 돌리지 말 것.** 로그가 끊겨 무한 루프처럼 보인다
- **전략 전부를 한 번에 돌리면 실행 시간 제한에 걸린다.** `--strats` 로 나눌 것
  (3시드 1전략 ≈ 30초, ref 포함 5전략 ≈ 3분)
- **단일 시드 비교는 카오스적이다.** 개입 하나가 판정 시점을 밀면 난수열이 갈려
  같은 시드에서도 100일씩 튄다. **반드시 3시드 평균으로 볼 것**
- 봇 정책을 고칠 때는 **그 정책이 실제로 발동하는지부터 세어 볼 것.**
  이번 세션에 매각 정책이 800일 동안 0회 발동한 채로 "효과 없음" 결론이 날 뻔했다

---

## 8. 파일별 변경 지도 (이번 세션)

| 파일 | 단계 | 내용 |
|---|---|---|
| `core/balance.js` | 2·3 | `negoActs` 등 개입 상수 7개, `stakeStep` 등 매집 상수 8개 |
| `core/state.js` | 3 | `stock.stake` 추가 |
| `core/loop.js` | 3 | `tickStake(s)` 한 줄 |
| `systems/mna.js` | 2·3 | `NEGO_ACTS` · `negoAct` · `negoLeft` 신설, `startNego` 가 `stakeBonus` 를 읽음, `completeAcq` 가 `clearStake` 호출 |
| `systems/stock.js` | 3 | `tickStake` · `stakeStars` · `stakeBonus` · `toggleStake` · `clearStake` 신설 |
| `ui/panelLeft.js` | 2 | `negoActsHtml` — 개입 버튼과 `◆◆◇` |
| `ui/companyPopup.js` | 3 | 선택지 두 단 분리, 지분 표시 |
| `ui/modal.js` | 3 | 선택지 그룹 머리말(`head`) 지원 — 2줄 |
| `ui/help.js` | 2·3 | 개입 안내 추가, 매각 문구 정정 |
| `tools/sim.mjs` | 전 | 등급 구간 off-by-one 수정, `active` 전략, `--acts` · `--stake`, 협상 결과 집계 |
| `tools/smoke.mjs` | 2·3 | 72 → 94 케이스 |
| `systems/stock.js` | 3-b | 비상장 장외 매입 — `privMap` · `privAmt` · `sellPrivStake`, `stakeRatio`/`tickStake`/`leak` 분기 |
| `ui/companyPopup.js` | 3-b | 밑작업을 비상장에도 개방 · `사둔 지분을 되판다` |
| `core/balance.js` | 3-b | `stakePrivSell 0.85` |
| `tools/sim.mjs` | 3-b·4 | 매집 대상에서 `listed` 조건 제거 · `--table` 스위치와 최선 수 정책 |
| `systems/negoTable.js` | 4 | **신규** — 요구/접근/상성, `tableRound`·`resolveTable`·`bestPicks`·`delegateTable` |
| `ui/negoTable.js` | 4 | **신규** — 라운드마다 모달을 다시 띄운다. 판정 없음 |
| `systems/mna.js` | 4 | `startNego(s, t, direct)` · `finishNego` 를 테이블 → `applyTable` → `judgeNego` 로 분리 |
| `ui/companyPopup.js` | 4 | 파견을 직접/위임 두 갈래로 |
| `core/balance.js` | 4 | `tableHitSuccess` 등 5개 |
| `HANDOFF.md` | 전 | 기준선·등급 구간·버그 이력·남은 과제 갱신 |

---

## 9. 새 세션 시작 시

1. `company-tycoon-stage3.zip` 압축 해제 (또는 패치 2개를 `5a61f3b` 위에 적용)
2. `npm install` (jsdom — 검증 도구 전용)
3. `npm run smoke` → **138/138** 나오면 정상
4. `node tools/sim.mjs --runs=0` → §2-1 등급 구간 표와 대조
5. **§5 의 결정(B 인가 C 인가)을 먼저 받고 시작할 것.** 어느 작업이든 287일에
   닿지 않는다는 것을 **네 번** 확인한 상태다

---

## 10. 감축 패스 — 캐주얼 재조정 (`11bc87f` ~ HEAD)

### 10-1. 왜 했나 — §3-b 의 한 줄이 분기점이었다

> **"산 것은 구간 단축이 아니라 판단 밀도다"**

287일 구간이 M&A 루프로 안 움직인다는 계측이 네 번 나온 뒤(처리량 2배 −24일 /
결렬 0% −16일 / 목표 변경 −21일 / 매집 **+12일**), §5 의 선택지 B(축을 사옥으로
옮긴다)와 C(M&A 에 계속 얹는다) 중 C 로 갔다. 문제는 C 를 고른 것이 아니라
**판단을 넣는 이유가 "재미"가 아니라 "빈 시간을 메우는 것"이 됐다**는 점이다.

카이로 소프트 문법에서 협상 15일 동안 막대만 차는 것은 결함이 아니라 기능이다 —
그 시간에 플레이어는 도시와 매장 픽셀을 본다. 이 게임에는 그 화면이 이미 있는데
그것을 결핍으로 읽었다.

**증상 (감축 전)**

| | |
|---|---|
| 인수 1건당 조작 | 밑작업 4~5택 → 파견 2택 → 개입 3회(4종) → 테이블 2~5라운드 → 지불 → 인수 후 3택 |
| 완주 기준 누적 | 테이블 모달 **60~80회**, 개입 버튼 **75회** (인수 22~28건) |
| 한 화면 수치 | `companyPopup` 11줄 · `panelLeft` 게이지 6개 + 분해 5줄 (자기 원칙 §10-3 위반) |
| 노출 용어 | 프리미엄·인수금융·장외·우발채무·지주회사 체제 (§10-1 용어 상한 위반) |

개별 기능은 다 합리적이고 계측도 붙어 있다. **문제는 동시 노출과 반복 빈도였다.**

### 10-2. 선별 기준 — 깊이가 아니라 두 축

1. 1회 사이클당 조작 횟수 (반복 피로)
2. 결과가 그림·아이콘으로 바로 읽히나 (모델 학습 비용)

| | 판정 | 결과 |
|---|---|---|
| 계열사 태그 | 남김 | 카드 뽑기 재미. 손대지 않음 |
| 업종 퍼크·사업부 | 남김 | "같은 업종 3개" 한 줄로 끝나는 수집 재미 |
| 미리 사두기 ★ | 남김 + **강화** | 토글 1개로 조작 상한을 지킨 유일한 기능. 여기에 정보 공개를 얹었다 |
| 매물 경쟁 마감 | 남김 | 시간 압박은 설명이 필요 없다 |
| 협상 테이블 | **단판으로** | C |
| 개입 4종 | **2종으로** | B |
| 그룹 효율 분해 | 접음 | A |
| 상장/비상장 두 경로 | 유지 | 이번 범위 밖. 규칙이 두 벌인 것은 남은 부채 |
| 계열사 매각 | 유지 | 계측상 자금원으로 성립 안 하지만(§2-3) 제거는 이번 범위 밖 |

### 10-3. 하지 않기로 한 것

**D안(인수금융을 '선택'으로) — 폐기.** §3-d 에서 열린 과제지만 방향이 반대다.
"현금을 남기고 빌릴까"는 재무 시뮬의 재미지 캐주얼 타이쿤의 재미가 아니고,
§3-d 스스로 *"무조건 대출이 정답이 될 수 있고 계수는 재 봐야 안다"*고 적어 뒀다.
**"이 게임에 레버리지 플레이가 없다"는 것은 고칠 결함이 아니라 장르에 맞는 상태다.**

**E(격차 17.6%) — 종결.** "큰 회사를 사면 빨리 큰다"는 플레이어 직관과 일치한다.
봇 17.6% 는 사람이 체감하는 값이 아니다.

**단계 5의 인수 방식 4종 — 폐기.** 후반 전용이라 아무도 못 보고
'적대적 인수 수사 +7'은 접대비와 중복이다.

**단계 6 라이벌 본체 시뮬 — 폐기, 대신 껍데기만.** 라이벌이 스스로 매물을 사고
자라는 모델은 관리할 상태와 계측 축을 늘린다. 이름·성격·대사만 붙였다 (`core/rivals.js`).
캐주얼에서 제일 잘 먹는 것은 수치가 아니라 캐릭터인데, 그것은 조작 0개로 살 수 있다.

### 10-4. 무엇을 어떻게 잘랐나

**A (`11bc87f`) — 노출만. 밸런스 무영향이 검증 조건이었다.**
`companyPopup` 11줄 → 5줄, 그룹 효율 분해 5줄 → 한 줄, 테이블 모달 숫자 2줄 제거.
접힌 값은 `title` 툴팁으로 남겼다. 용어: 프리미엄 → 웃돈 · 인수금융 → 인수 대출 ·
장외 → (표기 삭제) · 우발채무 → 숨은 빚 · 지주회사 체제 → 그룹 본사.
**주석과 식별자는 손대지 않았다.**

**B (`a060156`) — 개입 4종 → 2종.**
'실사'를 없애고 숨은 특성 공개를 **밑작업 ★★**(`stakeRevealAt`)로 옮겼다.
정보를 얻는 길은 남고 협상 중 조작만 줄어든다. 매집이 성공도·인수가에 더해
정보까지 주게 되어 밑작업이 한 축으로 묶인다.
'중단'은 `free: true` 로 개입 횟수에서 뺐다 — 같은 줄에 두면 3회 제한을 쓰는 것처럼
읽혀 손절을 망설이게 된다.

**C (`cfcbeef`) — 테이블 단판.** §10-5 참조.

**D (HEAD) — 라이벌 이름.** 매물 id 해시로 결정적 선택. 새 상태가 없어 `SAVE_V` 유지,
게임 rng 를 쓰지 않아 시드 스트림 무관. `tgt.rivalName` 은 표시용 캐시일 뿐이라
없으면 `rivalOf` 로 다시 뽑는다 (구버전 세이브 안전).

### 10-5. 테이블 단판 — 계수를 세 번 재서 잡았다

라운드를 1로 줄이면 `rollDemands` 의 `rand()` 소비량이 바뀌어 **시드가 밀리고 이전
계측과 비교 자체가 불가능해진다** (§7-1 에서 이미 밟은 함정). 그래서 요구는
예전과 같은 `2 + diff` 개를 계속 뽑고 **앞의 1개만 쓴다.**

| 구성 | 일수 (1001/2002/3003) | 평균 | 결렬률 |
|---|---|---|---|
| 다라운드 (감축 전) | 683 / 718 / 618 | 673 | 23% |
| 단판 · 보정 없음 (hit 9) | 732 / 753 / 630 | 705 | 38% |
| 단판 · hit 22 | 699 / 756 / 639 | 698 | 37% |
| 단판 · hit 34 / miss −18 | 689 / 717 / 624 | 677 | 30% (시드별 20~36% 로 튐) |
| **단판 · hit 34 / miss −10** | **695 / 716 / 624** | **678** | **26%** |

**왜 22 로는 부족했나.** 다라운드는 난이도가 높을수록 라운드가 늘어 보정도 같이
커졌다 — 난이도 스케일링이 규칙에 숨어 있었다. 단판에는 그것이 없다. 대신 값을
크게 잡으면 **성공도 상한(100)이 스케일링을 대신한다** — 쉬운 매물은 이미 성공도가
높아 남는 값이 버려지고, 어려운 매물에만 온전히 얹힌다.

**대칭을 깼다.** §3-b 의 hit/miss ± 대칭은 다라운드 전제였다. 단판에는 헛수를 만회할
다음 판이 없으므로 적중 34 / 헛수 −10 으로 벌점을 절반 이하로 낮췄다.
−18 에서 시드별 결렬률이 20~36% 로 튀었고 −10 에서 26% 로 모였다.
**위임이 정확히 0 인 것은 그대로다** (§3-b 의 함정).

### 10-6. 최종 계측 — 전 전략이 기준선과 일치

| 전략 | 평균 | 시드별 | 엔딩 |
|---|---|---|---|
| normal | 712 | 707 / 770 / 659 | 미종료 |
| aggressive | 570 | 583 / 632 / 497 | 미종료 |
| reckless | 556 | 555 / 724 / 390 | bankrupt 1/3 |
| occult | 505 | 523 / 496 / 496 | occult |
| active | 748 | 738 / 799 / 709 | 미종료 |

스위치: `--table` 695/716/624 · `--acts` 681/741/644 (결렬률 29% · 수사 60~69) ·
`--stake` 722/772/655 (A·B 전후 완전 동일).

### 10-7. 함정 — **§7 에 이미 적혀 있는데도 밟았다**

**`node tools/sim.mjs` 를 직접 부르면 이전 `dist/` 번들을 그대로 쓴다.**
§7 의 첫 줄에 그대로 적혀 있는 내용인데, 계측을 빠르게 돌리려고 직접 호출하다가
소스를 되돌려도 같은 숫자가 나왔다 — "밸런스 무영향"을 증명한 줄 알았던 비교
두 건이 사실은 같은 번들을 두 번 잰 것이었다. 재검증 결과 두 건 다 결론 자체는
맞았지만, 그때 근거로 댄 숫자는 근거가 아니었다.

**교훈은 '함정을 적어 두는 것으로는 부족하다'는 쪽이다.** 다음에 손댈 사람은
계측 명령을 `npm run sim -- --noref --strats=...` 형태로만 쓰거나,
`sim.mjs` 가 `dist` 의 mtime 을 보고 소스보다 오래됐으면 경고하도록 만들 것.

### 10-8. 남은 부채

- 상장/비상장 매집이 규칙 두 벌이다. 합칠 수 있으면 인지 비용이 준다
- 계열사 매각은 계측상 자금원으로 성립하지 않는데(§2-3) 이유를 플레이어가 알 방법이 없다
- 업종 전환 모달의 선택지 9개 — 드문 조작이라 이번엔 뒀다
- 단판 테이블은 다라운드보다 분산이 크다 (결렬률 시드별 17~36%). 대수의 법칙이
  안 먹히는 구조라 계수로는 더 못 줄인다. 줄이려면 3인 팀이 4종 요구를 못 덮는
  구조를 손봐야 하는데, 그것은 숨은 규칙이 된다

---

## 11. 구조 수정 · 후반 리스크 (`f1d07cc` ~ HEAD)

감축 패스(§10) 뒤 계측을 붙여 보니 밸런스 숫자가 아니라 **구조 결함 세 개**가 나왔다.

### 11-1. 순자산이 계열사를 세지 않았다 — 가장 큰 것

```js
const netWorth = s => s.co.cash - debtTotal(s);   // 예전
```

계열사 23개 · 시총 34조 시점에도 순자산이 **현금과 정확히 같았다.** 그런데 7개
등급 중 4개의 승급 조건이 순자산이었다. 즉 **게임의 메인 행동인 인수가 승급
게이지를 역행시켰다.** HUD 에는 순자산 항목조차 없었다.

세 값을 포함 관계로 정리했다.

```
보유 자금 = 현금
순자산    = 보유 자금 + 계열사 가치 − 부채     ← 순위를 이 값에서 뽑는다
시가총액  = 순자산 + 본업 가치(매장 수익 8배)
```

순위 입력이 시총 → 순자산으로 바뀌어 이름도 `자산 순위` 로 바로잡았고,
초반 규모가 한 자릿수 작아진 만큼 `rankFloor` 를 1e8 → 1e7 로 내렸다
(안 내리면 첫 70일이 5,000위에 붙어 성장이 안 보인다).

**부작용 — 은행이 조용히 헐거워졌다.** `co.cap` 이 부풀자 그 값을 보던 세 곳
(대출 한도 · 신용 평가 · 파산 보험)이 같이 헐거워져 reckless 파산이 1/3 → 0/3
으로 사라졌다. 시총에는 매장 수익의 8배 자본화가 들어 있어 그것을 담보로
잡으면 "앞으로 벌 돈"으로 지금 빌리는 셈이다. `grossAssets`(보유 자금 +
계열사 가치, 부채 차감 전)를 새로 두고 셋을 옮겼다.

### 11-2. 대기업 등급이 존재하지 않았다

| 시드 | 대기업 진입 | 다음 등급 | 구간 |
|---|---|---|---|
| 1001 | 612일 | 613일 | **1일** |
| 2002 | 637일 | 644일 | 7일 |
| 3003 | 502일 | 544일 | 42일 |

승급 연출이 이틀 사이 두 번 터지고 지나간다. 원인은 대기업 목표
(순자산 7,000억 + 순위 120위)의 **두 조건이 같은 지표에서 나오는 것**이었다.
순위가 순자산에서 나오게 되면서 완전 중복이 됐다.

승급 목표를 등급마다 병목 축이 달라지도록 다시 깔았다 —
**자금 → 행동 → 수집 → 자금 → 도전 → 조합 → 순위.**

| 등급 구간(3시드 평균, 일) | 전 | 후 |
|---|---|---|
| 구멍가게 / 동네슈퍼 / 스타트업 | 59 / 112 / 57 | 37 / 105 / 112 |
| 중소기업 / 중견기업 | 237 / 118 | 163 / 167 |
| 대기업 / 글로벌그룹 | **17** / 112 | **56** / 69 |

대기업 목표를 '사업부 5개'로 바꾼 부수 효과로 **사업 재편에 존재 이유가
생겼다** (§10-8 에 부채로 남겨 뒀던 것).

### 11-3. 첫 M&A 가 158일차였다

M&A 타이쿤인데 메인 콘텐츠가 플레이 시간의 22% 지점에 열렸다. 동네슈퍼 목표에서
순자산 조건을 빼고 구멍가게 목표를 낮춰 **105일**로 당겼다.

---

## 12. 후반 리스크 — 협상단 2팀 · 경제 위기

### 12-1. 파산은 설계된 리스크가 아니라 버그의 부산물이었다

11-1 을 고치자 파산이 사라졌다. 원인을 찾느라 네 가지를 쟀고 **세 개가 무효**였다.

| 시도 | 결과 |
|---|---|
| `acqRatePremium` 3.2 → 5.0 | 이자만 오르고 연체·파산 불변 |
| `negMonthsToBust` 4 → 3 | **완전 불변** — 현금이 음수로 안 간다 |
| `acqLoanRatio` 0.80 → **0.45** | 총 차입 2.17조로 **한 자리도 안 변함** |
| `loanTermMonths` 15 → 12 | 연체 1 → 2회. 유일하게 효과 |

세 번째가 결정적이었다 — 한도를 반토막 냈는데 차입액이 그대로라는 건 봇이
한도가 아니라 **부족액만큼만** 빌린다는 뜻이고, 대출 한도는 병목이 아니었다.

결론: 옛 구조에서는 회사를 사면 순자산이 줄어 승급을 못 하고 → `capCeiling` 에
갇혀 작은 매물만 사다가 현금이 말라 파산했다. **그 악순환이 사라진 것이다.**

### 12-2. 협상단 2팀 — 중견기업부터

`s.nego`(단일) → `s.negos`(배열 0~2건) · `staff.slot`. **`SAVE_V 12 → 13`.**

**2팀은 중견기업(등급 4)부터 열린다.** 처음부터 열면 조작이 두 배가 되어
감축 패스와 정면으로 부딪친다. 후반에만 열면 초반 조작은 그대로이고, 통합 중
계열사가 겹치면서 위험이 발동한다. 2팀 전에는 UI 에 '1팀'이라는 말도 안 나온다.

`onTeam` 을 슬롯 번호로 갈아엎지 않고 `slot` 을 얹었다 — 관리 인력이 '협상단에
안 들어간 직원'으로 계산되므로, 기존 의미를 두면 economy·tabs·sim 계산을
손대지 않아도 된다. 84곳 중 실제로 고친 것은 절반이다.

**버그도 하나 나왔다.** `checkBankrupt` 가 현금이 양수면 즉시 return 해서 연체
3회 판정에 영영 닿지 못했다. 연체 알림은 "3회 누적 시 파산합니다"라고
약속하는데 연체 4회를 쌓고도 완주하는 판이 있었다.

### 12-3. 통합 적자를 체감되게 + 적자 경고

`통합 중 유지비 0.46 → 0.58`(`pmiUpkeep`). 협상 10일 · 통합 40일이라 1팀만으로도
최대 4건이 겹친다. 계산상 통합 중 계열사는 하루 `cap × 0.026` 적자다.

**먼저 신호를 준다.** 일일 손익은 회사 창을 열어야만 보여서 현금이 마르는 것을
알아챌 방법이 없었다 — 어느 날 갑자기 자금이 0 이 된다. 5일 연속 적자면 인박스와
토스트로 알리고 통합 중 계열사 수를 짚어 원인을 말해 준다.

### 12-4. 경제 위기 — 후반의 유일한 긴장

순자산 3,000억부터 걸린다. 규칙 세 가지를 지켰다.

1. **반드시 예고한다.** D-30 뉴스 → D-15 인박스(대비 안내) → D-0 발동.
   캐주얼에서 최악은 "아무것도 잘못 안 했는데 망했다"다. 조작은 0개 늘어난다.
2. **대비 수단은 전부 이미 손에 있다** — 현금 비중 · 부채 · 파산 보험 · 업종 분산.
   **가장 많이 가진 업종이 가장 크게 맞는다**(×0.42 vs ×0.62). 지금까지 업종
   집중은 순이득뿐이라 고민이 없었는데 여기서 처음으로 대가가 붙는다.
3. **위험이자 기회다.** 위기 중 매물이 28% 싸진다. 손실만 있으면 벌칙이지
   이벤트가 아니다 — 현금을 쥐고 있던 쪽이 여기서 판을 뒤집는다.

발동 확률은 0.004 → 0.013 으로 올렸다. 3판 중 1판만 겪으면 없는 콘텐츠다.
지속 90일을 70일로 줄여 봤지만 완주 일수가 노이즈 범위에서 안 변해 되돌렸다.

### 12-5. 승급 목표에 진행도를 붙였다

`active` 봇이 940일까지 대기업에 갇힌 것을 추적하다 나왔다 — 매각 15회로
사업부가 3개에서 안 늘었는데, 목표에는 "사업부 5개 결성"이라고만 적혀 있어
**지금 몇 개인지 알 방법이 없었다.** `goalText` 가 진행도를 같이 낸다.

### 12-6. 최종 계측

| 전략 | 평균 | 시드별 | 파산 | 위기 |
|---|---|---|---|---|
| normal | 743 | 735 / 862 / 633 | 0/3 | 623 / 687 / 542일차 |
| aggressive | 623 | 573 / 659 / 638 | 0/3 | 485 / 577 / 588일차 |
| reckless | 432 | 516 / 420 / 360 | **2/3** | — |
| occult | 601 | 604 / 721 / 478 | — | — |
| active | 850 | 933 / 940 / 676 | — | 716 / 766 / 565일차 |

**안전하게 하면 안전하고(normal 0/3), 무리하면 망한다(reckless 2/3).**
`aggressive·2002` 는 위기 발동 시점 현금이 −201억이었다.

smoke 138/138 · `SAVE_V 13`.

### 12-7. 남은 부채

- **`active` 850일이 튄다.** 매각 정책이 사업부 목표를 역행한다. 봇 정책 문제지만
  플레이어도 같은 함정을 밟을 수 있다 — 12-5 로 완화했을 뿐 해소는 아니다
- 대기업 목표(사업부 5개)가 업종 분포 운에 걸린다. 재편으로 강제할 수 있으므로
  막히지는 않지만, 시드에 따라 구간이 길어진다
- `reckless` 가 `aggressive` 보다 빨리 끝난다. 이름과 실제가 안 맞는다
  (§3-d 에서 leveraged → aggressive 로 개명한 것과 같은 계열)
- 상장/비상장 매집이 규칙 두 벌 (§10-8 에서 이월)

---

## 13. 버그 수정 — 인수 직후 게임 정지 · 죽은 긴축 선택지

`smoke 138/138` 과 sim 이 전부 통과하는 상태에서 **실제 플레이에서만 터지는**
버그가 두 건 있었다. 검증 도구가 왜 못 잡았는지가 이 절의 핵심이다.

### 13-1. `co.divs` 한 필드에 주인이 둘 — 치명

`state.js` 의 `co` 리터럴에 **같은 키가 두 번** 있었다. JS 는 조용히 뒤엣것을 택한다.

```js
divs: 0,     // :36  사업부 "수"          ← data.js TIERS 가 읽는다
divs: [],    // :44  사업부 "업종 키 배열"  ← 뒤가 이긴다
```

주인도 둘이었다.

```js
economy.js:tickEconomy   s.co.divs = divisionsOf(s).length;   // 매일 숫자를 적는다
mna.js:checkDivisions    const was = s.co.divs || [];         // 배열이라고 믿고 읽는다
                         now.filter(k => !was.includes(k))    // 숫자.includes → TypeError
```

사업부가 0개인 동안은 `0` 이 falsy 라 `|| []` 로 구제된다. **사업부가 하나
생기는 순간부터** 다음 `completeAcq` 가 던진다.

**증상.** `judgeNego` 의 결제 선택지가 `s.co.cash -= price; completeAcq(...); resume()`
순서라, 예외가 `resume()` 을 통째로 삼킨다.

- 돈은 빠져나가고 계열사도 편입되는데 **`speed 0` 에 멈춘 채 안 돌아온다**
- 배속 버튼으로 억지로 풀면 `pausedSpeed` 가 오염된 채라 **그 뒤로는 모달이 떠도
  게임이 안 멈춘다** — 협상 이벤트 창을 띄운 채 날짜가 흐른다
- 두 번째 이후 `사업부 출범` · `그룹 본사 출범` 연출이 영영 안 뜬다.
  대기업 승급 목표가 사업부 5개인데 그 연출이 없다

**왜 검증이 놓쳤나.** smoke 는 `사업부 결성`(divisionsOf)과 `사업부 수가 상태에
갱신된다`(co.divs)를 **각각 따로만** 봤다. 두 조건이 겹치는 경로 — 사업부가 있는
상태에서 모달을 거쳐 인수를 완료하는 — 가 없었다. sim 은 `tickDay` 를 직접 돌려
jsdom 이 onclick 예외를 삼키고, 아래 두 줄(`recalcCap`·`checkTier`)은 다음 날
루프가 어차피 다시 부르므로 **숫자가 한 자리도 안 변한다.** §12-6 표가 멀쩡했던 이유다.

**수정.** 숫자는 `divs`(economy 소유)로 두고 배열만 `divKeys` 로 분리.

```js
// mna.js:checkDivisions
const was = Array.isArray(s.co.divKeys) ? s.co.divKeys : now;   // 구버전 세이브는 현재를 기준선으로
s.co.divKeys = now;
```

`SAVE_V` 는 안 올린다 — 위 `Array.isArray` 분기가 구버전 세이브에서 중복 출범
연출을 막는다. **13 그대로.**

### 13-2. 경제 위기 '허리띠를 조인다'가 순손실이었다

`c.eased = true` 를 세우기만 하고 **읽는 곳이 소스 어디에도 없었다.**
플레이어는 인지도 −0.3 을 내고 아무것도 못 받는다. 안내 문구도
`Math.round((crisisMul + 0.08) * 100)` = **70%** 를 "손실 70% 로 완화"라고 찍었다 —
배수를 손실률 자리에 넣어, 바로 위 본문의 `-38%` 보다 나쁘게 읽혔다.

- `BAL.crisisEaseUp: 0.08` 을 꺼내고 `crisisMul()` 이 `c.eased` 를 읽게 했다
  (`Math.min(1, base + easeUp)`)
- 문구는 `손실 38% → 30% 로 완화` 로. 상수 하나를 두 곳이 같이 본다
- 위기 종료 시 `c.hard` 와 함께 `c.eased` 도 지운다 — 다음 위기로 새면 안 된다

**밸런스 무영향 확인.** sim 봇은 위기 모달에서 `cs[0]`(버틴다)를 누르므로 이 경로를
안 지난다. `--noref --strats=normal` 재측정 **735 / 862 / 633** — §12-6 과 동일.

### 13-3. 재발 방지 세 가지

1. **`npm run check` 에 중복 키 게이트.** acorn 으로 `ObjectExpression` 만 훑는다.
   전 소스 통틀어 중복은 `divs` 한 건뿐이었으니 지금 넣으면 노이즈가 0 이다.
   **acorn 이 devDependency 에 하나 늘었다** (검증 도구 전용. 빌드는 여전히 무의존)
2. **`modal.js:safeRun`.** 선택지 핸들러 대부분이 `pause()` 상태에서 불려 마지막
   줄에서 `resume()` 한다. 중간에 예외가 나면 시계가 죽는 구조라 **여기가 단일
   실패점**이다. `try/catch` 로 감싸고 실패 시 `resume()` 한다.
   `console.error` 로 남기므로 smoke 의 '런타임 에러 없음' 은 그대로 잡는다
3. **smoke 138 → 148.** 추가한 것:
   - `divs 는 숫자로만 쓰인다` · `사업부 키는 별도 필드(divKeys)`
   - **`사업부가 있어도 인수 완료가 안 터진다`** · **`인수 결제 뒤 배속이 복원된다`**
     — 모달을 실제로 클릭해 결제 경로 전체를 지난다. 이 조합이 비어 있었다
   - `두 번째 사업부도 출범 알림이 나간다` · `같은 사업부를 두 번 출범시키지 않는다`
   - `긴축이 실제로 손실을 줄인다` · `긴축은 인지도를 대가로 받는다` ·
     `위기가 끝나면 긴축도 함께 지워진다`
   - `핸들러가 던져도 배속이 복원된다` (일부러 던지는 케이스. 집계에서 뺀다)

### 13-4. 교훈

**단위 케이스 두 개가 각각 통과해도 그 둘이 만나는 지점은 비어 있을 수 있다.**
`divisionsOf` 도 맞고 `co.divs` 갱신도 맞았는데, 그 둘이 같은 필드를 다른 타입으로
쓴다는 사실은 어느 케이스도 보지 않았다. 상태 필드를 새로 넣을 때는
**누가 쓰고 누가 읽는지 한 줄로 적어 둘 것** — 주인이 둘이면 그때부터 버그다.

---

## 14. 감사 패스 — 죽은 선택지 · 죽은 기능 · 무료 디폴트 (HEAD)

계획 이행 여부를 코드와 대조하다 나온 것들이다. **계획은 다 들어가 있었다.**
문제는 (a) 문서가 코드를 못 따라와 기준선이 재현되지 않았고 (b) 그 사이에
규칙 결함 네 개가 자랐다는 것이다.

### 14-0. 문서가 코드보다 뒤처져 있었다

`§12-6` 의 `735 / 862 / 633` 이 재현되지 않았다 (실측 `675 / 826 / 662`).
smoke 도 문서의 148 이 아니라 **176** 이었다 — 문서에 없는 시스템(매대 배정 ·
매장 근무 · 직원 경험치)이 그 사이에 들어와 있었다.

> **교훈:** 기준선 표는 **재현해 보고** 갱신할 것. 표가 안 맞으면 회귀를 못 잡는
> 정도가 아니라, 없는 회귀를 쫓게 된다.

### 14-1. 위임이 죽은 선택지였다 — 가장 큼

계측: 위임 결렬률 **49%** vs 직접 **21%**, 완주 721 → 670일. 클릭 한 번의 대가로는
너무 크다. 그런데 이건 확률 문제가 아니었다.

```
설득 job·delay  ∪  자료 price·data  =  요구 4종 전부
```

`approachOf` 가 성격에서 접근을 뽑으므로(승부사→압박, intel>nego→자료, 나머지→설득),
**승부사 아닌 intel 형 1명 + nego 형 1명만 넣으면 적중률이 영구히 100%** 였다.
분산도 0이다. 테이블이 매 협상의 판단이 아니라 **편성 한 번**으로 끝나 있었다.

§4-7 은 "위임에 페널티를 두면 항상 직접이 정답이 된다"고 피하려 했는데,
감축 C 에서 hit 를 9 → 34 로 올리면서 페널티 없이 같은 결과가 났다.

**고친 것 세 가지.**

1. **상성표에 구멍을 냈다.** 세 접근이 `delay` 를 공유한다 —
   설득 `job·delay` / 자료 `data·delay` / 압박 `price·delay`.
   어느 **두** 접근을 조합해도 3/4 만 덮인다. 네 종을 다 덮으려면 세 접근을
   전부 갖춰야 하고, 그러려면 협상력이 낮은 intel 형을 협상단에 넣어야 한다 —
   `teamPower` 는 nego 만 보므로 **테이블 적중과 성공도 증가율이 서로를 물어뜯는다.**
   `delay`(시간 끌기)를 공유 축으로 고른 이유는 셋 중 유일하게 '무엇을 내주는가'가
   아니라 '언제 하는가' 라서다.
   > 4종 요구를 2칸짜리 접근 3개로 덮을 때, 어떤 두 개도 4종을 못 덮게 하려면
   > **공통 원소가 하나 있어야 한다** (삼각형 구조는 한 요구를 영영 못 덮게 만든다).
   > 그래서 `delay` 가 '누구나 받아치는 쉬운 요구' 가 된 것은 우연이 아니라 제약이다.
2. **직접 협상이 개입 1회를 쓴다** (`acts: negoActs - (direct ? 1 : 0)`).
   마감(`rivalDue`)이 걸린 판에서는 이 한 칸이 `시한 제시`를 못 쓰게 만든다.
3. **위임 쪽 기본 성공도를 올렸다** — `negoSuccessBase 1.75 → 2.15`.

**결과:** 위임 결렬률 49% → 38%, 직접 21% → 31%. **완주 일수는 거의 같아졌다**
(678 vs 682). 이게 더 나은 착지다 — *"직접은 결렬을 줄이고, 위임은 개입 여력을
남긴다"* 가 진짜 트레이드오프이고, 어느 쪽도 지배하지 않는다.

> **sim 의 기본 봇은 개입을 안 쓰므로 2번의 대가가 계측에 안 잡힌다.**
> 봇에게 직접 협상은 여전히 순이득인데도 완주가 같다는 뜻이니, 실플레이어에게는
> 위임이 유리한 판도 생긴다. `--acts --table` 을 같이 켠 봇이 다음 계측 과제다.

### 14-2. 미리 사두기가 순손실이었다 — 원인 세 겹

계측: 평균 **★0.47** (문서 §2-4 의 1.6~2.6 대비 1/3), 완주 무영향.
**그런데 묶인 돈은 1,327~4,332억이었다.** 돈은 나가는데 ★ 는 안 붙는 상태.

1. **`tickStake` 가 자금 부족 시 토글을 지웠다.** ★1 이 차는 데 2.5일이 걸리는데
   하루만 현금에 스쳐도 매집이 영구 중단되고 투입금만 남는다. 되팔면 비상장은
   15% 손실이다. → 그날만 건너뛰고 이어 사도록 고쳤다 (`'paused'` 상태, 알림 1회).
2. **봇 정책이 잘못돼 있었다.** 매집 대상을 `현금 − 진행 중 협상 cap × 1.4` 의
   잔여로 골랐는데 그 잔여가 현금의 44% 라, 다음 대상이 **늘 지금 대상의 절반
   이하**로 내려앉았다. 매집 지출은 하루 `cap × 0.012` 인데 감당 여부를 **인수가로**
   걸고 있었던 것이다. 게다가 `nextId` 우선 규칙 때문에 인수 사다리까지 같이
   낮아졌다. → 인수 판단과 같은 잣대(`cap × 1.55 <= cash × mult`)로 통일.
3. **설계에서 기회비용이 빠져 있었다.** "지분 3% = 할인 3%p 라 본전" 은 총액만
   맞고, 그 돈이 인수 시점까지 20일 넘게 묶이는 것과 결렬 시 15% 손실이
   빠져 있었다. 합치면 기대값이 음수라 켤 이유가 없다.
   → `stakePrem 0.03 → 0.040`. 여전히 "항상 켜는 게 정답"은 아니다 —
   깔아 둘 현금이 있어야 하고 결렬 손실은 그대로다.

**결과:** ★0.47 → **1.37**, 결렬률 38% → 29%.

### 14-3. 압류가 빚을 통째로 지우고 있었다 — 무료 디폴트

`s.bank.overdue` 가 **영구 누적**이라, 초반에 작은 대출로 세 번 미끄러지면 그 뒤
아무리 잘 굴려도 사형이 예약돼 있었다. 게다가 `seizeSub` 이 압류하면서 같은
카운트를 올려 **인수 대출 3건 압류 = 그 자체로 파산**이었다.
계측에서 `reckless` 가 **120일차 · 계열사 0개**로 죽었다 — 첫 M&A 평균보다 이르다.

연체를 **연속** 기준으로 바꾸고(한 달 정상 상환하면 초기화) 압류를 카운트에서
빼 봤더니, 이번엔 **`reckless` 가 0/6 파산에 완주 584일** — 무차입(726)보다
20% 빨랐다. 원인은 압류가 대출 레코드를 통째로 지우는 것이었다.
**빌린 돈은 쓰고 회사만 반납하면 채무가 소멸** → 무한 디폴트가 정답이다.

고친 것:

- 연체는 **연속** 3회 (economy.js:tickMonth 가 초기화)
- **압류도 연체로 센다** — 다만 연속 기준이라 회복 가능하다
- **담보 처분가(`subSellRate`)로 회수하고 모자란 만큼은 무담보 채무로 남긴다.**
  대가가 압류 한 번으로 끝나지 않고 현금흐름으로 이어진다

**결과:** `reckless` **3/6 파산** · `normal`/`aggressive` 0/6 — 경계가 다시 섰다.

### 14-4. 등급 구간 — 첫 M&A 가 154일차였다

| 등급 | 전 | 후 (§15 반영) |
|---|---|---|
| 구멍가게 | 37 | 9 |
| 동네슈퍼 | 117 | **30** |
| 스타트업 | 79 | 35 |
| 중소기업 | 201 | 249 |
| 중견기업 | 151 | 157 |
| 대기업 | **39** | **82** |
| 글로벌그룹 | 111 | 57 |

- **첫 M&A 154일차 → 40일차.** 자세한 것은 §15 (`tierRetailMul` 을 건드리는
  안은 §15 에서 폐기하고 되돌렸다).
- **대기업 구간 39일 → 78일.** §11-2 가 56일로 벌려 놨던 것이 되돌아와 있었다.
  **사업부 6개로 올리는 안은 폐기했다** — 6시드 중 하나가 아예 막혔다
  (§12-7 이 경고한 업종 분포 운). 대신 `사업부 5개 + 순자산 6조` 로 자금 축을
  겹쳤다. 순자산은 중소기업 이후 관문으로 안 쓰였고, 순위(글로벌)와 달리
  절대값이라 §11-2 가 지적한 '같은 지표 두 번' 에도 안 걸린다.

### 14-5. 밸런스 기준선 → **§15-4 로 갱신됨**

§15(첫 M&A 앞당기기)가 완주 일수를 통째로 밀었다. **§15-4 표만 유효하다.**

### 14-6. 바뀐 상수

```
negoSuccessBase   1.75 → 2.15    (위임 결렬률 49% → 38%)
stakePrem         0.03 → 0.040   (현금 잠김의 기회비용 보상)
```

규칙 변경(상수 아님): 상성표 `delay` 공유 · 직접 협상 개입 1회 · 매집 일시정지 ·
연체 연속 판정 · 압류 잔여 채무 · 대기업 목표에 순자산 6조.

> `tierRetailMul[1]` 을 3.2 → 4.6 으로 올렸다가 **§15 에서 되돌렸다.**
> 스타터 매물이 같은 일을 더 정확하게 하고, 이쪽은 초반 경제 전체를 부풀린다.

### 14-7. 이번에도 §10-7 을 밟았다

**빌드가 실패한 채로 `node tools/sim.mjs` 를 돌려 이전 번들을 쟀다.**
`monthlyDue` 가 `bank.js` 와 `bankPanel.js` 에 중복 정의돼 번들러가 막고 있었는데
(그 중복 자체도 이번에 정리했다), `npm run build >/dev/null 2>&1 && ...` 로
출력을 죽여 놔서 못 봤다. 그 상태로 낸 "reckless 가 안 죽는다" 는 결론이 틀렸다.

§10-7 이 이미 "함정을 적어 두는 것으로는 부족하다"고 적어 뒀는데 또 밟았다.
**계측은 아래처럼 빌드 실패와 stale 경고를 둘 다 잡고 중단하는 스크립트로만 돌릴 것.**

```bash
npm run build || exit 1                     # 출력을 죽이지 말 것
node tools/sim.mjs ... | grep -q "오래됐습니다" && exit 1
```

### 14-8. 남은 것

- **`active` 811일이 여전히 튄다** (§12-7 이월). 매각 정책이 사업부 목표를
  역행한다. 대기업 목표에 사업부가 남아 있어 구조는 그대로다
- **`--acts --table` 을 같이 켠 봇** — 직접 협상의 개입 1회 대가가 계측에 안 잡힌다
- **중소기업 206일** — 여전히 최장(30%). §2-2 의 결론(M&A 루프로는 못 건드린다)은
  다섯 번째로 유효하다. 사옥 콘텐츠로 채우는 것이 맞다 (§5-B)
- 상장/비상장 매집이 규칙 두 벌 (§10-8 에서 이월)

---

## 15. 첫 M&A 를 40일차로 — 스타터 매물 (HEAD)

### 15-1. 등급 잠금 문제가 아니었다

"동네슈퍼 단계에서도 M&A 가 되게 하자"는 제안이 나왔는데, **M&A 는 처음부터
등급으로 잠겨 있지 않다.** `startNego` 에 등급 검사는 없고 `capCeiling` 만 본다 —
구멍가게(등급 0)도 **3억까지** 살 수 있다. 인수가가 모자라도 협상은 시작할 수
있고 성사 시점에 인수 대출로 메운다 (`companyPopup` 이 그렇게 안내한다).

그럼 왜 117일이었나. `tools/probe-first-mna.mjs` 를 새로 만들어 재 봤다.

| 시드 | 최저 매물 | 인수가 | 자기자금 도달 |
|---|---|---|---|
| 1001 | 4,778만 | 5,733만 | **43일** |
| 3003 | 6,051만 | 7,261만 | 46일 |
| 6006 | 6,675만 | 8,009만 | 50일 |

**두 가지가 겹쳐 있었다.**

1. **매물 곡선의 밑동이 높다.** 최저 매물이 5,000~6,700만인데 시작 자금은 500만이다.
2. **117일은 게임이 아니라 봇의 숫자였다.** sim 봇은 `cap × 1.55 <= cash × 0.62`,
   즉 **인수가의 약 2.5배**를 쌓고서야 움직인다. 실플레이어는 자기자금 도달(43일)
   이나 그보다 더 이르게(대출) 들어갈 수 있다.

> **교훈:** 봇의 완주 일수를 '게임의 페이싱' 으로 읽으면 안 된다. 봇 정책이
> 보수적인 축은 실제보다 늦게 보인다. §14-2 에서 매집이 죽어 보였던 것과 같은 종류다.

### 15-2. 고친 것 — 곡선이 아니라 밑동 세 칸

```js
const STARTER_CAPS = [1.0e7, 1.6e7, 2.4e7];   // core/state.js
```

`seedMarket` 이 맨 아래 세 칸만 이 값으로 깐다. **곡선(`6e7 × 250000^t`)은 그대로 둔다** —
밑동을 낮추면 중반 사다리가 통째로 밀려 기준선을 다시 잡아야 한다.
`rnd()` 는 어느 갈래로 가든 한 번만 소비하므로 시드 스트림도 보존된다.

결과: 최저 인수가 약 1,100만, **자기자금 도달 10~14일.**

**구멍가게 승급 목표를 2,000만 → 1,000만으로 낮췄다.** 목표가 첫 인수가보다
높으면, 인수할 돈이 생긴 뒤에도 한참 구멍가게로 남다가 순자산 2,000만을 찍는
순간 동네슈퍼 → (첫 M&A 이미 완료) → 스타트업 으로 **승급이 이틀 사이 두 번**
터진다. §11-2 가 대기업에서 겪은 증상과 같다.
smoke 에 `구멍가게 목표 < 첫 인수가` 를 불변식으로 박아 뒀다.

**온보딩 한 줄.** 창업 인박스에 지금 살 수 있는 회사를 **이름으로** 짚어 준다
(`main.js`). 맵의 건물 대부분이 자릿수가 달라 "아직 못 하는 것" 으로 읽히는데,
실제 후보를 지목하면 그 오해가 사라진다. 조작은 0개 늘어난다.

**`tierRetailMul[1]` 은 되돌렸다** (4.6 → 3.2, 원래 값). 스타터 매물이 같은 일을
더 정확하게 하고, 매출 배수는 초반 경제 전체를 부풀린다. 바뀐 상수가 하나 줄었다.

### 15-3. 등급 구간

```
구멍가게  9 · 동네슈퍼 30 · 스타트업 35 · 중소기업 249 · 중견기업 157 · 대기업 82 · 글로벌 57
```

**첫 M&A 성사 = 스타트업 진입 = 28/60/57/27/32/36 → 평균 40일차** (전체의 6.5%).
117일차·17% 였던 것에서 내려왔다.

- **중소기업 249일(40%)** 이 더 도드라졌다. 앞이 짧아진 만큼 비중이 커진 것이고
  절대값은 거의 그대로다. §2-2 의 결론(M&A 루프로는 못 건드린다)은 다섯 번째로
  유효하다 — 사옥 콘텐츠 몫이다 (§5-B)
- 구멍가게 9일은 짧다. 경영 튜토리얼을 더 주고 싶으면 목표를 올리는 게 아니라
  **스타터 매물값을 올려야 한다** (그래야 §15-2 의 순서가 안 깨진다)

### 15-4. 밸런스 기준선 → **§16-4 로 갱신됨**

`node tools/sim.mjs --noref --strats=X --seeds=1001,2002,3003,4004,5005,6006`

| 전략 | 완주 | 결렬률 | 종료 |
|---|---|---|---|
| `normal` (위임) | **620** | 39% | 0/6 |
| `normal --table` (직접) | **580** | **26%** | 0/6 |
| `aggressive` | 526 | 40% | 0/6 |
| `occult` | 470 | 32% | **6/6 occult** |
| `reckless` | 440 | 41% | **5/6 bankrupt** |

리스크와 속도가 단조롭게 붙어 있다 — `reckless` 440 < `occult` 470 <
`aggressive` 526 < 직접 580 < 위임 620.

**`reckless` 의 실패 모양이 바뀐 것이 핵심이다.**

| | 전 | 후 |
|---|---|---|
| 파산 시점 | 120~330일차 | **300~540일차** |
| 그때 계열사 | 0~14개 | **11~28개** |
| 그때 등급 | 스타트업~중견 | **대기업~글로벌그룹** |

예전에는 첫 M&A 도 못 해 보고 죽었다. 지금은 깊이 들어간 뒤 레버리지로 무너진다.
살아남은 1개 시드(3003)는 연체 0에 **392일 완주로 전 전략 최속**이다 —
고분산 루트로 성립한다.

### 15-5. 새 도구

`tools/probe-first-mna.mjs` — 최저 매물값과 자기자금 도달일을 시드별로 찍는다.
**페이싱을 의심할 때 sim 완주 일수보다 먼저 볼 것.** 봇 정책이 섞이지 않는다.

```bash
node tools/probe-first-mna.mjs
```

---

## 16. 조용한 버그 세 건 — 예외가 안 나는 종류 (HEAD)

"더 잡을 게 없나" 로 한 번 더 훑어서 나온 것들이다. **셋 다 예외가 안 나고,
셋 다 스모크를 통과하고 있었다.** 그래서 각각에 대해 기계적 방어를 같이 넣었다.

### 16-1. 협상단이 경험치를 못 받고 있었다 — 테스트가 버그를 가렸다

2팀 전환(§12-2)에서 `s.nego`(단일) → `s.negos`(배열)로 바뀌었는데 **네 곳이 옛
필드를 계속 읽고 있었다.** `undefined` 는 falsy 라 조용히 전부 '협상 안 함' 으로
읽혔다.

| 위치 | 증상 |
|---|---|
| `company.js:tickStaff` | 협상단 전원이 `expNego`(4) 가 아니라 `expIdle`(1) 을 받았다 |
| `ui/tabs.js` | 직원 카드가 협상 중에도 '협상 대기' 로 표시됐다 |
| `ui/hirePanel.js` ×3 | **협상 중인 직원을 아무 경고 없이 내보낼 수 있었다** |

첫 번째가 제일 크다. "경험치는 **실제로 한 일**에서만 나온다 — 협상에 나가거나
계열사를 관리하거나. 놀고 있으면 거의 안 는다" 는 설계(§`BAL.expNego` 주석)가
**정확히 반대로** 돌고 있었다. 협상단에 넣으면 오히려 제일 적게 받았다.

**스모크가 이걸 가리고 있었다.**

```js
S.nego = { id:'x', ..., team:[a.id], ... };   // 옛 필드를 손으로 세운다
g.tickStaff(S);
r.push(['협상단은 협상에서 경험치', a.exp - e0 === B.expNego, ...]);   // ✓ 통과
```

테스트가 상태를 손으로 조립하면 **그 조립이 곧 사양이 된다.** 필드 이름이 바뀌어도
테스트는 옛 이름으로 계속 통과한다. 지금은 실제 진입점(`startNego`)으로 협상을
만들어 검사한다.

**고친 것.** 판정을 `mna.js:negoInSlot(s, e)` 한 곳에 두고 UI 가 그걸 부른다.
2팀이므로 **자기 슬롯이 협상 중일 때만** 협상 경험치다 — 빈 팀에 편성만 해 두고
노는 것이 이득이 되면 안 된다.

**방어.** `tools/deadfield.mjs` — 상태에 없는 필드를 읽는 곳을 정적으로 훑는다.
`npm run check` 에 붙였다. 같이 걸린 `s.lastEvent_`(상태에 얹힌 함수 참조)도
모듈 지역 변수로 내렸다 — 세이브에 JSON 으로 안 남는 값이 상태에 있으면
죽은 참조와 구분이 안 된다.

### 16-2. Esc 한 번으로 시계를 죽일 수 있었다

`pause()` 뒤에 열린 모달을 **✕ 버튼이나 Esc 로 닫으면** `resume()` 이 선택지
핸들러 안에만 있어서 `speed 0` 에 멈춘 채 안 돌아왔다. 배속 버튼으로 억지로 풀면
`pausedSpeed` 가 오염돼 **그 뒤로는 모달이 떠도 게임이 안 멈춘다.**

§13-1 이 `completeAcq` 의 TypeError 로 겪었던 바로 그 증상이다. 그때 `safeRun` 으로
예외 경로는 막았는데 **닫기 경로는 비어 있었다.** 해당하는 모달이 여섯 개였다
(협상 이벤트 · 협상 테이블 · 인수가 확정 · 협상 결렬 · 일반 이벤트 · 경제 위기).

게다가 그중 넷은 **선택 자체가 판정**이라 닫기로 빠져나가는 것이 이득이었다 —
악재 이벤트는 성공도가 안 깎이고, 일반 이벤트는 비용을 안 낸다. 이벤트가
8.5일마다 오므로 "Esc 로 넘긴다" 가 최적 플레이가 된다.

**고친 것 두 겹.**

1. `modal.js:dismissModal` — 닫기 전용 경로. 마지막 모달이 닫힐 때 `resume()` 한다.
   중첩 모달의 위쪽만 닫는 경우에는 아래가 여전히 결정을 기다리므로 멈춰 둔다
2. 결정 모달 여섯 개에 `dismissable: false` — 닫기 버튼이 아예 안 뜬다

### 16-3. 협상 테이블에 다라운드 시절의 죽은 선택지가 남아 있었다

`나머지는 협상단에 맡긴다` — 단판(§10-5)이 된 뒤로 '나머지' 가 없고, 위임 결과는
정확히 0 이다. **파견 때 이미 개입 1회를 내고 직접 협상을 골랐는데 그 값을 버리는**
언제나 손해인 버튼이었다. 제거했다. 맡길 거면 파견 단계에서 맡긴다.

### 16-4. 계측 — 밸런스는 노이즈 범위

경험치 수정으로 협상력이 오르므로 기준선을 다시 잡았다 (6시드).

| 전략 | §15-4 | §16 | 종료 |
|---|---|---|---|
| `normal` (위임) | 620 | **616** · 결렬률 38% | 0/6 |
| `normal --table` | 580 | **588** · 결렬률 26% | 0/6 |
| `aggressive` | 526 | 518 | 0/6 |
| `occult` | 470 | 470 | **6/6 occult** |
| `reckless` | 440 | 374 | **4/6 bankrupt** |

**이 표가 현재 기준선이다.** smoke **197/197**.

### 16-5. 새로 추가된 방어

| 도구 | 무엇을 막나 |
|---|---|
| `tools/deadfield.mjs` (`npm run check`) | 이름이 바뀐 뒤 남은 죽은 상태 참조 |
| smoke `닫아도 배속이 복원된다` | 닫기 경로에서 시계가 죽는 것 |
| smoke `결정 모달에는 닫기 버튼이 없다` | 판정을 Esc 로 회피하는 것 |
| smoke `협상 안 하는 팀은 협상 경험치가 아니다` | 2팀에서 편성만 해 두고 노는 것 |

### 16-6. 남은 것 (판단 필요, 손대지 않음)

- **`간판스타`(매장 매출 +12%)가 협상단에 있어도 매출을 올린다.** `retailPotential`
  의 `salesBuf` 가 `s.staff` 전체를 세는데, 이건 매장 근무(`shopBuf`)가 생기기 전부터
  있던 항이라 기준선의 일부다. '한 사람은 한 곳에만' 원칙과는 어긋나지만,
  `shopOf` 로 좁히면 아무도 매장에 없을 때 배수가 내려가 **초반 경제가 통째로
  밀린다.** 고칠 거면 `shopBuf` 와 함께 다시 잡아야 한다
- **`active` 전략**이 여전히 튄다 (§12-7 · §14-8 이월)
- 상장/비상장 매집이 규칙 두 벌 (§10-8 이월)
