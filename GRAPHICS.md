# 그래픽 — 회전과 해상도

## 결정 변경 (이전 판 폐기)

앞선 판에서 **three.js 이식**을 권고했으나 **철회한다.** 현행 캔버스 2D 를 유지하고
두 가지만 얹는다.

1. **카메라 90° 회전 4방향** — 가림 문제의 근본 해결
2. **월드 해상도 상향** — 도트 수를 늘려 세밀하게

철회 이유는 회전 비용을 과대평가했기 때문이다. 2D 쿼터뷰에서 90° 회전은
**타일 좌표 변환 하나**로 끝난다. `prism` 은 좌우 대칭이라 건물 몸통은 어느
방향에서 봐도 같고, 방향을 타는 건 **간판·옥상 소품·창문 배치뿐**이다.
그걸 위해 600KB 의존성과 오프라인 배포 포기를 감수할 이유가 없다.

(`proto/iso3d.html` 은 참고용으로 남겨 둔다. 나중에 진짜 3D 가 필요해지면 출발점이다.)

---

## 1. 카메라 회전 — **구현 완료**

### 좌표 변환

투영식이 `x = (gx-gy)*16`, `y = (gx+gy)*8` 이므로, 격자만 돌리면 화면도 돈다.

```js
// iso.js
function rotTile(gx, gy, view, W, H) {
  switch (view & 3) {
    case 0: return [gx, gy];
    case 1: return [gy, W - 1 - gx];
    case 2: return [W - 1 - gx, H - 1 - gy];
    case 3: return [H - 1 - gy, gx];
  }
}
```

**그리는 쪽과 판정하는 쪽이 같은 함수를 쓰면 클릭은 자동으로 따라온다.**
`lotC()` 가 회전된 좌표를 쓰면 `isoHit` / `plateHit` 은 손댈 필요가 없다.

**결과: `iso.js` 에 `rotG` / `rotFace` 를 두고 `city.js` 의 모든 타일→월드 변환을
`cityX` / `cityY` 한 쌍으로 모았다.** 그리는 쪽과 판정하는 쪽이 같은 함수를 지나므로
클릭 판정은 자동으로 따라온다. 조작은 독의 ◀ ▶ 버튼과 <kbd>Q</kbd> <kbd>E</kbd>.

실제로 손댄 곳은 예상보다 적었다 — 건물 몸통은 `prism` 이 좌우 대칭이라 방향을
안 타고, 간판도 항상 카메라 쪽 면에 붙으므로 그대로 뒀다. **방향을 탄 건
도로 차선(홀수 뷰에서 rx·ry 스왑), 보행자 방향(`rotFace`), 차체 진행축 셋뿐이다.**

검수는 `npm run shot` 이 `shots/view0..3.png` 를 떨군다. 스모크에는 네 방향 모두
"보이는 위치 ≠ 판정 위치" 가 생기지 않는지 검사를 넣었다.

### 손봤던 곳

| 대상 | 할 일 | 난이도 |
|---|---|---|
| `cityGround()` | 지면 레이어를 view 별로 굽는다. 캐시 키에 view 추가 | 낮음 |
| `drawSign` | 간판이 붙는 면을 view 로 고른다. 지금은 항상 남쪽 | 낮음 |
| `drawRoofProps` · 굴뚝 · 헬리패드 | 옥상 소품 위치를 회전 | 낮음 |
| `isoWin` 창문 배치 | 좌·우면 인자를 view 에 따라 스왑 | 낮음 |
| `traffic` · 손님 | 진행 방향(`pedFace`)을 view 만큼 회전 | 중간 |
| `store.js` 사옥 | **회전 안 한다.** 매장·사무실은 고정 배치가 읽기 쉽다 | — |
| 세이브 | `s.view` 추가 → `SAVE_V` 상향 | 낮음 |
| UI | 독에 회전 버튼 2개 + <kbd>Q</kbd> <kbd>E</kbd> | 낮음 |

전환은 즉시 스냅이 아니라 **한 번 페이드**로 덮는다. 2D 라 중간 각도를 못 그리므로
회전 중 프레임을 보여 주려 하면 어색해진다. `zoomInto` 의 연출을 재활용한다.

전환은 스냅이 아니라 `zoomInto` 와 같은 페이드로 130ms 덮는다. 2D 라 중간 각도를
못 그리므로 회전 프레임을 보여 주려 하면 어색해진다.

---

## 2. 해상도 상향 — 논의가 필요한 부분

### 지금 무슨 일이 벌어지고 있나

`fitCanvas()` 는 배율 `PX` 를 정수로만 잡는다. 1080p 브라우저 기준 캔버스는
약 **1904×865 디바이스 픽셀**이고, 현재 월드는 672×384 라 `PX = 2` 다.

즉 **화면에 담기는 월드 픽셀이 952×432 장뿐이다.** 건물 하나가 가로 40 도트쯤
쓰는데 그 안에 창문·간판·차양을 다 넣어야 하니 세밀해질 여지가 없다.
"더 고해상도로" 의 실체는 **이 952×432 를 늘리는 것**이다.

늘리는 방법은 하나뿐이다 — **`PX` 를 1로 낮추고 아트를 그만큼 키워 그린다.**
`PX=1` 이면 담기는 월드 픽셀이 1904×865, 즉 **4배**가 된다.

> `PX=1` 이라고 픽셀 감성이 사라지는 건 아니다. DPR 2 디스플레이에서는
> `CV.width = CSS폭 × 2` 라 `PX` 가 자동으로 2로 잡히고 월드 픽셀 1개가
> CSS 픽셀 1개가 된다. **레티나에서는 지금과 똑같이 또렷하다.**
> 달라지는 건 DPR 1 모니터로, 도트가 화면 픽셀 1개가 되어 **덜 청키해진다.**
> "더 고해상도로 가는 느낌" 이 정확히 이 변화다.

### 선택지

타일 크기를 키우면서 `PX` 를 1로 내린다. 수치는 1904×865 기준이다.

| 안 | 타일 | PX | 월드 크기 | 블록 격자 | 화면상 타일 폭 | 도트 밀도 | 배치 가능 회사 |
|---|---|---|---|---|---|---|---|
| **현행** | 32×16 | 2 | 672×384 | 7×7 = 49 | 64px | 1× | 38 |
| **A 고해상도** | 64×32 | 1 | 1344×768 | 7×7 = 49 | 64px | **4×** | 38 |
| **B 광역+고해상도** | 48×24 | 1 | 1560×840 | **10×10 = 100** | 48px | **2.25×** | **~80** |
| **C 절충** | 56×28 | 1 | 1596×812 | 9×9 = 81 | 56px | 3.06× | ~64 |

### 결정 — **B (48×24, 10×10 블록)** ← 확정, 미구현

**3번과 5번이 같은 문제이기 때문이다.** 맵이 비어 보이고 회사가 적은 것도,
디테일이 안 들어가는 것도, 전부 "화면에 담기는 월드 픽셀이 952×432 뿐" 에서
나온다. B 는 도트를 2.25배로 늘리면서 블록을 49 → 100 으로 두 배 넓힌다.
회사를 80개까지 놓을 수 있어 **이번에 38개로 늘린 걸 다시 두 배로** 갈 수 있다.

A(64×32) 는 디테일이 가장 좋지만 맵 크기가 그대로라 5번이 안 풀린다.
C 는 둘 사이인데 B 대비 이득이 크지 않으면서 블록이 19칸 적다.

**B 의 대가는 화면상 타일이 64px → 48px 로 25% 작아지는 것이다.**
건물이 조금 작아 보인다. 다만 도트는 2.25배라 **작아지면서 더 선명해진다.**
이 교환이 받아들여지는지가 판단 지점이고, 이건 계측이 안 된다 — 봐야 안다.

### 실제 비용 — 여기가 진짜 문제다

**타일 크기를 키워도 그림은 저절로 좋아지지 않는다.** 좌표를 전부 곱하면
같은 그림이 굵은 픽셀로 나올 뿐이다. **이득은 재작화에서만 나온다.**

| 작업 | 성격 | 예상 |
|---|---|---|
| `iso.js` TW/TH 상수 변경 | 기계적. `rhomb`/`prism` 은 인자를 받으므로 자동 | 1시간 |
| `city.js` 좌표·치수 스케일 | 하드코딩 숫자 200여 개. 기계적이지만 **눈으로 다 봐야 함** | 0.5일 |
| `store.js` 좌표·치수 스케일 | 방 크기(16×10 타일)와 월드 크기 재계산 포함 | 0.5일 |
| **건물 5종 재작화** | 창틀·차양·간판·외벽 패턴·옥상. **여기가 본체** | 2~3일 |
| 지형·소품 재작화 | 나무·집·논밭·차량·사람 | 1~2일 |
| `RENDER.md` 개정 | 배율 정책이 바뀌므로 필수 | 0.5일 |

**총 5~7일.** 회전(1~2일)과 합치면 6~9일이다.

부수 확인 사항:

- **지면 레이어 메모리** — 현재 약 65만 픽셀. B 에서 약 260만 픽셀 → 10MB.
  view 4방향을 다 캐시하면 40MB 라 **회전 시 다시 굽는 쪽**이 맞다 (1방향만 보관)
- **드로우 비용** — `rhomb` 는 `ry` 루프, `prism` 은 `rx/2` 루프라 **선형**으로 는다.
  타일 1.5배면 호출당 1.5배. 지면은 캐시되므로 프레임 비용은 건물·소품만
- **`PX=1` 이 하한이라 오히려 안정적이다** — 지금은 창이 작아지면 `PX` 가 2 → 1 로
  떨어져 맵이 절반이 되는 실패 모드가 있다. `PX=1` 이 기본이면 더 떨어질 곳이 없다

### 단계

```
1단계  회전                      ✔ 완료
2단계  좌표계 스케일 (1일)        ← 다음. 아래 상수만 바꾸면 된다
3단계  재작화 (4~6일)             건물부터. 한 종류씩 하고 npm run shot 으로 대조
```

**2단계에서 바꿀 값 (확정)**

| 위치 | 지금 | 바꿀 값 |
|---|---|---|
| `iso.js` `TW`, `TH` | 32, 16 | **48, 24** |
| `balance.js` `cityBlocks` | 7 | **10** |
| `balance.js` `npcCount` | 38 | **80** |
| `canvas.js` `CITY_HEAD` | 40 | **60** (헤드룸도 1.5배) |
| `canvas.js` `CITY_H` 식의 꼬리 여백 | 24 | **36** |
| `city.js` · `store.js` 하드코딩 치수 | — | **×1.5 후 짝수로 반올림** |

마지막 줄이 중요하다. `rhomb` 은 `rx = 2*ry` 이고 `ry` 가 정수여야 계단이 맞으므로
**`rx` 는 항상 짝수여야 한다.** 22 → 33 이 아니라 **34** 로 간다. 홀수로 두면
마름모 폭이 한 줄씩 어긋나 사선이 지저분해진다.

`store.js` 는 방 크기(16×10 타일)가 월드 크기를 정하므로 `STORE_W`/`STORE_H` 도
같이 다시 계산해야 한다.

**2단계에서 멈추고 판단할 것.** 재작화는 되돌리기 비싼 유일한 구간이다.
2단계 직후의 그림은 "굵은 픽셀" 상태라 지금보다 나아 보이지 않는 게 정상이다.
거기서 봐야 하는 건 화질이 아니라 **맵 크기와 건물 밀도**다.

---

## 왜 이번에 맵을 8×8 로 못 늘렸나 (계측 기록)

5번 요청을 받고 블록을 7×7 → 8×8 로 늘려 봤다가 되돌렸다.

| | 월드 높이 | PX=2 에 필요한 캔버스 높이 |
|---|---|---|
| 7×7 (이전) | 404 | 808px |
| **8×8** | **432** | **864px** |
| 7×7 (현재, 여백 축소) | **384** | **768px** |

1080p 브라우저의 캔버스 높이가 약 865px 이다. 8×8 은 **여유가 1px** 이라
북마크 바 하나만 켜도 `PX` 가 1로 떨어져 맵이 절반이 된다. 실패 모드가 너무 급하다.

그래서 7×7 을 유지하되 여백을 깎아 384 로 줄였다 (`PX=2` 요구가 808 → 768px 로
내려가 여유가 늘었고, 1440p 에서는 `PX=3` 도 닿는다). 대신 **회사를 23 → 38 로
늘리고 도시 밖을 교외 주택가로 채워** 밀도로 해결했다.

계측: 순수 초록(빈 잔디) 화면 점유율 **44.0% → 14.2%**. (`npm run shot` 후 픽셀 집계)

**맵을 진짜로 넓히려면 해상도 정책부터 바꿔야 한다.** 그게 위의 B 안이다.

---

## 되돌릴 조건

- DPR 1 모니터에서 도트가 1px 이 되며 아기자기함이 죽는다 → A(64×32) 로 후퇴
- 재작화 없이 좌표만 키운 그림이 오히려 조잡해 보인다 → 현행 유지
- 회전 후에도 상호판 클릭이 여전히 필요하다 → 그건 정상이다. 회전은 가림을
  줄이지 클릭을 대체하지 않는다. 둘 다 남긴다

---

## 지금도 유효한 규칙

`RENDER.md` 의 다음은 해상도가 바뀌어도 그대로다.

- 폰트는 **10 / 12 / 15 세 단만** — 글자는 월드 층이 아니라 텍스트 층에 그린다.
  타일이 커져도 글자는 안 커진다. 오히려 **상대적으로 작아져 라벨 밀도가 는다**
- 배율은 **정수만**
- 사선은 `fill()` 이 아니라 `fillRect` 계단으로
- 최상위 이름은 전부 고유 (번들러 제약)
