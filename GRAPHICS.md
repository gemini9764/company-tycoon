# 그래픽 — 회전과 해상도

## 결정 변경 (이전 판 폐기)

앞선 판에서 **three.js 이식**을 권고했으나 **철회한다.** 현행 캔버스 2D 를 유지하고
두 가지만 얹는다.

1. **카메라 90° 회전 4방향** — 가림 문제의 근본 해결 (완료)
2. **월드 해상도 상향** — 도트 수를 늘려 세밀하게 (좌표계 완료 · 재작화 남음)

철회 이유는 회전 비용을 과대평가했기 때문이다. 2D 쿼터뷰에서 90° 회전은
**타일 좌표 변환 하나**로 끝난다. `prism` 은 좌우 대칭이라 건물 몸통은 어느
방향에서 봐도 같고, 방향을 타는 건 **간판·옥상 소품·창문 배치뿐**이다.
그걸 위해 600KB 의존성과 오프라인 배포 포기를 감수할 이유가 없다.

(`proto/iso3d.html` 은 참고용으로 남겨 둔다. 나중에 진짜 3D 가 필요해지면 출발점이다.)

---

## 1. 카메라 회전 — **구현 완료**

### 좌표 변환

투영식이 `x = (gx-gy)*16`, `y = (gx+gy)*8` 이므로, 격자만 돌리면 화면도 돈다.

```js
// iso.js
function rotTile(gx, gy, view, W, H) {
  switch (view & 3) {
    case 0: return [gx, gy];
    case 1: return [gy, W - 1 - gx];
    case 2: return [W - 1 - gx, H - 1 - gy];
    case 3: return [H - 1 - gy, gx];
  }
}
```

**그리는 쪽과 판정하는 쪽이 같은 함수를 쓰면 클릭은 자동으로 따라온다.**
`lotC()` 가 회전된 좌표를 쓰면 `isoHit` / `plateHit` 은 손댈 필요가 없다.

**결과: `iso.js` 에 `rotG` / `rotFace` 를 두고 `city.js` 의 모든 타일→월드 변환을
`cityX` / `cityY` 한 쌍으로 모았다.** 그리는 쪽과 판정하는 쪽이 같은 함수를 지나므로
클릭 판정은 자동으로 따라온다. 조작은 독의 ◀ ▶ 버튼과 <kbd>Q</kbd> <kbd>E</kbd>.

실제로 손댄 곳은 예상보다 적었다 — 건물 몸통은 `prism` 이 좌우 대칭이라 방향을
안 타고, 간판도 항상 카메라 쪽 면에 붙으므로 그대로 뒀다. **방향을 탄 건
도로 차선(홀수 뷰에서 rx·ry 스왑), 보행자 방향(`rotFace`), 차체 진행축 셋뿐이다.**

검수는 `npm run shot` 이 `shots/view0..3.png` 를 떨군다. 스모크에는 네 방향 모두
"보이는 위치 ≠ 판정 위치" 가 생기지 않는지 검사를 넣었다.

### 손봤던 곳

| 대상 | 할 일 | 난이도 |
|---|---|---|
| `cityGround()` | 지면 레이어를 view 별로 굽는다. 캐시 키에 view 추가 | 낮음 |
| `drawSign` | 간판이 붙는 면을 view 로 고른다. 지금은 항상 남쪽 | 낮음 |
| `drawRoofProps` · 굴뚝 · 헬리패드 | 옥상 소품 위치를 회전 | 낮음 |
| `isoWin` 창문 배치 | 좌·우면 인자를 view 에 따라 스왑 | 낮음 |
| `traffic` · 손님 | 진행 방향(`pedFace`)을 view 만큼 회전 | 중간 |
| `store.js` 사옥 | **회전 안 한다.** 매장·사무실은 고정 배치가 읽기 쉽다 | — |
| 세이브 | `s.view` 추가 → `SAVE_V` 상향 | 낮음 |
| UI | 독에 회전 버튼 2개 + <kbd>Q</kbd> <kbd>E</kbd> | 낮음 |

전환은 즉시 스냅이 아니라 **한 번 페이드**로 덮는다. 2D 라 중간 각도를 못 그리므로
회전 중 프레임을 보여 주려 하면 어색해진다. `zoomInto` 의 연출을 재활용한다.

전환은 스냅이 아니라 `zoomInto` 와 같은 페이드로 130ms 덮는다. 2D 라 중간 각도를
못 그리므로 회전 프레임을 보여 주려 하면 어색해진다.

---

## 2. 해상도 상향 — 논의가 필요한 부분

### 지금 무슨 일이 벌어지고 있나

`fitCanvas()` 는 배율 `PX` 를 정수로만 잡는다. 1080p 브라우저 기준 캔버스는
약 **1904×865 디바이스 픽셀**이고, 현재 월드는 672×384 라 `PX = 2` 다.

즉 **화면에 담기는 월드 픽셀이 952×432 장뿐이다.** 건물 하나가 가로 40 도트쯤
쓰는데 그 안에 창문·간판·차양을 다 넣어야 하니 세밀해질 여지가 없다.
"더 고해상도로" 의 실체는 **이 952×432 를 늘리는 것**이다.

늘리는 방법은 하나뿐이다 — **`PX` 를 1로 낮추고 아트를 그만큼 키워 그린다.**
`PX=1` 이면 담기는 월드 픽셀이 1904×865, 즉 **4배**가 된다.

> `PX=1` 이라고 픽셀 감성이 사라지는 건 아니다. DPR 2 디스플레이에서는
> `CV.width = CSS폭 × 2` 라 `PX` 가 자동으로 2로 잡히고 월드 픽셀 1개가
> CSS 픽셀 1개가 된다. **레티나에서는 지금과 똑같이 또렷하다.**
> 달라지는 건 DPR 1 모니터로, 도트가 화면 픽셀 1개가 되어 **덜 청키해진다.**
> "더 고해상도로 가는 느낌" 이 정확히 이 변화다.

### 선택지

타일 크기를 키우면서 `PX` 를 1로 내린다. 수치는 1904×865 기준이다.

| 안 | 타일 | PX | 월드 크기 | 블록 격자 | 화면상 타일 폭 | 도트 밀도 | 배치 가능 회사 |
|---|---|---|---|---|---|---|---|
| **현행** | 32×16 | 2 | 672×384 | 7×7 = 49 | 64px | 1× | 38 |
| **A 고해상도** | 64×32 | 1 | 1344×768 | 7×7 = 49 | 64px | **4×** | 38 |
| **B 광역+고해상도** | 48×24 | 1 | 1560×840 | **10×10 = 100** | 48px | **2.25×** | **~80** |
| **C 절충** | 56×28 | 1 | 1596×812 | 9×9 = 81 | 56px | 3.06× | ~64 |

### 결정 — **B (48×24, 10×10 블록)** ← 확정, **구현 완료**

**3번과 5번이 같은 문제이기 때문이다.** 맵이 비어 보이고 회사가 적은 것도,
디테일이 안 들어가는 것도, 전부 "화면에 담기는 월드 픽셀이 952×432 뿐" 에서
나온다. B 는 도트를 2.25배로 늘리면서 블록을 49 → 100 으로 두 배 넓힌다.
회사를 80개까지 놓을 수 있어 **이번에 38개로 늘린 걸 다시 두 배로** 갈 수 있다.

A(64×32) 는 디테일이 가장 좋지만 맵 크기가 그대로라 5번이 안 풀린다.
C 는 둘 사이인데 B 대비 이득이 크지 않으면서 블록이 19칸 적다.

**B 의 대가는 화면상 타일이 64px → 48px 로 25% 작아지는 것이다.**
건물이 조금 작아 보인다. 다만 도트는 2.25배라 **작아지면서 더 선명해진다.**
이 교환이 받아들여지는지가 판단 지점이고, 이건 계측이 안 된다 — 봐야 안다.

### 실제 비용 — 여기가 진짜 문제다

**타일 크기를 키워도 그림은 저절로 좋아지지 않는다.** 좌표를 전부 곱하면
같은 그림이 굵은 픽셀로 나올 뿐이다. **이득은 재작화에서만 나온다.**

| 작업 | 성격 | 예상 |
|---|---|---|
| `iso.js` TW/TH 상수 변경 | 기계적. `rhomb`/`prism` 은 인자를 받으므로 자동 | 1시간 |
| `city.js` 좌표·치수 스케일 | 하드코딩 숫자 200여 개. 기계적이지만 **눈으로 다 봐야 함** | 0.5일 |
| `store.js` 좌표·치수 스케일 | 방 크기(16×10 타일)와 월드 크기 재계산 포함 | 0.5일 |
| **건물 5종 재작화** | 창틀·차양·간판·외벽 패턴·옥상. **여기가 본체** | 2~3일 |
| 지형·소품 재작화 | 나무·집·논밭·차량·사람 | 1~2일 |
| `RENDER.md` 개정 | 배율 정책이 바뀌므로 필수 | 0.5일 |

**총 5~7일.** 회전(1~2일)과 합치면 6~9일이다.

부수 확인 사항:

- **지면 레이어 메모리** — 현재 약 65만 픽셀. B 에서 약 260만 픽셀 → 10MB.
  view 4방향을 다 캐시하면 40MB 라 **회전 시 다시 굽는 쪽**이 맞다 (1방향만 보관)
- **드로우 비용** — `rhomb` 는 `ry` 루프, `prism` 은 `rx/2` 루프라 **선형**으로 는다.
  타일 1.5배면 호출당 1.5배. 지면은 캐시되므로 프레임 비용은 건물·소품만
- **`PX=1` 이 하한이라 오히려 안정적이다** — 지금은 창이 작아지면 `PX` 가 2 → 1 로
  떨어져 맵이 절반이 되는 실패 모드가 있다. `PX=1` 이 기본이면 더 떨어질 곳이 없다

### 단계

```
1단계   회전                     ✔ 완료
2단계   좌표계 스케일             ✔ 완료
2.5단계 밀도·색·캐릭터            ✔ 완료 (아래)
3단계   건물 재작화              ✔ 완료 (아래)
```

### 3단계 — 건물 5종 재작화

다섯 종이 **공유하는 마감 프리미티브**를 먼저 만들고, 그 위에 종류별 특징만
얹는 구조로 갔다. 32×16 시절엔 넣을 자리가 없던 것들이다.

| 프리미티브 | 하는 일 |
|---|---|
| `pane` | 유리에 창틀을 두르고 아랫변에 반사를 넣는다 |
| `windowGrid` | **열 수를 rx 로 계산해** 창을 면 폭에 맞춘다 |
| `floorBands` | 층 구분 띠 |
| `cornerPost` | 가까운 모서리의 세로 기둥 — 두 면이 만나는 선을 살린다 |
| `parapet` | 옥상 테두리 한 겹 |
| `roofDeck` | 옥상 바닥 + 물탱크 · 실외기 · 채광창 |
| `awning` | 면의 일부 구간에만 얹는 줄무늬 차양 |

종류별 실루엣:

| 종류 | 업종 | 실루엣 |
|---|---|---|
| tower | IT · 금융 · 전자 | 수직 멀리언으로 위로 뻗고, 72px 넘으면 옥탑이 한 단 물러난다 |
| lab | 제약 | 낮고 넓다. 가로로 긴 유리 띠 + 흰 외벽 + 접시 안테나 |
| plant | 건설 | 골판 외벽 + 톱니 지붕 3개 + 굴뚝과 연기 |
| neon | 미디어 | 벽 한 면이 통째로 전광판. 주사선이 흐르고 테두리가 깜빡인다 |
| shop | 생필품 · 식품 · 유통 · 의류 | 큰 쇼윈도 + 줄무늬 차양 + 옥상 간판 |

**여기서 두 번 걸렸다.**

1. **창 열 자리를 상수로 박으면 작은 건물에서 창이 사라진다.** `isoWin` 이
   `d > rx - 2` 를 잘라내므로, `10 + i*12` 같은 고정 좌표는 rx=30 짜리 건물에서
   바깥 열이 통째로 없어진다. 재작화 전 그림이 휑했던 직접 원인이 이것이었고,
   `windowGrid` 가 rx 로 열 수를 계산해 푼다.
2. **1층 간판띠가 작은 건물의 벽을 다 먹는다.** 상가는 몸통이 33px 뿐인데
   기단+띠+립에 18px 를 쓰면 창을 넣을 자리가 안 남는다. 15px 로 줄였다.

검수는 `npm run shot` 이 떨구는 **`shots/style-{tower,lab,plant,neon,shop}.png`**
다섯 장이다 — 업종을 통째로 바꿔 3x 로 확대해 찍으므로, 섞여 있는 도시 그림으로는
안 보이던 마감이 그대로 드러난다.

### 2.5단계 — 2단계를 보고 나서 고친 것

2단계 직후의 화면을 보고 나온 지적들을 여기서 풀었다.

| 지적 | 조치 |
|---|---|
| 건물이 다닥다닥 붙어 있다 | 블록 간격 3 → **4타일**. 블록 사이 도로가 한 줄에서 두 줄이 됐다 |
| 맵 가장자리가 잘린다 | 블록 10×10 → **8×8**(회사 44). 월드 1536×824 |
| 색감이 눈이 아프다 | 맵 색 91종 + 업종색 10종을 **파스텔**로. 규칙은 RENDER.md §7 |
| NPC 가 조잡하다 | 22×44 로 **재작화**. 규칙은 RENDER.md §8 |
| 사옥 갔다 오면 맵이 줄어든다 | `fitCanvas` 가 CSS 크기를 px 로 박던 문제. RENDER.md §3 |
| 맵을 움직일 수 없다 | **드래그 이동 + 휠 줌 1~3x**. RENDER.md §카메라 |

파스텔로 가면서 걸린 것은 전부 "밝아지니 대비가 사라진다" 한 갈래였다 —
그림자가 회색 얼룩이 되고, 창문이 몸통에 묻히고, 건물 몸통을 어두운 색과
섞으면 파스텔이 아예 안 나온다. RENDER.md §7 에 규칙으로 남겼다.

**2단계에서 바꾼 값 (실제 반영분)**

| 위치 | 이전 | 지금 |
|---|---|---|
| `iso.js` `TW`, `TH` | 32, 16 | **48, 24** |
| `balance.js` `cityBlocks` | 7 | **10** (100블록 / 30×30 타일) |
| `balance.js` `npcCount` | 38 | **80** |
| `canvas.js` `CITY_HEAD` | 40 | **60** |
| `canvas.js` `CITY_H` 꼬리 여백 | 24 | **36** → 월드 1440×792, `PX=1` |
| `canvas.js` `STORE_W`/`STORE_H` | 416×280 | **624×404**, `PX=2` |
| `canvas.js` `STORE_O.y` | 56 | **72** |
| `city.js` · `store.js` 하드코딩 치수 | — | **×1.5, `rx` 는 짝수 반올림** |
| `state.js` `SAVE_V` | 9 | **10** (lot 좌표가 바뀌어 구버전 세이브 폐기) |
| `index.html` `#title-cv` | 320×150 백킹 | **640×300**, CSS 1:1 |

`rhomb` 은 `rx = 2*ry` 이고 `ry` 가 정수여야 계단이 맞으므로 **`rx` 는 항상 짝수다.**
22 → 33 이 아니라 **34** 로 갔다. 홀수로 두면 마름모 폭이 한 줄씩 어긋나 사선이 지저분해진다.

### 2단계에서 배운 것 (다음에 또 걸릴 자리)

**1. 숫자를 손으로 고치지 않았다.** 200여 개를 손으로 옮기면 반드시 몇 개를 빠뜨린다.
도형 호출(`rhomb`/`prism`/`faces`/`isoWin`/`fillRect` 등)의 **인자 자리만** 골라
기계적으로 곱하는 일회용 코드모드를 쓰고, 그 다음 diff 를 전부 눈으로 읽었다.
코드모드가 못 잡는 자리는 정해져 있다 — 이번에 손으로 고친 것들이다.

| 못 잡는 자리 | 예 |
|---|---|
| 변수 선언의 치수 | `const rx = s ? 8 : 10`, `BLDG_H`, `MY_H`/`MY_R` |
| 산식 안의 상수 | `Math.floor((h - 22) / 8)`, `rx = 24 + Math.round(k*4)*2` |
| 배열 리터럴 좌표 | 공원 나무 자리 `[[-18,-2], …]` |
| for 루프의 시작·끝·증가분 | 논밭 이랑, 횡단보도 줄무늬 |
| 삼항으로 준 `ry` | `big ? 4 : 3` — `rx` 만 커지고 비율이 깨진다 |

반대로 **곱하면 안 되는 숫자**도 있다. `% 6` 같은 나머지 연산, `frame / 90` 같은
애니메이션 주기, `clamp(v, 0, 1)` 의 상한, `(k - 1)` 같은 인덱스 오프셋.
코드모드에 "나눗셈 우변과 나머지 연산은 건드리지 않는다" 규칙을 넣어도
`clamp(v, 0, 1)` 처럼 새는 게 나온다. **diff 를 읽는 단계를 건너뛰면 안 된다.**

**2. 글자에 딸린 상자는 1.5배 하면 안 된다.** 글자는 텍스트 층이라 월드가 커져도
같이 안 커진다. 오히려 `PX` 가 2 → 1 로 내려가 **화면상 상대 크기가 커졌다.**
상호판·`drawLabel`·화이트보드처럼 글자를 담는 상자는 폰트 크기에서 뽑아야 한다.
(`flushPlates` 의 `bh`, `drawLabel` 의 `h = size + 6`)

**3. 상호판을 옮기면 스모크가 깨진다.** 상호판은 히트박스이기도 해서
`tools/smoke.mjs` 가 `g.y + 19` 같은 상수로 찍어 본다. 상호판 y 를 14 → 21 로
옮기면서 이 상수도 29 로 같이 옮겼다. **그리는 좌표와 판정 상자(`plateBoxes.h`)가
같은 값을 봐야 한다** — 이번에 하나는 16, 하나는 11 로 어긋나 있었다.

**4. 사옥의 세로 예산이 도시보다 빡빡하다.** 사옥은 `PX=2` 로 도는데 월드 높이가
420 이면 캔버스가 840px 있어야 한다 — 1080p 에서 여유가 25px 뿐이라 배율이 1로
떨어지는 실패 모드가 급했다. 여백을 깎아 **404**(필요 808px)로 맞췄다.

**5. 타이틀 캔버스는 백킹이 고정이라 같이 안 커진다.** 320×150 백킹을 2배로 띄우던
구조라 타일만 커지면 그림이 넘친다. 백킹을 640×300 으로 키우고 CSS 1:1 로 바꿔
**인게임과 도트 크기를 맞췄다** (인게임이 `PX=1` 이므로 타이틀도 1배여야 한다).

### 2단계 계측

| | 이전 | 지금 |
|---|---|---|
| 도시 월드 | 672×384 · `PX=2` | **1536×824 · `PX=1`(1x)** |
| 화면에 담기는 월드 픽셀 | 952×432 | **1440×792 (3.1배)** |
| 화면상 타일 폭 | 64px | 48px |
| 블록 / 회사 | 49 / 38 | **64 / 44** (2.5단계에서 100/80 → 64/44) |
| 빈 잔디 화면 점유율 | 24.9% | **25.6%** (밀도 유지) |
| 지면 레이어 | 약 65만 픽셀 | 약 231만 픽셀 (2532×912) |
| 사옥 화면 크기 | 416×280 · `PX=3` = 1248×840 | 624×404 · `PX=2` = **1248×808** |

**화면상 크기는 거의 그대로인데 도트 수가 2.25배가 됐다.** 지금 그림이 눈에 띄게
좋아 보이지 않는 게 정상이다 — 같은 그림을 굵은 픽셀로 그린 상태다.
**이득은 3단계 재작화에서만 나온다.**

~~새로 생긴 제약: 좁은 캔버스에서 도시 가장자리가 잘린다~~ → **2.5단계에서 해소.**
블록을 8×8 로 줄이고 **드래그·줌**을 붙였다. 이제 잘려도 끌어서 볼 수 있다.

**3단계로 넘어가기 전에 판단할 것.** 재작화는 되돌리기 비싼 유일한 구간이다.
지금 봐야 하는 건 화질이 아니라 **맵 크기와 건물 밀도**다 — `shots/city.png`.

---

## 왜 이번에 맵을 8×8 로 못 늘렸나 (계측 기록)

5번 요청을 받고 블록을 7×7 → 8×8 로 늘려 봤다가 되돌렸다.

| | 월드 높이 | PX=2 에 필요한 캔버스 높이 |
|---|---|---|
| 7×7 (이전) | 404 | 808px |
| **8×8** | **432** | **864px** |
| 7×7 (현재, 여백 축소) | **384** | **768px** |

1080p 브라우저의 캔버스 높이가 약 865px 이다. 8×8 은 **여유가 1px** 이라
북마크 바 하나만 켜도 `PX` 가 1로 떨어져 맵이 절반이 된다. 실패 모드가 너무 급하다.

그래서 7×7 을 유지하되 여백을 깎아 384 로 줄였다 (`PX=2` 요구가 808 → 768px 로
내려가 여유가 늘었고, 1440p 에서는 `PX=3` 도 닿는다). 대신 **회사를 23 → 38 로
늘리고 도시 밖을 교외 주택가로 채워** 밀도로 해결했다.

계측: 순수 초록(빈 잔디) 화면 점유율 **44.0% → 14.2%**. (`npm run shot` 후 픽셀 집계)

**맵을 진짜로 넓히려면 해상도 정책부터 바꿔야 한다.** 그게 위의 B 안이다.

---

## 되돌릴 조건

- DPR 1 모니터에서 도트가 1px 이 되며 아기자기함이 죽는다 → A(64×32) 로 후퇴
- 재작화 없이 좌표만 키운 그림이 오히려 조잡해 보인다 → 현행 유지
- 회전 후에도 상호판 클릭이 여전히 필요하다 → 그건 정상이다. 회전은 가림을
  줄이지 클릭을 대체하지 않는다. 둘 다 남긴다

---

## 지금도 유효한 규칙

`RENDER.md` 의 다음은 해상도가 바뀌어도 그대로다.

- 폰트는 **10 / 12 / 15 세 단만** — 글자는 월드 층이 아니라 텍스트 층에 그린다.
  타일이 커져도 글자는 안 커진다. 오히려 **상대적으로 작아져 라벨 밀도가 는다**
- 배율은 **정수만**
- 사선은 `fill()` 이 아니라 `fillRect` 계단으로
- 최상위 이름은 전부 고유 (번들러 제약)
