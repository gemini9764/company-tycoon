# 컴퍼니 타이쿤 — 작업 인수인계

새 세션에서 이 문서만 읽고 바로 이어서 작업할 수 있도록 정리한 컨텍스트.

---

## 0. 한 줄 요약

기획서(`NHN_사전과제.md`)를 기반으로 만든 **웹 경영 시뮬레이션 프로토타입**.
구멍가게에서 시작해 M&A로 계열사를 늘려 글로벌 그룹까지 가는 게임.
바닐라 JS + 캔버스, 서버 없음, 저장은 로컬. 현재 **플레이 가능한 상태로 완주까지 검증 완료**.

그래픽은 **캔버스 2D 유지**로 결정했다 (three.js 이식은 철회).
**회전(1단계) · 해상도 스케일(2단계) · 밀도와 색과 캐릭터(2.5단계)까지 완료.**
남은 건 **3단계 건물 재작화**뿐이다 — `GRAPHICS.md`.

**난수는 시드 고정이다.** 같은 시드는 같은 판을 만든다 (`core/rng.js`).
밸런스를 잴 때 이 전제가 없으면 회차마다 ±8% 가 흔들려 비교가 불가능하다.

**성공에는 엔딩이 없다.** 글로벌그룹·시총 1위는 마일스톤이고 게임은 계속된다.
끝나는 건 파산·수사·무속 세 가지 실패뿐이다.

---

## 1. 작업자 프로필 / 프로젝트 규칙

- 웹 게임 개발 10년차 시니어, 주력 언어 JavaScript
- 경영 전략 시뮬 개발 경력 있음. 카이로 소프트 `게임 개발 스토리`, `월간 아이돌` 같은 캐주얼 게임 경험
- 게임 시스템 + UI/UX 양쪽 개발 경험

프로젝트 지침(`CLAUDE.md`)에서 요구하는 것:

- 전제는 명시할 것. 불확실하면 물을 것. 해석이 갈리면 선택지를 제시할 것
- 단순함 우선, 최소 코드
- 내 변경이 만든 고아 코드는 제거할 것

응대 방식: 사족·아부 없이 결론부터. 문제를 발견하면 추측 대신 계측해서 원인을 특정.

---

## 2. 요구사항 (사용자가 명시한 것)

1. 변경된 기획서 기반으로 프로토타입 재작성
2. **서버 없이 로컬에 저장 데이터**를 두는 방식
3. UI는 `월간 아이돌` + 카이로 소프트 `게임 개발 스토리` 참고
4. **모바일 앱 형태가 아니라 브라우저 화면 전체를 활용**하는 데스크톱 레이아웃
5. 단일 HTML이 아니라 **디렉터리 구조로 코드 관리**
6. 탑뷰 → 쿼터뷰 전환에 맞춰 **그래픽을 3D 느낌으로. 단 픽셀 톤은 유지**
   → three.js 검토 후 **철회**. **카메라 회전 + 해상도 상향**으로 방향 확정 (`GRAPHICS.md`)
7. 쿼터뷰에서 **건물이 겹쳐 클릭이 불편한 문제 보완** → §클릭 판정
8. **타이틀 화면** — 새로 시작 / 이어하기
9. 인게임 **설정 메뉴 — 사운드 조절만**
10. **상장 기준 = 중견기업 이상 무조건.** 플레이어 회사는 상장하지 않는다
11. **회사 수 증가 + 맵이 비어 보이는 문제** → 회사 23 → 38 → **80**, 도시 밖을 교외로
12. **1일 = 15초**, 이벤트 종류·빈도 증가
13. **카메라 90° 회전 4방향** (`Q`/`E`)
14. **해상도 B안** — 타일 48×24 · `PX=1` **(구현 완료)**
17. **건물 간격을 벌릴 것** → 블록 간격 3 → **4타일**(도로 두 줄), 블록 **8×8** · 회사 **44**
18. **색감을 파스텔로** → 맵 색 91종 + 업종색 10종 교체 (`RENDER.md` §7)
19. **맵 드래그 이동 + 휠 확대** → 정수 배율 1~3x (`RENDER.md` §카메라)
20. **글로벌 도달 일수를 기존으로 되돌릴 것** → 886일 (§6)
21. **시드 고정** → `core/rng.js`. 게임 스트림과 연출 스트림을 분리
22. **NPC 캐릭터 재작화** → 22×44 (`RENDER.md` §8)
15. **무한 진행** — 글로벌그룹 이후 시총 순위, 1위 이후 시총 기록. 성공 엔딩 폐지
16. **글로벌그룹 도달 시간 단축** — 1,313일 → 990일

---

## 3. 산출물 위치

| 파일 | 내용 |
|---|---|
| `company-tycoon-src.zip` | 소스 트리 전체 (모듈 구조, 빌드/테스트 도구 포함) |
| `company-tycoon.html` | 빌드 산출물. 의존성 없이 더블클릭으로 실행 (215KB) |
| `GRAPHICS.md` | 그래픽 방향 — 회전 설계 · 해상도 선택지 · 계측 기록 |
| `proto/iso3d.html` | three.js 프로토타입. **철회했으나 참고용으로 남김** (온라인 필요) |

**터미널 없이 쓸 수 있다.** Node.js만 있으면 되고 `npm install` 은 필요 없다.

| 파일 | 하는 일 |
|---|---|
| `개발서버-실행.command` / `.bat` | 개발 서버를 띄우고 브라우저를 연다 |
| `배포본-만들기.command` / `.bat` | `src/` 를 묶어 `dist/company-tycoon.html` 생성 |

실행기 본체는 `tools/launch.mjs` 다. `.bat` 은 **순수 ASCII 여야 한다** —
cmd.exe 가 배치 파일을 실행 중 다시 읽기 때문에 코드페이지 전환 지점 뒤에 한글이 있으면
파싱이 어긋난다. 한글 출력은 전부 `launch.mjs` 가 맡는다.

터미널을 쓴다면:

```bash
npm run dev      # localhost:5173 — ES 모듈 그대로, 수정 즉시 반영
npm run build    # dist/company-tycoon.html 생성
npm run smoke    # UI 상호작용 자동 검증 (24 케이스, jsdom 필요)
npm run shot     # 렌더 스냅샷 shots/*.png (@napi-rs/canvas 필요)
npm run sim      # 밸런스 시뮬레이션 (jsdom 필요)
```

`npm install` 은 **테스트 도구(jsdom)에만** 필요하다. 빌드는 자체 번들러(`tools/bundle.mjs`)를
쓰므로 외부 의존이 없다.

> ES 모듈은 `file://`에서 CORS로 막히므로 `index.html`을 브라우저로 직접 열면 안 된다.
> 개발은 개발 서버, 공유는 `dist/company-tycoon.html`.

---

## 4. 코드 구조

의존 방향은 `core → systems → ui/render → main` 한 방향.
`systems`가 모달을 띄울 때만 `ui/modal.js`를 부르는데, 함수 선언 호이스팅 덕에 런타임 문제 없음.

```
src/
├─ main.js              부팅과 인트로. 엔트리
├─ debug.js             window.game QA 핸들 (게임 로직은 여기 의존 안 함)
├─ core/                도메인 기반. UI·렌더를 모른다
│  ├─ balance.js        BAL — 밸런스 상수 전부 (튜닝은 여기서만)
│  ├─ rng.js            결정론적 난수. 게임 스트림 / 연출 스트림 분리
│  ├─ data.js           TIERS, SECTORS, DIFFS, CREDITS, SHAMAN_TIERS, RUMOR_GRADES, 이름풀
│  ├─ derive.js         순수 파생 함수 (teamOf, sumStat, debtTotal, netWorth)
│  ├─ state.js          S — 단일 상태. newState / setS / seedMarket / makeStaff / makeShaman
│  ├─ storage.js        저장 어댑터 (window.storage → localStorage → 메모리)
│  ├─ clock.js          배속·일시정지 (pause / resume / clearPause)
│  ├─ loop.js           tickDay(하루 진행), frameLoop(렌더 프레임)
│  └─ util.js           won(금액 포맷), rnd/rint/pick/clamp/chance, $, esc
├─ systems/             상태를 바꾸는 규칙. 렌더를 직접 안 건드림
│  ├─ company.js        recalcCap(시총·순위), 신용등급, checkTier, capCeiling, teamPower
│  ├─ economy.js        매출/비용, tickMonth(월 정산), pmi(통합 진척), 그룹 운영 효율
│  ├─ mna.js            startNego / tickNego / negoEvent / finishNego / completeAcq
│  ├─ bank.js           loanLimit / takeLoan / repayLoan / seizeSub(담보 압류) / checkBankrupt
│  ├─ stock.js          주가 변동, 매수·매도
│  ├─ shaman.js         살굿·축원굿, 발각 판정
│  ├─ rumor.js          찌라시 획득·사용
│  ├─ events.js         국가·국세청·경쟁사·무속 이벤트 (bill()로 비용 상한)
│  └─ ending.js         엔딩 판정 (global / bankrupt / probe / occult)
├─ render/              쿼터뷰 도트 렌더. 외부 에셋 0 — 규칙은 `RENDER.md`
│  ├─ iso.js            2:1 아이소메트릭 투영과 도형(rhomb/prism/faces/isoWin), 지면 캐시
│  ├─ canvas.js         정수 배율 파이프라인, 클릭 판정, 툴팁, drawText, 캐릭터 스프라이트
│  ├─ city.js           도시 맵 (M&A) — 6×6 블록, 업종별 건물 실루엣, 공원·논밭 지형
│  └─ store.js          매장 + 사무실 + 사장실 (경영) — 타일 격자 위 손님 경로 탐색
├─ ui/                  DOM. 상태를 읽기만 하고 조작은 systems 호출
│  ├─ hud.js            상단 지표·배속·저장·도움말
│  ├─ icons.js          12×12 도트 아이콘 격자 + 캔버스로 굽는 iconEl
│  ├─ dock.js           상단 바(모드·회전·공통 버튼) + 맵 우상단 모드 전용 버튼
│  ├─ shopPanel.js      '매장' 창 (사옥 전용) — 발주 / 시설 / 운영 서브탭
│  ├─ desk.js           사장실 결재 — 하루 1회, 리스크 수치를 내리는 유일한 수단
│  ├─ panelLeft.js      '회사' 창 내용 (현황 / 일일 손익 / M&A / 그룹 운영 / 리스크 / 계열사)
│  ├─ tabs.js           열린 창 id(TAB)를 쥐고 창 내용을 그린다 — 최대 모듈
│  ├─ bankPanel.js      은행 탭 + 인수금융 창(openAcqLoan)
│  ├─ companyPopup.js   맵에서 회사 클릭 시 상세
│  ├─ modal.js          모달 스택
│  ├─ toast.js          토스트 / 뉴스 티커 / 알림함
│  ├─ help.js           도움말 본문
│  └─ index.js          renderAll
└─ styles/main.css      디자인 토큰 + 레이아웃

proto/
└─ iso3d.html          three.js 프로토타입 — 스택 판단용. 빌드에 안 들어간다

tools/
├─ bundle.mjs           무의존 ES 모듈 번들러 (한 스코프로 이어 붙임 → 최상위 이름 고유해야 함)
├─ build.mjs            단일 HTML 빌드
├─ launch.mjs           실행기 본체 (.bat/.command 가 호출)
├─ serve.mjs            개발용 정적 서버 (무의존, node만)
├─ harness.mjs          jsdom 테스트 공통 셋업 (dist 산출물을 띄움)
├─ smoke.mjs            UI 상호작용 검증
├─ shot.mjs             렌더 스냅샷 (shots/*.png). 캔버스를 실제 픽셀로 그린다
└─ sim.mjs              밸런스 시뮬레이션 (페이지 안에서 통째로 실행)
```

---

## 5. 게임 설계 요약

### 핵심 루프
경영으로 자금 확보 → M&A 대상 물색 → 협상단 파견 → 흡수·통합 → 등급 상승 → 더 큰 타겟

### 타이틀

부팅하면 무조건 타이틀부터 뜬다(`ui/title.js`). 메뉴는 **새로 시작 / 이어하기 / 설정** 셋.

- 세이브가 없으면 '이어하기'를 **숨기지 않고 비활성**으로 둔다 — 숨기면 "저장이 안 되나?"로 읽힌다
- 세이브가 있는데 '새로 시작'을 누르면 덮어쓰기 확인을 받는다
- 배경 그림은 `render/iso.js` 의 도형을 그대로 써서 그린다. 타이틀에서부터 쿼터뷰를 보여 주고,
  '외부 에셋 0' 도 지킨다. 그림 확인은 `npm run shot` → `shots/title.png`
- 렌더 루프는 **게임이 시작된 뒤에** 돈다. 타이틀 시점엔 `S` 가 없어서 `draw()` 가 볼 상태가 없다

### 설정

독의 **설정** 버튼(톱니) → 모달. **사운드만** 있다 — 음소거 / 전체 / 배경음 / 효과음.

- 사운드는 `core/audio.js` 가 WebAudio 로 **직접 합성**한다. 음원 파일을 두면 '외부 에셋 0'이 깨진다
- AudioContext 는 브라우저 정책상 **첫 사용자 제스처**에서만 열린다. `unlockAudio()` 를
  최초 클릭/키 입력에 물려 뒀고, 그전 호출은 조용히 무시된다
- 설정은 세이브와 **다른 키**(`company-tycoon-prefs`)에 저장한다. 새 게임을 해도 유지돼야 하고,
  세이브 포맷이 바뀌어 구버전 세이브가 폐기될 때 같이 날아가면 안 된다
- jsdom 에는 AudioContext 가 없다. 전부 no-op 으로 떨어져 스모크가 사운드 때문에 터지지 않는다

### 클릭 판정 — 쿼터뷰에서 건물이 겹치는 문제

원인이 둘이었다.

| 원인 | 증상 |
|---|---|
| 판정이 **사각형 근사** | 건물 마름모 바깥 모서리(빈 하늘)까지 앞 건물이 먹는다 |
| 앞 건물이 뒤를 **완전히 덮음** | 판정을 고쳐도 애초에 클릭할 픽셀이 없다 |

두 갈래로 풀었다 (`render/city.js`).

1. **실루엣 판정** (`isoHit`) — 마름모 기둥의 실제 실루엣으로 본다.
   그 x 에서의 세로 반경 `(1 - |dx|/rx) * ry` 만큼만 인정하면 모서리 오판이 사라진다
2. **상호판을 클릭 타깃으로** (`plateHit`) — 상호판은 `flushPlates` 가 건물을 전부 그린 뒤
   맨 위에 얹으므로 **절대 가려지지 않는다.** 이걸 그대로 히트박스로 쓰면 뒤에 완전히 숨은
   회사도 항상 집힌다. `plateBoxes` 가 매 프레임 갱신되고, 판정은 **역순**(나중에 그린 게 위)
3. **가림 투시** (`focusOf`) — 호버 대상보다 앞에 있으면서 겹치는 것만 `globalAlpha 0.24`.
   상호판으로 숨은 회사를 집을 수 있게 되면서 "집었는데 안 보인다"가 생겨 필요해졌다

> 근본 해결(카메라 회전)은 3D 이식과 묶여 있다. `GRAPHICS.md` 참고.

### 화면 — 월간 아이돌 계열 UI

상시 패널이 없다. 맵이 화면을 꽉 채우고, 정보는 아이콘 버튼으로 연다.
버튼은 **모드와 무관한 것**과 **그 모드에서만 쓰는 것**을 자리로 갈라 놓았다.

```
┌──────────────── HUD (자금·시총·순위·신용·부채·배속·저장) ─────────────────────┐
├──────────────────────────────────────────────────────────────────────────────┤
│ [도시|사옥] [◀▶]                          찌라시 알림 설정   ← 늘 같은 자리    │
├──── 속보 (클릭하면 지난 소식이 아래로, 캔버스 위에 겹쳐 펼쳐진다) ─────────────┤
│                                              회사 주식 은행  ← 도시일 때       │
│                     캔버스                   매장 직원 무당  ← 사옥일 때       │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘
```

- **왜 갈랐나** — 모드를 나눠 놓고 버튼을 전부 띄우면 모드를 나눈 의미가 없다. 도시는
  M&A 축(회사·주식·은행), 사옥은 경영 축(매장·직원·무당)만 띄운다. 찌라시·알림·설정은
  어느 모드에서나 쓰므로 상단 바에 고정한다.
- 모드를 바꾸면 **그 모드에서 못 쓰는 창은 자동으로 닫힌다** (`ui/dock.js:inMode`).
  안 그러면 은행 창을 열어 둔 채 사옥으로 넘어가 버튼 없는 창이 떠 있게 된다.

- 버튼을 누르면 가운데에 **창 하나**가 뜬다. 같은 버튼을 다시 누르거나 `Esc`·바깥 클릭으로 닫힌다.
- 열린 창은 `ui/tabs.js` 의 `TAB` 하나가 쥔다. `null` 이면 닫힘, `'co'` 면 회사 창, 나머지는 탭 id.
  내용을 그리는 쪽은 **자기 창이 열렸을 때만 일하도록 스스로 막는다** — `renderLeft` 는 `TAB !== 'co'`,
  `renderRight` 는 `TABS.includes(TAB)` 로 걸러낸다. 안 그러면 매일 갱신할 때 없는 DOM을 찾는다.
- 아이콘은 `ui/icons.js` 의 12×12 격자 문자열이다. 이모지는 OS마다 모양이 달라지고 이미지는
  '외부 에셋 0' 원칙에 걸려서 직접 찍는다. `npm run shot` 이 `shots/icons.png` 로 뽑아 준다.
- 주식 창은 안에 `시장 전체 / 관심` 서브탭이 있다. 관심 등록이 시장 목록을 덮어쓰지 않는다.
- 뉴스는 `S.log`(60건)를 그대로 펼친다. 펼침 목록은 **캔버스 위로 띄운다** — 무대 높이를
  바꾸면 맵 배율이 한 단계 떨어질 수 있다.
- 알림 배지는 `알림` 버튼에만 있다. HUD 의 종 버튼은 중복이라 뺐다.
- **설정** 모달에는 사운드 아래에 `메인 타이틀로` 가 있다. 게임 중일 때만 뜬다
  (`ui/title.js:canExit`). 저장 이후 진행이 날아가므로 저장 여부를 한 번 묻는다.
  실제 복귀 동작은 `main.js:backToTitle` 이 쥐고 `setExitHandler` 로 등록한다 —
  설정 모듈이 main 을 직접 부르면 모듈 고리가 지저분해진다.
- **잠긴 버튼**(무당)은 `disabled` 로 두지 않는다. 브라우저가 `disabled` 요소에는 마우스
  이벤트를 안 줘서 `title` 툴팁이 안 뜨기 때문. 대신 `.locked` 로 아이콘을 죽이고
  모서리에 자물쇠 배지를 붙이며, 누르면 해금 조건을 토스트로 알린다.
  ('잠김' 글자를 라벨 위에 겹쳐 놓던 이전 방식은 둘 다 안 읽혔다)
- 상시로 봐야 하는 협상 진행도는 창이 아니라 **맵**에 있다 — 대상 건물 위 진행률 라벨,
  그리고 경영 모드 사무실의 화이트보드.
- 두 모드 이름은 **도시**(M&A) / **사옥**(경영). 내부 id 는 `city` / `store` 그대로다
  - 두 모드 모두 **쿼터뷰(2:1 아이소메트릭)**. 카이로 소프트 레퍼런스 기준
  - 도시 — 32×32 타일, 블록 **8×8=64칸** 중 45칸이 회사(자사 포함). 나머지 19칸은 렌더가 채운다.
    `blockPitch`(4) 주기로 2칸이 블록, 2칸이 도로다 — **블록 사이 도로가 두 줄**이라
    건물이 서로 붙지 않는다. pitch 가 4의 배수라 회전(gx → W-1-gx)에도 판정이 안 깨진다.
    도심(가운데)일수록 오피스·아파트, 바깥으로 갈수록 상가 → 공원 → 논밭·주택으로 바뀌어
    도시-외곽 그라데이션이 생긴다. **채움 건물에는 상호판이 없어** 클릭할 회사와 구분된다.
    업종이 건물 실루엣으로 갈리고(고층/연구소/공장/전광판/상가), 옥상에는 물탱크·실외기·
    광고탑·헬리패드가 해시로 얹힌다. 상호판은 정렬 뒤 따로 얹는다.
    도시 바깥은 **맵 경계로부터의 거리로 세 단**을 나눈다(`drawOutskirts`) —
    0~5타일 교외 주택가(도로망+집·상가), 6~13타일 마을, 그 밖은 논밭·숲.
    전부 논밭이던 시절엔 화면의 44%가 빈 잔디였다. 지금은 그 절반 이하다.
    블록을 49 → 100 으로 넓히면서도 밀도는 유지했다 (`npm run shot` 픽셀 집계 전후 동일).
    전부 지면 레이어에 굽기 때문에 **프레임 비용은 0** 이다
  - 경영 모드 — 좌 매장(진열대·냉장고·냉동고·평대·계산대), 우 사무실 + 사장실(고정).
    손님은 타일 격자에서 길을 찾아 집기를 우회한다. 직원은 책상 뒤에 앉아 얼굴이 보이고,
    책상은 고용 인원만큼 늘어난다(최대 13석). 협상단 편성 직원은 자리를 비우고 `출장` 표시
  - 하단 스트립: 일매출 · 순익 · 인지도 · 손님 수 · 직원 수
- **조작은 전부 키와 마우스다.** 독에 회전·확대 버튼을 두면 상시 자리를 차지하면서
  정작 드래그·휠은 안 알려 주게 되어 걷어냈다. 안내는 **설정 모달의 '조작' 란**에 모았다
  (`ui/settings.js` 의 `KEYS`). 키 캡은 밝은 바탕이어야 한다 — 모달이 밝아서 어두운
  캡에 어두운 글자를 얹으면 안 읽힌다
- 단축키: 드래그 이동 / 휠·`+`·`−` 확대 / `Q`·`E` 회전 / `Tab` 모드 전환 /
  `Space` 일시정지 / `Esc` 창 닫기

### 시간
1 tick = 1일, 30일 = 1개월 정산(급여·이자·보험료). **기본 1일 = 15,000ms (15초).**

1초에서 15초로 늦추면서 두 가지가 따라왔다.

- **배속에 8x 추가** (`BAL.speeds`). 완주가 1,700일 안팎이라 1x 로는 약 7시간이다.
  8x 면 약 52분. **1x 는 초반 체감용이고 실플레이는 배속 전제다** — 이 전제가 싫으면
  등급 목표를 낮춰 완주 일수 자체를 줄여야 한다 (§10)
- **이벤트 빈도** — 쿨다운 12 → 4일, 확률 0.055 → 0.22. 기대 간격 30일 → 8.5일(약 2분).
  종류도 7 → 16종으로 늘렸다 (`EV_MARKET` 5종 신설, 국가·국세청·무속 각 1종 추가)
- **`BAL.eventCostScale`** — 빈도를 3.5배 올렸으니 건당 비용을 0.30 배로 낮춰야
  시간당 지출이 유지된다. **이 계수를 안 넣었을 때 무차입 전략이 동네슈퍼에서
  파산했다** (sim 계측). 빈도를 바꾸면 반드시 이 값도 같이 볼 것

### 등급 (7단계)

| 등급 | 승급 목표 | 해금 |
|---|---|---|
| 구멍가게 | 순자산 3,000만 | |
| 동네슈퍼 | 순자산 1억 + 첫 M&A | |
| 스타트업 | 계열사 3개 | 무당 시스템 |
| 중소기업 | 순자산 500억 + 계열사 5개 | 국세청 이벤트 |
| 중견기업 | 난이도 '상' 이상 인수 | 국가 이벤트 |
| 대기업 | 순자산 1조 + 시총 100위 | |
| 글로벌그룹 | 시총 1위 → 정상 엔딩 | |

인수 규모 상한: `[3억, 20억, 200억, 2000억, 1.5조, 6조, 무제한]` (`systems/company.js:capCeiling`)

### 엔딩 4종
- `global` — 시총 1위 (정상)
- `bankrupt` — 연체 3회 또는 자본 마이너스 4개월
- `probe` — 수사 게이지 100 (로비 남용)
- `occult` — 미신지수 100 (굿 남용)

### 진행 구조 — 끝이 없다

`ending.js` 는 이제 **실패만** 처리한다.

- **마일스톤** (`MILESTONES`) — 글로벌그룹 등극 → 시총 50위 → 10위 → 3위 → **1위**.
  모달을 띄우되 **게임을 멈추지 않는다.** 여기서 일시정지를 걸면 무한 진행이 매번 끊긴다
- **1위 이후** — 순위는 1이 천장이라 목표가 사라진다. `CAP_STEPS`(50조 → 100조 →
  300조 …) 로 넘어가고 그 뒤로는 3배씩 **무한히** 이어진다
- **엔딩** — `bankrupt` / `probe` / `occult` 셋뿐. `global` 은 삭제했다
- 화면의 '다음 목표' 는 `goalText(s)` 하나가 등급 목표 → 마일스톤 → 시총 단계를
  이어서 내놓는다. 여기 한 곳만 보면 된다

### 카메라 회전

`view`(0~3)를 `Q`/`E` 또는 독의 ◀ ▶ 로 돌린다. **타일 좌표만 돌리고 투영식은
손대지 않는다** — 자세한 규칙은 `RENDER.md` §카메라 회전.
사옥(`store`) 모드는 회전하지 않는다. 고정 배치가 읽기 쉽다.

**전환은 암전이 아니라 실제 회전이다.** 타일 좌표를 실수로 돌리면 중간 각도가
나온다 — 건물이 호를 그리며 옮겨 가고, 지면은 구워 둔 비트맵 두 장에 같은 회전의
화면 좌표 변환을 걸어 교차 페이드한다. 자세한 규칙은 `RENDER.md` §카메라 회전.

### 카메라 — 이동과 확대

도시는 **드래그로 끌고 휠로 확대**한다. 배율은 **정수 1·2·3x 만** — 분수면
도트가 울렁거린다. 확대는 포인터 밑 좌표를 고정하고, 4 CSS 픽셀을 넘게 끌면
뒤따르는 `click` 을 버려 맵을 끌 때 회사 창이 안 열린다. 사옥은 카메라가 없다.

> `fitCanvas` 는 **백킹스토어만** 맞춘다. CSS 크기를 JS 가 px 로 덮으면
> 레이아웃이 잡히기 전에 잰 값이 굳어 캔버스가 래퍼보다 커진다 —
> "사옥 갔다 오면 맵이 줄어든다"의 정체였다. 크기 변화는 `ResizeObserver` 로 잡는다.

### 해상도 (2단계 완료)

타일이 **48×24**, 도시 1x 배율이 **`PX=1`**, 사옥이 **`PX=2`** 다.
화면상 크기는 이전과 거의 같은데 **도트 수가 2.25배**가 됐다.

**지금 그림이 눈에 띄게 좋아 보이지 않는 게 정상이다.** 같은 그림을 굵은 픽셀로
그린 상태이고, 이득은 3단계 재작화에서만 나온다. 지금 봐야 하는 건 **맵 크기와
건물 밀도**다. 계측과 "다음에 또 걸릴 자리"는 `GRAPHICS.md` §2단계에 정리했다.

주의 하나 — **글자를 담는 상자(상호판·라벨·화이트보드)는 월드와 같이 1.5배 하면
안 된다.** 글자는 텍스트 층이라 안 커지는데 `PX` 는 오히려 내려가서, 상자만
헐거워진다. 폰트 크기에서 뽑을 것.

### 상장 · 회사 배치

- **상장 기준은 규모다.** `capTier(cap) >= LIST_TIER(4, 중견기업)` 이면 무조건 상장.
  확률 요소를 없앴다. NPC 는 목표 달성 개념이 없으므로 시총 구간(`TIER_CAP`)으로 등급을 환산한다
- **플레이어 회사는 상장하지 않는다.** 자사 주식 거래는 없다 (기획서 3-3 주식시장 항목)
- 계열사는 인수 순간 `owned` 가 되어 주식 목록에서 빠진다
- 회사 수는 `BAL.npcCount` (44). 블록은 `BAL.cityBlocks`² (8×8 = 64). 간격은 `BAL.blockPitch` (4)

### 사옥(경영) 콘텐츠

사옥은 한동안 **디오라마**였다. 손님이 걸어다니고 매출이 오르지만 누를 것이 없어
바로 도시로 돌아가게 됐고, 버튼을 모드별로 가르면서 광고·점검마저 도시 전용 '회사'
창에 갇혀 사옥에서 조작 가능한 것이 0이 됐다. 다음 셋으로 메웠다.

| 축 | 내용 | 어디서 |
|---|---|---|
| **재고** | 매일 소모되고, 바닥나면 매출이 `invFloor`(35%)까지 빠진다. 발주는 50% 이상 채우면 8% 할인. 자동 발주는 단가 +12% | 매장 창 → 발주 |
| **시설** | 진열대·계산대·냉장·사무실 4종 × 3단계. 매출·손님·재고 유지일·관리 인력에 영구 보너스 **+ 사옥 그림이 실제로 변한다** | 매장 창 → 시설 |
| **사장실 결재** | 하루 1회. 기부(신용↑·미신지수↓) / 언론 대응(수사↓) / 사내 복지 | 사옥 캔버스에서 **사장 책상 클릭** |

- **사옥 캔버스에 클릭 판정이 생겼다.** 그전까지 `hitLot` 은 도시에서만 동작해
  사옥 화면은 아무 데나 눌러도 반응이 없었다. 지금은 사장 책상 → 결재,
  계산대 → 매장 창. (`render/store.js:storeHit`)
- **매장 이벤트 6종** — 위생 점검·절도·인플루언서·단골 납품·진상 손님·정전.
  **사옥에 있을 때 선택지가 더 좋다** (`atShop`). 자리를 비우면 '보고만 받는' 한 줄만
  남아 결과가 나쁘다 — 모드를 오갈 이유를 만드는 장치다.
- 사장실 결재는 **미신지수·수사 압박을 내리는 유일한 수단**이다. 무속 루트가
  반드시 스캔들로 끝나던 문제(남은 과제 4번)를 여기서 푼다.
- 재고는 진열대 그림에 그대로 반영된다 — 재고가 빌수록 상품이 사라진다.

#### 시설 증설이 그림에 반영되는 방식

숫자만 오르면 성장이 안 보인다. 레벨마다 집기가 실제로 늘어난다.

| 시설 | 그림 |
|---|---|
| 진열대 | 냉장고 아래 줄에 진열대가 최대 3칸 늘어난다 (8 → 11칸) |
| 계산대 | 계산대 한 대와 **점원 한 명**이 늘고, Lv2 부터 셀프 계산 단말이 붙는다 |
| 냉장 | 냉장고가 최대 3대 늘어난다 (3 → 6대) |
| 사무실 | 복합기·캐비닛·화분이 늘고 카펫 색이 바뀐다 |
| (총합) | 매장 바닥이 **나무 → 타일(4단계) → 대리석(8단계)** 로 바뀐다 |

**늘어난 집기는 손님이 못 지나간다.** 그래서 `SHOP_BLOCK`(고정 Set)을 직접 쓰지 않고
`blockedAt()` 을 거치며, 손님 목적지도 `shelvesNow()` 로 증설분을 포함한다.
자리는 gy=2·4·5·7·9 가로 통로와 gx=0·4·6 세로 통로를 막지 않는 곳으로만 골랐다 —
**스모크가 시설 최대 상태에서 모든 진열대에 경로가 닿는지 검사한다.** 집기를 추가할
때 이 검사를 통과하는지 반드시 확인할 것.

바닥은 오프스크린에 구워 두므로 캐시 키(`facilKey()`)에 레벨을 넣었다.
안 넣으면 증설해도 바닥이 안 바뀐다.

사장 책상 위에는 **오늘 결재가 남았을 때만** 금빛 `결재` 배지가 깜빡인다.

### 주요 시스템
- **M&A**: 진행도는 일정 속도로 차오르고, 성공도는 협상단 협상력 × 대상 난이도로 상승. 진행도 100%에서 **성공도 = 인수 확률**. 성사 후 인수가 지불해야 완료. 협상 중 25/50/75%에 선택지 이벤트
- **통합 기간(PMI)**: 인수 후 75일간 계열사 수익이 12%→100%로 상승. 관리비는 첫날부터 전액. **차입 인수의 현금흐름 리스크를 만드는 장치**
- **그룹 운영 효율**: ×0.55~1.6 배수가 계열사 수익 전체에 곱해짐. 업종 집중(+), 통합 중(−), 관리 인력 부족(−), 사업부 점검(+)
- **관리 인력**: 협상단에 안 넣은 직원이 계열사를 관리. 계열사 3개당 1명 필요. **협상단 vs 관리 트레이드오프**
- **은행**: 운영자금(한도=시총×0.14) / 인수금융(한도=인수가×0.8, 담보=인수 기업). 15개월 원리금 균등. **인수금융 상환 실패 시 계열사 압류**
- **무당**: 3등급. 살굿(대상 난이도↓·주가↓) / 축원굿(성공도↑). 실패 시 신벌, 발각 시 신용↓·미신지수↑
- **찌라시**: F~S급. 정보력에 비례해 획득. 인수가 프리미엄↓, C급 이상은 난이도 1단계↓
- **계열사 특성**: 업종 퍼크(계열사 1개당 누적, 5스택 상한) + 개별 태그 10종 + 사업부(같은 업종 3개 → 그 업종 퍼크 2배, 5개 사업부 → 지주회사 체제 관리 인력 -25%)
- **계열사 개별 운영**: 매각(시총×0.85) / 투자(시총 +12% · 부실·노후 태그 해소) / 재편(업종 전환 45일, 기간 중 수익 0)

---

## 6. 밸런스 현황

수치는 전부 `src/core/balance.js`의 `BAL`. 등급 목표는 `core/data.js:TIERS`, 규모 상한은 `systems/company.js:capCeiling`.

주요 상수:
```
startCash 500만 · dayMs 15000 · retailBase 90만/일
tierRetailMul [1, 3.2, 6, 14, 40, 120, 300]
subYield 0.068 (회수기간 ≈ 15일) · pmiDays 40 · pmiFloor 0.12
synMin 0.55 / synMax 1.6 · subsPerManager 3 · auditGain 0.08 / auditCap 0.24
negoProgressPerDay 6.8 (협상 ≈ 15일) · premiumBase 0.35
cityBlocks 8 · blockPitch 4 · npcCount 44
baseRate 3.5% · loanTermMonths 15 · opLoanRatio 0.14 · acqLoanRatio 0.80
rankTop 45조 / rankFloor 1억 / rankCurve 2.4 → 100위 ≈ 3.5조, 1위 ≈ 45조
gutMistrust 8 · mistrustPenaltyAt 30 · probeDeath 100 · mistrustDeath 100
subSellRate 0.85 · subSellPmiCut 0.60 · subInvestRate 0.25 / Gain 0.12
subRestructRate 0.40 · subRestructDays 45 · hiddenDebtRate 0.18
PERK_CAP 5 · DIV_AT 3 · HOLDING_AT 5  (core/tags.js)
```

### 기준선 — **시드 고정** (`node tools/sim.mjs --runs=3`)

난수가 시드에 묶여 있으므로 **이 표는 그대로 재현된다.** 시드는 `1001 / 2002 / 3003`
이고 `tools/sim.mjs` 의 `SEEDS` 에 박혀 있다. 바꾸면 아래와 비교가 안 된다.

계측 기준은 **시총 1위 도달일**이다 (성공 엔딩이 없으므로).

| 전략 | 1001 | 2002 | 3003 | 평균 | 결과 |
|---|---|---|---|---|---|
| 무차입 | 837 | 878 | 776 | **830** | 계속 진행 · 계열사 25~31 |
| 중간 레버리지 | 724 | 855 | 599 | 726 | 계속 진행 · **가장 빠름** |
| 극단 차입 + 로비 | 610 | 607 | 680 | 632 | 계속 진행 · 수사 0~40 |
| 무속 (결재 없음) | 586 | 586 | 478 | 550 | **3/3 무속 스캔들** |

**이 표는 계열사 특성(단계 1) 도입 후 다시 잰 것이다.** 도입 전 기준선은
무차입 886 / 레버리지 742 / 극단차입 703 / 무속 544 였다.

> **태그 추첨은 매물 생성이 전부 끝난 뒤 별도 패스에서 돈다** (`state.js:seedMarket`).
> 생성 루프 안에서 뽑으면 난수열이 밀려 같은 시드인데도 매물 배치·시총이 통째로
> 달라진다. 처음에 루프 안에서 뽑았더니 레버리지가 742 → 641 로 튀어 회귀처럼
> 보였는데, 패스를 분리하자 726 으로 돌아왔다. **설정 단계에서 난수를 새로 소비할
> 때는 소비 지점을 항상 맨 뒤로 밀 것.**

**무속 루트는 결재를 쓰느냐로 생사가 갈린다.** 결재 없이는 3시드 전부 550일
근처에서 죽고, 기부·언론 대응을 챙기면 완주한다. 대신 그 비용 때문에 무차입보다
**100일 느리다** — 리스크를 돈으로 갚는 구조가 의도대로 작동한다.
(`node tools/sim.mjs --noref --strats=occult --desk` 로 재현)

### 등급 구간 (무차입 · 시드 3개)

```
              1001   2002   3003    구간(평균)
구멍가게         1      1      1        1일
동네슈퍼        53     72     71       64일
스타트업       151    203    195      118일
중소기업       244    283    295       91일
중견기업       542    570    546      279일   ← 여전히 최장 (전체의 31%)
대기업         648    720    713      141일
글로벌그룹      667    725    741       17일   ← 대기업 조건과 거의 붙어 있다
```

이전(회사 38·시드 없음)의 중견기업 구간은 전체의 45% 였다. 지금은 31% 로
내려왔지만 **여전히 최장**이고, **글로벌그룹 구간 17일**도 그대로다 —
구조적 문제지 측정 잡음이 아니다 (남은 과제 2·9번).

**밸런스를 건드리면 반드시 `npm run sim` 으로 위 표와 비교할 것.**
시드가 같으므로 숫자가 다르면 그건 편차가 아니라 **내 변경의 결과다.**

### sim 실행 요령

부팅에 수십 초, 한 판에 2~5초가 든다. 실행 시간이 제한된 환경에서는 한 번에
전부 돌리지 말고 전략을 나눠라.

```bash
node tools/sim.mjs --runs=0                          # 등급 구간만
node tools/sim.mjs --noref --strats=occult --runs=3  # 전략 하나만
node tools/sim.mjs --noref --strats=occult --desk    # 사장실 결재를 쓰는 봇
node tools/sim.mjs --seeds=7,8,9                     # 다른 시드로
```

> **`npm run sim` 은 빌드부터 한다. `node tools/sim.mjs` 는 안 한다.**
> 하니스가 `dist/company-tycoon.html` 을 띄우므로, 소스만 고치고 `node` 로
> 바로 돌리면 **이전 빌드를 재는 것이다.** 밸런스를 고쳤는데 숫자가 한 자리도
> 안 바뀌면 이걸 의심할 것 — 실제로 한 번 걸렸다.

`--desk` 는 기본 봇에 없다. 결재를 기본으로 켜면 기준선이 통째로 바뀌므로,
'결재까지 챙기는 플레이'를 따로 재기 위한 스위치로 분리해 뒀다.

---

## 7. 지금까지 내린 설계 판단 (전제)

기획서에 비어 있어 임의로 결정한 것들. 되돌리려면 여기를 보면 된다.

1. **등급 7단계** — 기획서 3-2절에는 `동네슈퍼`가 있고 4-1 표에는 없다. 초반 피드백 구간이 필요해 3-2 기준 7단계로 가고 4-1 목표치를 보간했다. **기획서 두 절이 어긋나 있으니 원본을 맞춰두는 게 좋다.**
2. **국제정세 이벤트 제외** — 기획서에 '잠정 보류'로 표기
3. **등급 목표의 '자금'을 순자산(자금−부채)으로 변경** — 대출로 목표를 우회하는 구멍을 막기 위함
4. **저장** — `window.storage` → `localStorage` → 메모리 순 폴백. 60일마다 오토세이브. 포맷 변경 시 `state.js` 의 `SAVE_V` 를 올리면 구버전 세이브 폐기. **현재 v10** (맵을 10×10 블록으로 키우며 lot 좌표가 바뀌어 구버전 세이브는 폐기된다)
5. **상호 접미사를 업종에서 파생** — `늘봄바이오 — 제약`처럼 정합을 맞춤
6. **설정은 사운드만** — 요구사항이 그랬다. 항목이 늘면 `ui/settings.js` 의 `ROWS` 에 한 줄 추가
7. **사운드는 절차 합성** — 음원 파일을 두면 '외부 에셋 0'이 깨진다. 대신 음색 선택지가 좁다.
   본격적인 BGM 이 필요해지면 그때 원칙을 깰지 정할 것
8. **난수는 시드 고정** — `core/rng.js`(mulberry32). 스트림이 둘이다.
   `game` 은 결과에 영향을 주는 전부(매물 배치·인수 판정·이벤트)이고 세이브에
   상태(uint32 하나)가 남는다. `view` 는 차량·손님·머리 모양처럼 연출에만 쓰고
   저장하지 않는다. **섞으면 안 된다** — 사옥 창을 한 번 더 열었다는 이유로
   그날의 인수 판정이 달라지면 시드 고정이 무의미해진다.
   재현은 `game.newState('이름', 시드)`. 세이브에 `seed` 가 남아 있다
9. **그래픽 스택은 캔버스 2D 유지** — three.js 는 철회했다. 회전 + 해상도 상향으로 갈음.
   남은 건 재작화뿐이고, 그건 스택 판단이 아니라 아트 작업이다 (`GRAPHICS.md`)

---

## 8. 시뮬레이션으로 잡은 버그 이력

같은 문제를 다시 만들지 않도록 남긴다.

| 문제 | 원인 | 조치 |
|---|---|---|
| 1주차부터 영구 적자 | 초기 인건비 > 매출 | 매출·급여 계수 재조정, 자본 마이너스 4개월 파산 조건 추가 |
| 세무조사로 즉사 | 추징금이 시총 비례 | `bill()`로 보유 자금 기준 상한 도입 |
| 레버리지가 무조건 우월 | 인수 자산이 즉시 100% 수익 | ① 등급 목표를 순자산 기준으로 ② 통합 기간(PMI) 도입 ③ 연체 시 담보 계열사 실제 압류 |
| 중견기업에서 영구 정체 | 규제 완화 이벤트 누적으로 난이도가 영구 하락 → "난이도 상 인수" 조건 달성 불가 | `diff0`(원래 난이도) 보관해 등급 조건 판정에 사용, 규제 완화에 바닥 설정 |
| 순위가 중반 내내 5000위 고정 | 멱법칙 매핑이 하단에서 둔감 | 볼록 로그 매핑으로 교체 (`rankCurve`) |
| 대기업 등급이 3일만 유지 | 순자산 1조와 순위 100위가 사실상 동시 충족 | 순위 곡선 재설계로 100위에 실질 부여 |
| 상호와 업종 불일치 (`늘봄에프앤비 — 미디어`) | 이름 접미사와 업종이 독립 | 업종별 접미사 풀에서 파생 |
| **타이틀에서 설정이 안 열림** | 모달 레이어 `z-index: 50` < 타이틀 `60`. 버튼은 눌리는데 모달이 타이틀 **뒤에** 열렸다 | 모달을 `z-index: 80` 으로 올려 항상 최상단에 두고, 스모크에 타이틀→설정 경로 추가 |
| **음소거가 창을 닫아야 반영됨** | `renderModal()` 은 `cfg.body` 문자열을 그대로 다시 그린다. 본문을 새로 만들지 않아 옛 HTML 이 다시 그려졌다 | 토글 시 `cfg.body = settingsBody()` 로 갱신한 뒤 `renderModal()` |
| **이벤트 빈도를 3.5배 올렸더니 초반 파산** | 건당 비용은 그대로라 시간당 지출이 3.5배가 됐다 | `BAL.eventCostScale = 0.30`. **빈도와 비용은 항상 같이 본다** |
| **저장은 되는데 이어하기가 안 됨** | `newState` 는 `v: 5` 로 쓰는데 `loadGame` 이 `d.v === 3` 을 검사. 세이브를 매번 폐기하고 조용히 새 게임으로 갔다 | `state.js` 에 `SAVE_V` 상수를 두고 **두 곳이 그것만 보게** 함. 스모크에 `saveInfo()` 재적재 검사 추가 |
| **상호판을 옮겼더니 클릭이 안 됨** | 해상도 스케일 중 그리는 상자 높이만 16 으로 옮기고 판정 상자(`plateBoxes.h`)는 11 로 남았다 | 높이를 `bh` 변수 하나로 묶어 **그리는 쪽과 판정 쪽이 같은 값을 보게** 함. 스모크의 탐침 좌표도 같이 옮겼다 |
| **배포본만 흰 화면 (검증은 전부 통과)** | `canvas.js` 가 `HH` 를 import 하지 않고 썼다. 번들러가 스코프를 합치므로 `dist` 에서는 보이고, `src/` 를 직접 서빙하는 GitHub Pages 에서만 `ReferenceError` 로 죽었다 | `npm run check`(`tools/modcheck.mjs`) 신설 — **진짜 ESM 그래프를 노드에서 평가한다.** `npm run smoke` 가 이걸 먼저 돌린다. 검증 도구가 전부 번들만 보고 있었던 게 근본 원인이다 |
| **진행 막대가 100%를 넘어 그려짐** | 좌표 스케일 코드모드가 `clamp(v, 0, 1)` 의 상한 1 까지 1.5배로 곱했다 | 비율·나머지 연산·인덱스 오프셋은 스케일 대상이 아니다. **기계적 변환 뒤 diff 를 전부 읽는 단계를 건너뛰지 말 것** |

### 화면이 흐릿하던 문제 — 원인 3개가 겹쳐 있었다

| 원인 | 계측 |
|---|---|
| **Galmuri가 로드된 적이 없다** | `cdn.jsdelivr.net/gh/quiple/galmuri/…` 가 404. 전부 시스템 고딕으로 폴백 |
| **폰트를 설계 크기 밖에서 썼다** | Galmuri11은 12px 설계인데 10·11·13·14px 혼용, Galmuri9는 10px 설계인데 7·8·9px 혼용 |
| **캔버스를 CSS로 분수 배율 확대** | `fitCanvas` 가 임의 float 배율을 계산 → `image-rendering:pixelated` 여도 픽셀이 1~2칸으로 불균등 배분 |

### 손님이 떨면서 걷던 문제

걷기 프레임을 `Math.floor(frame / 8 + p.x) % 2` 로 뽑았다. `p.x` 가 매 프레임
소수점으로 변해 발 모양이 불규칙하게 튀었다. 위상은 **누적 이동 거리**로 뽑아야 한다.

조치와 앞으로 지켜야 할 규칙은 **`RENDER.md`** 에 따로 정리해 뒀다.
캔버스·폰트 크기·쿼터뷰 도형을 건드리기 전에 그 문서를 먼저 볼 것.

### 모듈 분할 때 잡은 것
- `S`, `pausedSpeed`, `TAB`, `customers` — import 바인딩 재할당 위반 → 각각 `setS()`, `clock.js` 소유, `setTab()`, 제자리 비우기로 전환
- `label()` → `drawLabel()` 개명 — 모달 선택지의 `label` 프로퍼티와 이름이 겹쳐 `ui`가 `render`를 잘못 참조

---

## 9. 검증 방법

```bash
npm run check   # ES 모듈 무결성 — src/ 를 그대로 로드해도 이름이 다 잡히는가.
                # 번들(dist)에서는 안 터지고 배포본에서만 터지는 버그를 잡는다.
                # smoke 가 이걸 먼저 돌린다
npm run smoke   # 72 케이스: 타이틀→설정→창업, 300일 진행, 7개 탭, 협상 전 사이클,
                # 대출·상환, 주식 매매, 살굿, 저장, 세이브 재적재,
                # 설정(타이틀 진입·볼륨·음소거 즉시 반영), 회사 수, 상장 기준,
                # 클릭 판정(실루엣·상호판), **카메라 회전 4방향**, **무한 진행**,
                # **시드 고정**(같은 시드 = 같은 판 · 연출 난수가 안 샌다),
                # **세이브가 난수열을 잇는다**, **계열사 특성**(태그 부여율·결정론·
                # 퍼크 상한·사업부 결성/해체·담보 매각 차단·투자·재편), 출력 무결성, 런타임 에러
npm run shot    # shots/{title,city,store,icons,view0..3,facil0,facil3}.png
                # + shots/style-{tower,lab,plant,neon,shop}.png — 건물 5종을
                #   업종을 통째로 바꿔 3x 로 찍는다. 한 종류씩 대조하는 용도
                # — 캔버스를 실제 픽셀로 그려 눈으로 검수
npm run sim     # 전략 4종 × 시드 다수. 등급 구간 + 엔딩 분포 출력
                # 오래 걸린다 — 쪼개는 법은 §6 'sim 실행 요령' 참고
```

> **sim 을 백그라운드로 돌리지 말 것.** 샌드박스류 환경에서는 셸 호출이 끝날 때
> 배경 프로세스가 죽는다. 그러면 로그가 중간에 멈춰 **게임이 무한 루프에 빠진 것처럼
> 보인다.** 실제로 이걸 한참 디버깅했는데 게임 쪽 문제가 아니었다. 반드시 포그라운드로,
> 시간이 모자라면 `--strats` 로 나눠 돌려라.

스모크는 캔버스를 스텁으로 삼켜서 **"터지지 않는다"까지만** 본다. 렌더를 고쳤으면
`npm run shot` 으로 그림을 직접 볼 것. 다만 노드에는 Galmuri가 없어 글자 모양은
실제와 다르다 — 배치와 겹침 확인용이다.

`smoke` 와 `shot` 은 **`dist/company-tycoon.html`(빌드 산출물)을 jsdom에 띄워** 검사한다.
번들 과정의 문제까지 같이 잡힌다. **다만 번들만 보면 놓치는 게 있다** — 번들러가
스코프를 합치므로 import 누락이 안 보인다. 그래서 `npm run check` 가 `src/` 쪽을
따로 본다 (§8 버그 이력). 브라우저 콘솔에서는 `game.S`, `game.tickDay()`로 내부 접근 가능.

현재 상태: **smoke 72/72 통과, 런타임 에러 0건, 출력 NaN/undefined 0건.**

---

## 10. 남은 과제 (우선순위 순)

### 게임 설계
1. **~~계열사별 개별 운영~~ → 단계 1 로 해소.** 태그 10종 + 업종 퍼크 10종 +
   사업부, 그리고 매각/투자/재편이 들어갔다 (`core/tags.js`·`systems/subs.js`·`ui/subPanel.js`).
   **다만 `tools/sim.mjs` 봇은 매각·투자·재편을 한 번도 쓰지 않는다** — 계측된 것은
   태그와 퍼크의 수동적 효과뿐이고, 능동 운영의 밸런스는 아직 안 잰 상태다.
   봇에 전략을 하나 더 붙이는 게 다음 순서다
2. **중견기업 구간 287일** — 단계 1 이후에도 그대로다 (279 → 287). 전체의 34%.
   사업부가 중간 목표는 만들어 줬지만 **소요 일수는 안 줄었다.** 3시드 모두 동일.
   밸런스 버그가 아니라 무차입 경로의 구조적 특성(자기자본으로 매물 값의 2.6배를
   모아야 함)이다. 콘텐츠를 얹을지 목표를 낮출지 판단 필요
3. **레버리지가 리스크 없이 16% 빠르다** — 무차입 886일 vs 레버리지 742일.
   압류가 거의 안 터진다. 인수금융 금리나 상환 기간을 조이면 경계가 선명해짐.
   **극단 차입 + 로비도 안 죽는다** (수사 6~62에서 멈춤). 완주가 빨라진 만큼
   수사가 쌓일 시간이 줄었다 — 수사 증가율을 일당이 아니라 **행위당**으로 바꿔야 한다
4. **~~무속 루트가 반드시 스캔들로 끝난다~~ → 해소 확인.**
   결재 없이 595/631/640일 전멸, 결재를 쓰면 1,348/1,352일 완주 (미신 39~54).
   의도대로 "리스크를 돈으로 갚는" 구조가 작동한다
5. **찌라시·주식이 성장에 거의 기여하지 않는다** — `stock.holds` 를 `mna.js`·`companyPopup.js`
   어디서도 참조하지 않는다. 지분을 아무리 모아도 인수에 영향이 없어서 주식이 별도의
   돈놀이로 떠 있다. 지분율을 협상 성공도·인수가에 연결하면 축이 하나 붙는다
6. **글로벌그룹 구간 16~51일** — 대기업 조건과 사실상 붙어 있어 등급 하나가 낭비된다

### 그래픽 (다음 작업)
6. **~~해상도 B안 2단계 — 좌표계 스케일~~ → 완료.** 타일 48×24 · `PX=1` · 블록 10×10 ·
   회사 80. 계측과 주의점은 `GRAPHICS.md` §2단계
7. **~~3단계 건물 재작화~~ → 완료.** 다섯 종이 공유하는 마감 프리미티브
   (`pane`·`windowGrid`·`floorBands`·`cornerPost`·`parapet`·`roofDeck`·`awning`)를
   먼저 만들고 그 위에 종류별 실루엣을 얹었다. 규칙은 `RENDER.md` §8,
   무엇을 왜 그렇게 했는지는 `GRAPHICS.md` §3단계.
   **남은 재작화는 채움 건물(오피스·아파트·상가)과 교외 지형이다** —
   지금은 회사 건물이 채움 건물보다 마감이 좋아 시선이 제대로 가지만,
   교외 주택은 여전히 32×16 시절 그림을 늘린 상태다
8. **~~좁은 화면에서 가장자리가 잘린다~~ → 드래그·줌으로 해소.** 다만 1x 에서
   전체가 안 들어오는 화면은 여전히 있다 — 월드가 1536×824 라 그보다 좁으면 잘린다

### 재고 도입 후 밸런스 (재측정 필요)

`retailUpkeep` 을 0.42 → 0.20 으로 낮추고 그만큼을 발주 비용으로 옮겼다.
자동 발주 기준 매장 총비용은 잠재매출의 **0.448배** (0.20 운영비 + 0.248 발주,
자동 할증 12% 포함). 수동으로 50% 이상씩 채우면 0.403배로 약 10% 싸다.
방치하면 매출이 35%로 떨어지고 비용도 같이 줄어 순이익이 대략 절반이 된다.

`sim` 은 자동 발주를 켠 상태로 돈다 — '관리는 하는' 플레이를 대표하는 보수적 기준선.

**완료.** 4전략 × 3시드를 전부 완주시켰다. 결과는 §6 표에 반영했다.
재고 도입으로 전 전략이 이전보다 5~10% 빨라졌는데, `retailUpkeep` 을 0.42 → 0.20 으로
낮춘 만큼을 발주로 다 회수하지 못한 것으로 보인다. 조일 거면 `invUnit`(0.031)이
1차 손잡이다.

### 밸런스 (신규)
8. **중견기업 구간 424일** — 여전히 최장. 여기를 더 줄이려면 `subYield` 가 아니라
   난이도 '상' 매물의 공급을 손봐야 한다 (매물 운에 걸려 있다)
9. **대기업 → 글로벌그룹이 5일** — 두 조건이 사실상 붙어 있어 등급 하나가 낭비된다.
   글로벌그룹 진입 조건을 순위 쪽으로 분리할 것
10. **1위 도달이 1x 로 5시간** — 25% 줄였지만 여전히 배속 전제다.
    무한 진행이라 '완주 압박' 은 없어졌으나, 더 줄일지 판단 필요
11. **사운드가 효과음 수준** — 배경음이 4마디 루프라 오래 들으면 지겹다.
    1일 15초라 한 판이 길어져 더 두드러진다

### 코드
12. **테스트가 스모크 수준**

 — `systems/`는 순수 함수라 노드에서 직접 단위 테스트하기 좋은데 지금은 jsdom을 거친다. `core`/`systems`만 import하는 유닛 레이어를 두면 훨씬 빨라짐
13. **번들러 제약** — 모듈을 한 스코프로 합치므로 최상위 이름이 고유해야 한다. 겹치면 빌드가 멈추며 알려주지만, 파일이 늘어나면 부담이 될 수 있다. 그때는 esbuild 도입을 재검토
14. **`ui/tabs.js`** — 창 내용 6종이 한 파일. 늘어나면 창별 분리
15. **죽은 import** — `ui/hud.js` 와 `ui/companyPopup.js` 가 `TAB` 을 import 하지만 안 쓴다 (이전부터 그랬음)
16. **타입 없음** — JSDoc + `checkJs`만 켜도 상태 객체 형태는 잡힌다

---

## 11. 새 세션 시작 시

1. `company-tycoon-src.zip` 압축 해제
2. `개발서버-실행` 더블클릭 → 게임이 뜨는지 확인
3. 검증까지 돌리려면 `npm install` 후 `npm run smoke` (24/24 나와야 정상)
4. `npm run sim`으로 밸런스 기준선 확인 (§6 표와 대조)
5. 작업 시작

기획서 원본(`NHN_사전과제.md`)과 프로젝트 지침(`CLAUDE.md`)은 프로젝트 파일에 그대로 있다.

렌더를 건드릴 거면 **`RENDER.md`**, 스택을 바꿀 거면 **`GRAPHICS.md`** 를 먼저 볼 것.
